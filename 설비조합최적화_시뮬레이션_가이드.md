---
title: 신재생에너지 설비조합 최적화 시뮬레이션 — 기능 가이드
date: 2026-06-23
tags: [설비조합최적화, 시뮬레이션, 가이드, 신재생에너지, LP최적화]
대상: 도메인 검토자·기능 사용자·후속 개발자
근거코드: optimize/(optimizer·targetCalculator·optimizeUI·powerEstimator), data/(설비최적화·경관계수·제약가중치·전력원단위), settings/(settingsStore·settingsUI)
---

# 설비조합 최적화 시뮬레이션 가이드

> 건축물의 **신재생에너지 의무설치비율(및 전력생산비율)을 충족하는 설비 조합**을 자동 탐색하고,
> 사용자 요구도(비용·디자인·법적심의 등)에 따라 **순위화**하는 기능. 본 문서는 입력·기준조건·가중치·점수계산 전 과정을 설명한다.

---

## 0. 한눈에 보기 — 처리 흐름

```
[입력]                    [라이브러리]                 [엔진]                       [출력]
 법적규제 기준             설비 8종(ⓐ~ⓡ)              ① 조합 생성(구조 다양화)      순위 카드(상위 10 + 차원 챔피언)
 전력생산비율(선택)   ──►  경관계수          ──►       ② 용량 LP(5개 목적함수)  ──►  · 종합점수 / 강점 태그
 가용 면적                 제약 매트릭스(9요인)         ③ 필터링·중복제거             · 6 목표값 / 정성 막대
 사용자 요구도(7차원)      전력 원단위                 ④ 정규화·가중합 순위화        · 9요인 제약 프로파일
 지자체(대지위치)          OPT_CONFIG(상수)            ⑤ 강점 태그·챔피언            · AI 설명 / 보고서 출력
```

- **하드 제약(반드시 충족)**: 의무설치비율, 전력생산비율, 공간별 가용면적, 지열 최소의무
- **소프트 목적(점수화)**: 초기비용·운영비·인센티브·디자인·시공성·의무근접·법규제약 (7개 요구도 차원)
- 두 축은 섞지 않는다 — 제약은 후보 통과 여부, 목적은 통과 후보의 순위를 정한다.

---

## 1. 입력 (Input)

UI: 좌측 패널 **「설비조합 최적화 시뮬레이션」** 탭. 코드: `optimize/optimizeUI.js`의 `collectCtx()`.

### 1.1 법적규제 기준 (필수)

| 입력 | 필드 | 설명 |
|------|------|------|
| 연간 에너지소요량 (kWh/yr) | `opt-energy-demand` | 의무비율 분모. **사업정보 계산결과(Output1) 자동 연동** |
| 의무설치비율 (%) | `opt-req-ratio` | 충족해야 할 의무비율. Output2 자동 연동 |
| 지열 최소 의무비율 (%) | `opt-geo-ratio` | (선택) 서울시 등 지열·수열 의무. 미입력 시 미적용 |

> 지열 의무 입력 시 건축면적(`inp-건축면적`)을 함께 사용해 **옵션1(면적 기준)·옵션2(생산량 기준)**을 각각 풀어 유리한 쪽을 추천한다(§4.4).

### 1.2 전력생산비율 (선택)

| 입력 | 필드 | 설명 |
|------|------|------|
| 건물유형 | `opt-building-type` | 전력 원단위 조회 키 |
| 연면적 (㎡) | `opt-building-area` | 전력소비량 산정용 |
| 전력생산비율 기준 (%) | `opt-power-ratio` | 미입력 시 **전력 제약 자체를 제외** |
| 예상 전력소비량 | (자동계산) | `연간예상전력소비량 = Σ(전력원단위 × 연면적)` (`powerEstimator.js`) |

> 전력생산비율 기준 + 예상소비량이 **모두 있을 때만** 발전용 연료전지(PAFC·SOFC발전)가 후보에 포함된다(전력 목표가 없으면 비발전 설비가 공정히 검토되도록 제외).

### 1.3 가용 면적 (기준면적 대비 비율 %, 미입력 = 무제한)

| 공간 | 기준면적 | 대상 설비 |
|------|----------|-----------|
| 옥상 | 건축면적 | 수평 PV |
| 외피 | 예상 외피면적 = `4·√건축면적 × (연면적/건축면적) × 층고` (기본 3.5m) | BAPV·BIPV |
| 대지 | 대지면적 | 지열 |
| 기계실 | 건축면적 | 연료전지 |

- 비율(%)을 입력하면 `기준면적 × 비율%`가 해당 공간의 상한이 된다. 빈칸이면 무제한.

### 1.4 사용자 요구도 (7차원, 5등급)

라디오 5등급 = **매우높음 · 높음 · 보통 · 낮음 · 매우낮음**. 각 차원이 점수 가중치로 환산된다(§5).

| 차원 | 키 | 의미 | 기본값 |
|------|-----|------|--------|
| 초기비용 절감 | `초기비용` | 장비비(ⓓ) 최소 선호 | 보통 |
| 운영비 절감 | `운영비` | 연간 운영순익(ⓛ) 최대 선호 | 보통 |
| 인센티브 확보 | `인센티브` | ZEB 기여도(ⓝ) 선호 | 보통 |
| 디자인 보존 | `디자인` | 외관훼손·외부공간 최소 (경관계수 보정) | 보통 |
| 시공 용이성 | `시공성` | 공사기간·난이도 최소 | 보통 |
| 의무비율 근접 | `의무근접` | 과충족 회피(딱 맞게) | **높음** |
| 법적심의 적합성 | `법규제약` | 건축심의·환경영향평가 제약 회피 | 보통 |

> **건물유형 + 사업형태 선택 시** `LIB_요구도`에서 표준 요구도를 자동 반영한다(`autoLoadRequirements`, 매트릭스 전표는 §2.6). 시공성은 토지개발·기계실·공사기간·공사난이도 4개 하위등급의 평균으로 환산한다. 법규제약은 표준값이 없어 기본 보통으로 둔다.

### 1.5 지자체 (경관 보정용)

사업정보의 대지위치(`sel-대지위치`)를 `normalize대지위치()`로 정규화해 **경관민감도** 조회 키로 사용한다(§5.2).

---

## 2. 라이브러리·데이터

모두 `window.LIB_*` / `window.OPT_CONFIG` 전역으로 로드되며, **설정 모달에서 편집** 가능하다(localStorage 오버라이드, §7).

### 2.1 설비 데이터셋 (`LIB_설비최적화`) — 8설비 × ⓐ~ⓡ 18변수

출처: `26.06.18_...입력및조건값.xlsx` (2026-06-18 확정).

| 변수 | 기호 | 단위 | 용도 |
|------|------|------|------|
| 단위에너지생산량 | ⓐ | kWh/㎡·yr | 의무비율 분자 산정 |
| 원별보정계수 | ⓑ | – | 별표10 가중치 |
| 연간단위에너지생산량 | ⓒ=ⓐ×ⓑ | kWh/yr | **의무비율 분자** |
| 장비비 | ⓓ | 원/kW | **초기비용** |
| 생산량단가 | ⓔ=ⓓ/ⓒ | 원/(kWh/yr) | 참고 |
| 연간발전량 | ⓕ | kWh/yr | **전력생산비율 분자**(발전가능 설비) |
| 전기/열절약·연료비·유지비 | ⓗⓘⓙⓚ | 원/kW·yr | 순익 산정 |
| 순익 | ⓛ=ⓗ+ⓘ−ⓙ−ⓚ | 원/kW·yr | **운영순익** |
| 설치면적 | ⓜ | ㎡/kW | **면적 제약** |
| ZEB기여도·디자인훼손도·외부공간차지도·공사기간·공사난이도 | ⓝⓞⓟⓠⓡ | 5등급 | **정성 목표값** |
| 단위용량 / 스냅단위 | – | kW | 연료전지 정수용량·용량 스냅 |
| 설치공간 / 발전가능 | – | – | 면적분류 / 전력기여 여부 |

**핵심 수치 요약**

| 세부형식 | 형식 | ⓑ보정 | ⓒ생산량 | ⓓ장비비(원/kW) | ⓛ순익 | ⓜ면적 | 설치공간 | 발전 |
|----------|------|:-----:|:-------:|:--------------:|:-----:|:----:|:--------:|:----:|
| 고정식(수평)PV | 태양광 | 0.95 | 1,290 | 2,600,000 | 126,496 | 5 | 옥상 | O |
| 고정식(수직)BAPV | 태양광 | 0.95 | 1,290 | 2,600,000 | 126,496 | 5 | 외피 | O |
| BIPV | 태양광 | **6.12** | 5,649 | 5,000,000 | 78,496 | 5 | 외피 | O |
| 수직밀폐형(지열) | 지열 | 1.26 | 1,089 | 2,000,000 | 133,420 | 0.44 | 대지 | X |
| PEMFC(건물용) | 연료전지 | 2.20 | 16,313 | 25,000,000 | 604,045 | 0.6 | 기계실 | O |
| SOFC(건물용) | 연료전지 | **8.71** | 80,115 | 100,000,000 | 181,136 | 1 | 기계실 | O |
| PAFC(발전용) | 연료전지 | 0.93 | 8,170 | 11,360,000 | 373,590 | 0.2 | 기계실 | O |
| SOFC(발전용) | 연료전지 | 8.71 | 80,115 | 16,666,667 | 235,362 | 0.14 | 기계실 | O |

> ⓑ보정계수가 클수록 **소용량으로 의무비율을 충족**한다(BIPV 6.12·SOFC 8.71이 특히 유리). 반대로 ⓒ가 큰 연료전지는 정수용량(PAFC 440·SOFC발전 300 kW/기)이라 소규모 설계가 어렵다.

### 2.2 경관계수 (`LIB_경관민감도` · `LIB_경관영향`)

- **경관민감도**(지자체별): 강 1.0(서울·인천·세종·대구·광주) / 중 0.5(경기·부산·대전·미조사) / 약 0.0(울산)
- **경관영향**(세부형식별): BAPV 1.0 > 수평PV 0.7 > BIPV 0.2 > 지열·연료전지 0.0
- 적용: 디자인 목적함수만 보정 (§5.2).

### 2.3 법적심의 제약 매트릭스 (`LIB_제약가중치`) — 8설비 × 9요인

건축심의 + 환경영향평가 기준의 설비별 제약 정성등급(문서 `0622_..._제약가중치_매트릭스.md` §2).

| 제약요인 | PV-R | BAPV | BIPV | 지열 | 연료전지 |
|----------|:----:|:----:|:----:|:----:|:--------:|
| 경관·디자인 | 높음 | 매우높음 | 낮음 | 매우낮음 | 낮음 |
| 빛반사·빛공해 | 보통 | 높음 | 보통 | 매우낮음 | 매우낮음 |
| 일조장해 | 낮음 | 보통 | 낮음 | 매우낮음 | 매우낮음 |
| 소음·진동 | 매우낮음 | 매우낮음 | 매우낮음 | 보통 | 높음 |
| 생태면적률 | 높음 | 낮음 | 매우낮음 | 보통 | 낮음 |
| 지형·지질 | 매우낮음 | 매우낮음 | 매우낮음 | 매우높음 | 매우낮음 |
| 수환경 | 매우낮음 | 매우낮음 | 매우낮음 | 높음 | 매우낮음 |
| 동식물상 | 낮음 | 보통 | 보통 | 매우낮음 | 매우낮음 |
| 구조·안전 | 보통 | 보통 | 보통 | 낮음 | 낮음 |

> **9요인을 1개 숫자로 평균하지 않고** 요인별로 보존한다. 별도 계수표 없이 기존 등급점수 척도(5~1)를 재사용하고, 요인별 가중치를 줄 수 있다(§5.3).

### 2.4 전력 원단위 (`LIB_전력원단위`) ⚠ 잠정값

건물유형별 전력 원단위(kWh/㎡·yr). 전력소비량(전력생산비율 분모) 산정용. **실측 확정 전 잠정 추정값**(데이터센터 등 변동폭 큼).

### 2.5 최적화 상수 (`OPT_CONFIG`, `settingsStore.js`)

| 키 | 기본값 | 용도 |
|----|--------|------|
| 연료전지최대기수 | 2 | 대용량 연료전지 조합 전개 한도 |
| 외피층고 | 3.5 m | 외피면적 산정 |
| 경관보정사용 | true | 경관계수 디자인 보정 on/off |
| 법규제약사용 | true | 법규제약 차원 on/off |
| 요구도점수 | 매우높음5…매우낮음1 | 요구도→가중치 환산 |
| 등급점수 | 매우높음5…매우낮음1 | 정성·제약 등급→점수 |
| 제약요인가중치 | 9요인 균등 1 | 제약 요인별 상대 가중 |

### 2.6 건물유형별 사용자 요구도 (`LIB_요구도`)

건물유형 + 사업형태 선택 시 **표준 요구도를 자동 반영**하는 매트릭스(출처: `...입력및조건값.xlsx` 「건물유형별 규제사항 및 사용자 요구도」 시트). 사용자는 자동 반영 후 7차원 요구도를 자유롭게 조정할 수 있다.

| 사업형태 | 건물유형 | 초기비용 | 운영비 | 디자인 | 인센티브 | 토지개발 | 기계실 | 공사기간 | 공사난이도 |
|----------|----------|:------:|:----:|:----:|:------:|:------:|:----:|:------:|:--------:|
| 민간 | 공동주택 | 높음 | 보통 | 높음 | 높음 | 높음 | 낮음 | 높음 | 보통 |
| 민간 | 사무실·업무시설 | 높음 | 보통 | 높음 | 높음 | 높음 | 높음 | 높음 | 보통 |
| 민간 | 상가 | 높음 | 보통 | 보통 | 보통 | 높음 | 낮음 | 높음 | 보통 |
| 민간 | 주상복합 | 높음 | 보통 | 높음 | 높음 | 높음 | 낮음 | 높음 | 보통 |
| 민간 | 데이터센터 | 높음 | 높음 | 낮음 | 낮음 | 높음 | 높음 | 높음 | 높음 |
| 공공 | 공동주택 | 낮음 | 보통 | 보통 | 낮음 | 보통 | 낮음 | 높음 | 보통 |
| 공공 | 사무실·업무시설 | 낮음 | 보통 | 낮음 | 낮음 | 보통 | 높음 | 높음 | 보통 |
| 공공 | 상가 | 낮음 | 보통 | 낮음 | 낮음 | 보통 | 낮음 | 높음 | 보통 |
| 공공 | 주상복합 | 낮음 | 보통 | 보통 | 낮음 | 보통 | 낮음 | 높음 | 보통 |
| 공공 | 데이터센터 | 낮음 | 높음 | 낮음 | 낮음 | 보통 | 높음 | 높음 | 높음 |

- **시공성** 요구도 = 토지개발·기계실·공사기간·공사난이도 **4개 등급의 평균을 다시 등급으로 환산**(개발기획서 §4.5·H3).
- **법규제약** 요구도는 이 매트릭스에 표준값이 없어 자동 반영 시 기본 **보통**으로 둔다(§1.4).
- 원본은 3등급 입력(높음/보통/낮음)이며, **매우높음·매우낮음 세분은 설정 화면에서 건물유형별로 직접 조정**한다(도메인 판단).

### 2.7 상류 계산 라이브러리 (Output1·Output2 산정 — 최적화 입력의 출처)

아래 4개는 최적화 엔진이 **직접 읽지는 않지만**, 사업정보로부터 최적화 입력(연간 에너지소요량·의무설치비율)을 만들어내는 **메인 계산(Output1·Output2)**에 쓰인다. 설정 모달의 「기존 계산 공용 라이브러리」 그룹에서 편집한다.

| 라이브러리 | 전역변수 | 구조 | 역할 |
|------------|----------|------|------|
| 신재생에너지계수 | `LIB_신재생에너지계수` | 20행(에너지원·형식·단위에너지생산량ⓐ·원별보정계수ⓑ) | 신재생 생산량 산정 |
| 지역계수 | `LIB_지역계수` | 17개 시도 맵(value 보정계수·solar_annual) | Output1 지역 보정 |
| 단위에너지사용량 | `LIB_단위에너지사용량` | 건축물 종류별 배열(kWh/㎡·yr) | Output1 용도별 사용량 |
| 의무비율 | `LIB_의무비율` | 사업형태·지역·주거구분·카테고리(가~라)·연도(2024~2030) 맵 | 의무설치비율 기준값 |

**산정식 (메인 계산 → 최적화 입력)**
```
Output1 예상에너지사용량 = Σ_용도 [ 연면적 × 지역계수(value) × 단위에너지사용량 ]   → opt-energy-demand
Output2 설치비율          = Σ(설비 생산량) / Output1 × 100                          → 충족 판정
의무설치비율 기준         = LIB_의무비율[키] (사업형태·지역·주거·카테고리·연도 조회) → opt-req-ratio
```

**핵심값 예시**
- **지역계수(value)**: 대구 1.04·전북 1.04 / 서울·대전·경남 1.00 / 부산·울산 0.93 (태양광 일사 `solar_annual`은 별도 컬럼)
- **단위에너지사용량(kWh/㎡·yr)**: 의료시설 643.52 · 숙박시설 526.55 · 업무시설(공공) 371.66 · 교육연구시설 231.33
- **원별보정계수ⓑ**: BIPV 6.12 · SOFC 8.71 · PEMFC 2.20 · 지열 1.26 · PAFC 0.93 → **§2.1 설비 데이터셋의 ⓑ와 정합**
- **의무비율**: 공공 비주거 2026년 36%(2030년 40%) / 주거는 지역×인증등급(가~라)×연도, `자율`·미시행(빈칸) 혼재

> ⚠ `LIB_의무비율`은 행이 많아(지역×주거구분×카테고리) 설정 모달에서 **행 필터**로 검색·편집한다. 본 가이드는 구조·조회 방식만 다루며 전체 값은 라이브러리 파일을 참조.

---

## 3. 최적화 기준 조건 (제약)

### 3.1 하드 제약 (반드시 충족 — 미충족 후보는 탈락)

1. **의무설치비율**: `Σ(ⓒ·용량) / 연간에너지소요량 × 100 ≥ 의무설치비율기준`
2. **전력생산비율**(선택): `Σ(ⓕ·용량, 발전가능) / 연간예상전력소비량 × 100 ≥ 전력생산비율기준`
3. **공간별 면적**: 공간별 `Σ(ⓜ·용량) ≤ 가용면적` (옥상/외피/대지/기계실 각각)
4. **지열 최소의무**(선택): 옵션1 `지열면적 ≥ 건축면적×비율%` 또는 옵션2 `지열생산량 ≥ 의무총량×비율%`

> 경계해 부동소수 오차 허용(`eps=1e-6`) — 예: 9.99999%를 10% 충족으로 인정.

### 3.2 소프트 목적 (점수화 — §5)

7개 요구도 차원. 하드 제약을 통과한 후보들 사이의 **상대 순위**만 결정한다.

---

## 4. 알고리즘 흐름 (`optimizer.js`)

### 4.1 조합 생성 — 구조 다양화 (`generateCombinations`)

조합 = **PV옵션 × 연료전지분기 × 건물설비 부분집합**

- **PV**: `[없음, 수평PV, BAPV, BIPV]` 중 택1
- **대용량 연료전지**(PAFC·SOFC발전): `없음 + (종류 × 1~최대기수)`, 1종 배타 — *전력 목표 있을 때만*
- **건물설비**(지열·PEMFC·SOFC건물): **부분집합 전수 열거**(공집합 포함)

> 부분집합 열거 이유: LP가 최소비용 단일설비로 수렴해 조합이 붕괴하는 것을 막고, "지열-only/PV+지열/PEMFC-only" 등 구조적으로 다른 후보를 확보해 다양한 요구도를 대표하게 한다.

### 4.2 용량 LP — 5개 목적함수 (`solveCapacity`)

각 조합을 **5개 목적함수로 각각 풀어** 서로 다른 용량 배분을 유도한다(`objCoeff`).

| 목적 | 방향 | 목적계수(설비당) |
|------|:----:|------------------|
| cost | 최소 | 장비비 ⓓ |
| benefit | 최대 | 순익 ⓛ |
| incentive | 최대 | 등급점수(ⓝ ZEB) |
| design | 최대 | (역방향 디자인훼손ⓞ + 외부공간ⓟ)/2 |
| constraint | 최대 | 9요인 평균 제약 적합도(1−강도) |

- **고정설비(연료전지 기수)** 기여를 제약 우변에서 차감 → 잔여 제약으로 연속설비 용량을 푼다.
- **최대화 목적**은 의무 충족 + **과설치 상한 20%**(`OVER설치=1.2`)를 둬 무한 설치를 막는다.
- 연속 LP 해는 **스냅단위 정수배로 올림**(`snapToUnit`) — 의무 충족 유지.

### 4.3 필터링·중복 제거

- LP infeasible 제거 → 법적규제 재검증(eps) → 스냅으로 늘어난 면적 재검증
- 동일 `설비:용량` 조합 통합(dedup)

### 4.4 지열 의무 옵션 비교 (해당 시)

지열 의무비율 > 0이면 옵션1(면적)·옵션2(생산량)을 각각 최적화하고, **1위 종합점수가 높은 쪽을 추천**한다(서울 "면적 또는 생산량" 관계).

---

## 5. 가중치 적용 & 점수 계산 (핵심)

### 5.1 요구도 → 가중치 환산 (`deriveWeights`)

각 요구도 등급을 **요구도점수**(매우높음5·높음4·보통3·낮음2·매우낮음1)로 바꾸고, **합이 1이 되도록 정규화**한다.

```
raw_k = 요구도점수[요구도_k]          (k = 7개 차원)
w_k   = raw_k / Σ raw                 (Σ w = 1)
```

> 예: 의무근접=높음(4), 나머지 6개=보통(3) → raw=[3,3,3,3,3,4,3]=22 → w(의무근접)=4/22≈0.18, 나머지 각 ≈0.136.

### 5.2 6개 목표값 산출 (`targetCalculator.js`)

조합의 채택 설비(items = [{설비, 용량}])로부터:

| 목표값 | 산식 |
|--------|------|
| 초기비용 | `Σ ⓓ·용량` |
| 운영순익 | `Σ ⓛ·용량` |
| 설치면적 | 공간별 `Σ ⓜ·용량` |
| 법적규제 | 의무비율·전력생산비율 + 충족 여부 |
| 정성(ZEB·디자인·시공성·인센티브) | 용량가중 "좋음점수" 평균 (아래) |
| 제약 | 9요인 프로파일 + 요인가중 적합도 (§5.3) |

**정성 점수 규칙**
- 등급점수: 매우높음5 … 매우낮음1
- **역방향 지표**(디자인훼손ⓞ·외부공간ⓟ·공사기간ⓠ·난이도ⓡ): 낮을수록 좋음 → `좋음점수 = (최대+최소) − 등급점수` = `6 − 등급점수`
- ZEB = `용량가중평균( 등급점수(ⓝ) )`
- 디자인 = `용량가중평균( (좋음ⓞ + 좋음ⓟ)/2 )` → **경관계수 보정**
- 시공성 = `용량가중평균( (좋음ⓠ + 좋음ⓡ)/2 )`
- 인센티브 = ZEB (proxy — 정밀 용적률% 매핑은 ECO2 연계 시 예정)

**경관계수 보정 (디자인 전용)**
```
계수      = 1 + 경관민감도(지자체) × 경관영향(세부형식)
보정디자인 = gmax − (gmax − 기본디자인) × 계수      (gmax=등급점수 최댓값, [gmin,gmax] 클램프)
```
→ 경관영향=0(지열·연료전지)은 어느 지자체에서도 계수=1(불변). 강한 지자체일수록 노출형 PV·BAPV의 디자인 패널티↑.

### 5.3 법규제약 — 9요인 프로파일 & 적합도

매트릭스를 평균 1개로 붕괴시키지 않고 **요인별로 산출**한다.

```
요인별 점수(f)   = 용량가중평균( 등급점수(매트릭스[설비][f]) )   (1~5, 높을수록 제약↑)
요인별 적합도(f) = (gmax − 점수(f)) / (gmax − gmin)               (0~1, 제약 낮을수록 1)
종합 적합도      = Σ_f [ 제약요인가중치(f) × 적합도(f) ] / Σ_f 제약요인가중치(f)
```

- 기본은 9요인 균등 가중. 입지·건물유형에 따라 특정 요인(예: 도심=경관, 데이터센터=소음)을 가중할 수 있다.
- `법규제약사용=false`이면 적합도=1(중립)로 순위에 영향 없음. 프로파일은 표시용으로 항상 산출.

### 5.4 정규화 (7차원 → 0~1)

| 차원 | 정규화 방식 | 방향 |
|------|-------------|:----:|
| 초기비용 | 조합집합 **min-max** | 낮을수록 1 |
| 운영순익 | 조합집합 **min-max** | 높을수록 1 |
| 인센티브 | **고정척도** `(v−gmin)/gspan` | 높을수록 1 |
| 디자인 | 고정척도 | 높을수록 1 |
| 시공성 | 고정척도 | 높을수록 1 |
| 의무근접 | `min(1, 의무기준/실제비율)` | 딱 맞을수록 1(과충족 0↓) |
| 법규제약 | 종합 적합도(0~1) | 제약 낮을수록 1 |

> 비용·순익은 절대범위가 가변이라 **min-max**, 정성·제약은 척도가 고정(등급점수)이라 **고정척도**를 쓴다. 고정척도는 미미한 raw 차이가 0↔1로 과장되는 것을 막는다. (min-max는 전 조합 동일값이면 중립 1.0 처리)

### 5.5 종합 점수 & 순위

```
종합점수 = Σ_k  w_k × n_k        (k = 7개 차원, 0~1)
```
- 점수 내림차순 정렬 → rank 부여. 화면 점수는 `×100`.

### 5.6 강점 태그 · 챔피언 (`tagDimensions`)

가중치와 무관하게 "이 조합이 어떤 요구도를 잘 만족하는가"를 표시.

```
rel = (정규화값 − min) / (max − min)        (차원별)
rel ≥ 0.85  → 해당 차원 태그 부여
최댓값 조합  → 챔피언(★)
차별성 없음(span < 0.05) → 태그 생략
```

---

## 6. 결과 표시 (`optimizeUI.render`)

- **표시 대상**: 종합점수 상위 10개 + **차원별 챔피언**(가중치 낮은 요구도의 대표 조합도 노출)
- **카드 구성**: 순위/점수/최적 배지 · 강점 태그칩(★챔피언) · 설비별 용량 막대 · 목표값(초기비용·연간순익·의무비율·전력생산) · 정성 막대(디자인·시공성·ZEB·**심의적합**) · **9요인 제약 프로파일(확장)** · AI 설명 버튼
- **배너**: 경관 보정 적용 안내(지자체 민감도), 지열 의무 옵션1/옵션2 비교 추천
- **AI 설명**: 조합별 추천 근거를 Gemini로 생성(설비 장단점·목표값 기반)
- **보고서 출력**: 조건표 + 순위표 + AI설명을 A4 보고서로 출력

---

## 7. 설정 편집 (⚙ 설정 모달, `settingsUI.js`)

좌측 상단 **⚙ 설정**에서 모든 값을 편집(localStorage 오버라이드, 저장 즉시 다음 계산 반영, 기본값 복원·JSON 내보내기/가져오기 지원).

- **최적화 가중치·상수**: 경관/법규제약 on·off, 연료전지 기수, 외피층고, 요구도점수·등급점수, **제약 요인별 가중치(9칸)**
- **라이브러리 테이블**: 설비최적화(ⓐ~ⓡ), 요구도, 전력원단위, 경관민감도·영향, **법적심의 제약 매트릭스(9요인 5등급 드롭다운)**, 계산 공용(신재생계수·지역계수·단위에너지·의무비율)

---

## 8. 한계·주의 (도메인 검토 필요)

1. **전력 원단위 잠정값** — `LIB_전력원단위`는 실측·공인통계 확정 전(특히 데이터센터). 전력생산비율 신뢰도에 직접 영향.
2. **제약 매트릭스 정성판정** — 경관·일조 등급은 공식문서 명문 임계치가 적어 도메인 추론 비중이 큼. 등급·요인가중치는 검토·조정 대상.
3. **경관민감도 미조사 지자체** — 비수도권 도(道)는 잠정 0.5(중). 추가 조사 시 갱신.
4. **인센티브 proxy** — 현재 ZEB기여도 기반. 정밀 용적률% 매핑은 ECO2 연계(P6) 시.
5. **단순 가중합 순위** — 완전한 Pareto front는 미구현(차원별 챔피언으로 부분 보완).
6. **PAFC 별표10** — 미등재 가정(ⓑ=0.93). 정식 등재 확인 시 값 갱신.

---

## 9. 빠른 사용 절차

1. 사업정보(대지위치·연면적 등) 입력 → Output1/2 계산 (의무비율·에너지소요량 자동 연동)
2. 「설비조합 최적화」 탭 진입 → 법적규제 기준 확인, (선택) 전력생산비율·가용면적 입력
3. 건물유형·사업형태 선택 → **표준 요구도 자동 반영** → 필요 시 7차원 요구도 조정
4. **최적 조합 탐색** 실행 → 순위 카드·태그·9요인 프로파일 확인
5. 관심 조합 **AI 설명 생성** → **보고서 출력**(PDF/Word/HTML)
6. 값 조정이 필요하면 **⚙ 설정**에서 라이브러리·가중치 편집 후 재실행

---

> 관련 문서: `신재생에너지_설비조합_최적화_시뮬레이션_개발기획서.md`(원본 명세), `HANDOFF_설비최적화.md`(개발 인수인계), `0622_신재생에너지별_제약가중치_매트릭스.md`(제약 매트릭스 근거).
