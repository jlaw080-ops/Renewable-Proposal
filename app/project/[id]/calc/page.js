import Card from "@/components/ui/Card";

export default function CalcPage() {
  return (
    <Card title="② 검토 계산">
      <p style={{ color: "var(--text-secondary)" }}>
        예상에너지사용량(Output1)과 설치비율·의무비율 판정(Output2)을 계산하는 화면입니다. Phase 3에서 구현됩니다.
      </p>
    </Card>
  );
}
