import Card from "@/components/ui/Card";

export default function OptimizePage() {
  return (
    <Card title="③ 최적화·AI 추천">
      <p style={{ color: "var(--text-secondary)" }}>
        설비조합 최적화 결과와 AI 추천을 확인하는 화면입니다. Phase 4에서 구현됩니다.
      </p>
    </Card>
  );
}
