import Card from "@/components/ui/Card";

export default function InfoPage() {
  return (
    <Card title="① 사업정보">
      <p style={{ color: "var(--text-secondary)" }}>
        사업형태·연도·대지위치(지도 검색)·용도별 연면적을 입력하는 화면입니다. Phase 2에서 구현됩니다.
      </p>
    </Card>
  );
}
