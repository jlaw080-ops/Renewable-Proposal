# UI/UX 전면 개편 (Technical Teal) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 앱 스킨을 ENERGINNO Theme T(잉크 네이비 #1F1F5C + 틸 #009BA4)로 전면 교체하고 A안 리뷰 결함 15건을 수정한다.

**Architecture:** `styles/tokens.css`의 토큰 **이름을 유지하고 값만 교체**해 컴포넌트 파급을 최소화. 다크 나브·스테퍼 상태·판정 밴드는 컴포넌트 국소 재구성. 계산 엔진·화면 플로우 무변경.

**Tech Stack:** Next.js(App Router, JS), Vanilla CSS + 커스텀 프로퍼티, `node --test`, Playwright(msedge 채널), NanumSquareNeo(`next/font/local`).

**스펙:** `docs/superpowers/specs/2026.08.09_UIUX개편_TechnicalTeal_설계.md` (승인됨)

## Global Constraints

- `legacy/`·`engine/`·`public/` 무수정 (엔진 동일성 원칙)
- `styles/tokens.css` 토큰 **이름 유지** — 값 교체·신설만 허용, 이름 변경 금지 (`--dot-step1~4` 삭제만 예외)
- 색 원칙: **텍스트·버튼 채움 = 딥 계열만** (#1F1F5C, #00757C, #00803A, #B23A3A, #B35400). 밝은 틸 #009BA4는 비텍스트(포커스 링·활성 바·게이지) 전용
- 테스트: `npm test` = `node --test "tests/*.test.mjs"` (Windows 글롭 따옴표 필수). 매 태스크 종료 시 전체 통과 확인
- 이 머신은 Dropbox 동기화 폴더라 `npm run build`/`dev`가 매우 느림 — 로컬 빌드 검증 금지, 시각 검증은 Task 14의 Vercel Preview로 일괄
- 커밋: `<type>: <한국어 설명>` (feat/fix/refactor/docs/test/chore), 브랜치 `redesign/teal`에서 작업. main 머지는 계획 범위 밖(사용자 승인 후)
- 문서 인코딩 UTF-8

---

### Task 1: 대비 게이트 테스트 (RED)

신규 팔레트가 WCAG 대비를 통과하는지 `tokens.css`를 파싱해 검증하는 테스트를 먼저 작성한다. 현행 토큰 값으로는 **실패해야 정상**(hint 2.5:1 등) — Task 2의 토큰 교체가 이 테스트를 통과시킨다.

**Files:**
- Create: `tests/tokensContrast.test.mjs`

**Interfaces:**
- Consumes: `styles/tokens.css`의 `--이름: #RRGGBB` 정의
- Produces: Task 2가 통과시켜야 할 게이트. 이후 색 조정 시 항상 이 테스트가 회귀 감지

- [ ] **Step 1: 테스트 작성**

```js
// tests/tokensContrast.test.mjs — 토큰 색 쌍 WCAG 대비 게이트 (텍스트 4.5:1 / 비텍스트 3.0:1)
import { test } from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

const css = readFileSync(new URL("../styles/tokens.css", import.meta.url), "utf8");
const tok = {};
for (const m of css.matchAll(/--([\w-]+):\s*(#[0-9A-Fa-f]{6})/g)) tok[m[1]] = m[2];

const lin = c => { c /= 255; return c <= 0.03928 ? c / 12.92 : ((c + 0.055) / 1.055) ** 2.4; };
const lum = h => {
  const x = h.slice(1);
  const [r, g, b] = [0, 2, 4].map(i => parseInt(x.slice(i, i + 2), 16));
  return 0.2126 * lin(r) + 0.7152 * lin(g) + 0.0722 * lin(b);
};
function ratio(f, b) {
  assert.ok(tok[f], `토큰 없음: --${f}`);
  assert.ok(tok[b], `토큰 없음: --${b}`);
  const [a, c] = [lum(tok[f]), lum(tok[b])];
  const [hi, lo] = a > c ? [a, c] : [c, a];
  return (hi + 0.05) / (lo + 0.05);
}

const TEXT_PAIRS = [ // 전경, 배경 — 4.5:1 이상
  ["text-primary", "bg-card"], ["text-primary", "bg-page"],
  ["text-secondary", "bg-card"], ["text-secondary", "bg-page"], ["text-secondary", "bg-card-inner"],
  ["text-hint", "bg-card"], ["text-hint", "bg-page"], ["text-hint", "bg-card-inner"],
  ["text-invert", "accent-primary"], ["text-invert", "accent-primary-dim"],
  ["accent-action", "bg-card"],
  ["color-pass", "color-pass-bg"], ["color-pass", "bg-card"],
  ["color-fail", "color-fail-bg"], ["color-fail", "bg-card"],
  ["color-na", "color-na-bg"],
  ["accent-warm", "accent-warm-bg"], ["accent-warm-dim", "bg-card"],
  ["nav-text", "nav-bg"], ["nav-text-dim", "nav-bg"],
];
const NONTEXT_PAIRS = [ // 3.0:1 이상 (포커스 링·활성 표시·게이지)
  ["border-focus", "bg-card"], ["accent-graphic", "bg-card"], ["nav-active", "nav-bg"],
];

test("텍스트 색 쌍 대비 4.5:1 이상", () => {
  for (const [f, b] of TEXT_PAIRS)
    assert.ok(ratio(f, b) >= 4.5, `--${f} / --${b} = ${ratio(f, b).toFixed(2)}:1 (< 4.5)`);
});
test("비텍스트 색 쌍 대비 3.0:1 이상", () => {
  for (const [f, b] of NONTEXT_PAIRS)
    assert.ok(ratio(f, b) >= 3.0, `--${f} / --${b} = ${ratio(f, b).toFixed(2)}:1 (< 3.0)`);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/tokensContrast.test.mjs`
Expected: FAIL — 현행 토큰에는 `accent-graphic`·`nav-bg` 등이 없고(`토큰 없음`), hint 대비도 미달

- [ ] **Step 3: 커밋**

```bash
git add tests/tokensContrast.test.mjs
git commit -m "test: 토큰 대비 WCAG 게이트 — 신규 팔레트 전제(RED)"
```

---

### Task 2: 토큰 값 교체 (GREEN)

**Files:**
- Modify: `styles/tokens.css` (전체 교체)

**Interfaces:**
- Produces: 아래 토큰 셋. 이후 모든 태스크가 이 이름·값을 전제. 신설 = `--accent-graphic`, `--accent-warm-bg`, `--nav-bg`, `--nav-text`, `--nav-text-dim`, `--nav-active`. 삭제 = `--dot-step1~4`

- [ ] **Step 1: tokens.css 색 섹션 교체** (타이포·간격·라운드·그림자·레이어·레이아웃·모션 섹션은 기존 그대로 유지)

```css
/* 디자인 토큰 — ENERGINNO Theme T (Technical Teal) 앱 이식
   원본: ~/.claude/skills/energinno-design-tokens/tokens.css (문서·슬라이드 정본)
   이 파일이 유일한 스타일 상수 원천. 컴포넌트 CSS에 리터럴 색·크기 하드코딩 금지
   원칙: 텍스트·버튼 채움은 딥 계열만, 밝은 틸(#009BA4)은 비텍스트 전용.
   대비 게이트: tests/tokensContrast.test.mjs */
:root {
  /* 캔버스·표면 */
  --bg-page: #F4F7F9;
  --bg-card: #FFFFFF;
  --bg-card-inner: #EFF2F6;
  --bg-input: #FFFFFF;
  --bg-input-readonly: #EFF2F6;
  --bg-hover: rgba(31, 31, 92, 0.05);

  /* 텍스트 (전 쌍 AA 실측 — 게이트 테스트 참조) */
  --text-primary: #1F1F5C;
  --text-secondary: #4A4A6B;
  --text-hint: #6A6A8C;
  --text-invert: #FFFFFF;

  /* 테두리 */
  --border-default: #D6D6E0;
  --border-strong: #B9BECF;
  --border-focus: #009BA4;

  /* 브랜드·액션 */
  --accent-primary: #1F1F5C;      /* 주 버튼 채움 (잉크 네이비) */
  --accent-primary-dim: #171745;
  --accent-primary-rgb: 31, 31, 92;
  --accent-action: #00757C;       /* 링크·강조 텍스트 (틸 딥) */
  --accent-action-dim: #005A60;
  --accent-graphic: #009BA4;      /* 게이지·활성 표시 — 비텍스트 전용 */
  --accent-warm: #B35400;         /* 경고·주의 */
  --accent-warm-dim: #B35400;
  --accent-warm-bg: #FDF1E5;

  /* 시맨틱 (판정 전용 — 장식 재사용 금지) */
  --color-pass: #00803A;  --color-pass-bg: #E4F5E9;
  --color-fail: #B23A3A;  --color-fail-bg: #FDECEC;
  --color-na:   #566070;  --color-na-bg:   #EFF2F6;

  /* 다크 나브 (시그니처) */
  --nav-bg: #1F1F5C;
  --nav-text: #FFFFFF;
  --nav-text-dim: #B1B1C6;
  --nav-active: #009BA4;

  /* ↓ 타이포·간격·라운드·그림자·레이어·레이아웃·모션 — 기존 값 그대로 */
}
```

주의: `--dot-step1~4` 블록은 삭제한다 (사용처는 Task 10에서 제거되므로 이 시점엔 `Stepper.jsx`가 미정의 변수를 참조하지만 런타임 오류는 없음 — 도트가 투명해질 뿐이고 Task 14 전에 해소됨).

- [ ] **Step 2: 게이트 통과 확인**

Run: `node --test tests/tokensContrast.test.mjs`
Expected: PASS (2 tests). 미달 쌍이 나오면 **전경 L을 낮춰** 조정 후 재실행 (색상 유지, 명도만)

- [ ] **Step 3: 전체 테스트**

Run: `npm test`
Expected: 기존 44 + 신규 2 전부 PASS

- [ ] **Step 4: 커밋**

```bash
git add styles/tokens.css
git commit -m "feat: 토큰 팔레트 Theme T 교체 — 잉크 네이비·틸, 전 쌍 AA 게이트 통과"
```

---

### Task 3: NanumSquareNeo 자체 호스팅 + 타이포 규칙

**Files:**
- Create: `app/fonts/NanumSquareNeoTTF-bRg.woff2` (400) · `-cBd.woff2` (700) · `-dEb.woff2` (800)
- Modify: `app/layout.js`, `app/globals.css`, `styles/tokens.css`(--font-body 폴백), `components/ui/badge.css`·`button.css`·`field.css`·`stepper.css`·`table.css`(weight 500→700), `app/dashboard.css`(타이틀 800)

**Interfaces:**
- Produces: `--font-sans` = NanumSquareNeo(400/700/800). 이후 CSS는 400/700/800만 사용(500 금지 — 이 서체에 없음)

- [ ] **Step 1: woff2 다운로드**

```bash
mkdir -p app/fonts
curl -sL -o app/fonts/NanumSquareNeoTTF-bRg.woff2 "https://cdn.jsdelivr.net/gh/moonspam/NanumSquareNeo@1.0/NanumSquareNeoTTF-bRg.woff2"
curl -sL -o app/fonts/NanumSquareNeoTTF-cBd.woff2 "https://cdn.jsdelivr.net/gh/moonspam/NanumSquareNeo@1.0/NanumSquareNeoTTF-cBd.woff2"
curl -sL -o app/fonts/NanumSquareNeoTTF-dEb.woff2 "https://cdn.jsdelivr.net/gh/moonspam/NanumSquareNeo@1.0/NanumSquareNeoTTF-dEb.woff2"
ls -la app/fonts   # 3파일 각 수백 KB인지 확인 (0바이트면 다운로드 실패)
```

(나눔글꼴은 SIL OFL 유사 자유 라이선스 — 자체 호스팅 허용)

- [ ] **Step 2: layout.js 폰트 교체**

```js
import localFont from "next/font/local";
import { IBM_Plex_Mono } from "next/font/google";
import ToastProvider from "@/components/ui/ToastProvider";
import "./globals.css";

const sans = localFont({
  src: [
    { path: "./fonts/NanumSquareNeoTTF-bRg.woff2", weight: "400" },
    { path: "./fonts/NanumSquareNeoTTF-cBd.woff2", weight: "700" },
    { path: "./fonts/NanumSquareNeoTTF-dEb.woff2", weight: "800" },
  ],
  variable: "--font-sans",
  display: "swap",
});
const mono = IBM_Plex_Mono({ subsets: ["latin"], weight: ["400", "600"], variable: "--font-plex-mono" });
```

(metadata·RootLayout 본문은 기존 그대로)

- [ ] **Step 3: 타이포 규칙**

`app/globals.css`에 추가:

```css
h1, h2, h3 { line-height: 1.25; word-break: keep-all; }
```

weight 500 → 700 치환 (500은 이 서체에 없어 합성됨):
- `components/ui/badge.css:4`, `button.css:4`, `field.css:2`, `stepper.css:11`, `table.css:4`의 `font-weight: 500` → `font-weight: 700`
- `app/dashboard.css:3` `.dash__title`의 `font-weight: 700` → `font-weight: 800`

`styles/tokens.css` 폴백 정리: `--font-body: var(--font-sans), 'Apple SD Gothic Neo', 'Malgun Gothic', sans-serif;` ('Noto Sans KR' 제거)

- [ ] **Step 4: 테스트 + 커밋**

Run: `npm test` → 전부 PASS (시각 확인은 Task 14 프리뷰에서)

```bash
git add app/fonts app/layout.js app/globals.css styles/tokens.css components/ui/badge.css components/ui/button.css components/ui/field.css components/ui/stepper.css components/ui/table.css app/dashboard.css
git commit -m "feat: NanumSquareNeo 자체 호스팅 도입 — 헤딩 800·weight 500 정리"
```

---

### Task 4: Button — brand 변형 삭제·네이비 단일 CTA·포커스/프레스

**Files:**
- Modify: `components/ui/button.css`, `app/page.js:83`, `components/optimize/RecommendPanel.jsx:8`

**Interfaces:**
- Produces: Button variant는 `primary`(네이비 채움)·`ghost`·`danger`만. `variant="brand"`는 존재하지 않음 — 이후 태스크에서 사용 금지

- [ ] **Step 1: button.css 교체**

```css
.btn {
  display: inline-flex; align-items: center; justify-content: center; gap: var(--sp-2);
  border: 1px solid transparent; border-radius: var(--radius-sm);
  font-size: var(--fs-14); font-weight: 700; cursor: pointer;
  transition: background var(--dur-fast) var(--ease-out), border-color var(--dur-fast) var(--ease-out), scale var(--dur-fast) var(--ease-out);
}
.btn--md { height: 36px; padding: 0 var(--sp-4); }
.btn--sm { height: 28px; padding: 0 var(--sp-3); font-size: var(--fs-13); }
.btn:disabled { opacity: .5; cursor: not-allowed; }
.btn:active:not(:disabled) { scale: 0.96; }
.btn:focus-visible { outline: 2px solid var(--border-focus); outline-offset: 2px; }

.btn--primary { background: var(--accent-primary); color: var(--text-invert); }
.btn--primary:hover:not(:disabled) { background: var(--accent-primary-dim); }

.btn--ghost { background: var(--bg-card); color: var(--text-primary); border-color: var(--border-default); }
.btn--ghost:hover:not(:disabled) { background: var(--bg-hover); }

.btn--danger { background: var(--bg-card); color: var(--color-fail); border-color: var(--border-default); }
.btn--danger:hover:not(:disabled) { background: var(--color-fail-bg); border-color: var(--color-fail); }
```

- [ ] **Step 2: 호출부 치환** — `variant="brand"` 2곳을 기본(primary)으로

- `app/page.js:83` `<Button variant="brand" onClick={...}>첫 프로젝트 만들기</Button>` → `<Button onClick={...}>첫 프로젝트 만들기</Button>`
- `components/optimize/RecommendPanel.jsx:8` `<Button variant="brand" ...>` → `<Button ...>`
- 확인: `grep -rn "variant=\"brand\"" app components` → 0건

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → PASS

```bash
git add components/ui/button.css app/page.js components/optimize/RecommendPanel.jsx
git commit -m "feat: CTA 네이비 단일화 — brand 변형 삭제·포커스 링·프레스 피드백"
```

---

### Task 5: Field/Select — 포커스 링 복원·labelHidden·모바일 16px

**Files:**
- Modify: `components/ui/field.css`, `components/ui/Field.jsx`, `components/ui/Select.jsx`, `app/project/[id]/optimize/optimize.css`, `components/optimize/comboCard.css`, `components/report/report.css`

**Interfaces:**
- Produces: `Field`·`Select`에 `labelHidden` prop(불리언) — 라벨을 DOM에 유지하되 시각적으로 숨김. Task 6이 사용

- [ ] **Step 1: field.css 포커스 수정 + 모바일 16px**

`field.css:11`의 `.field__input:focus { border-color: var(--border-focus); outline: none; }` 를 다음으로 교체:

```css
.field__input:focus-visible { outline: 2px solid var(--border-focus); outline-offset: 1px; border-color: var(--border-focus); }
```

파일 끝에 추가:

```css
/* iOS 자동 확대 방지 — 모바일 입력 16px */
@media (max-width: 767px) {
  .field__input { font-size: 16px; }
}
```

textarea들도 동일 원칙 — 각 파일 끝에 추가:
- `app/project/[id]/optimize/optimize.css`: `@media (max-width: 767px) { .cm__memo textarea { font-size: 16px; } }`
- `components/optimize/comboCard.css`: `@media (max-width: 767px) { .cc__memo textarea { font-size: 16px; } }`
- `components/report/report.css`: `@media (max-width: 767px) { .pm__editor textarea { font-size: 16px; } }`

- [ ] **Step 2: labelHidden prop**

`Field.jsx` 라벨 줄 교체:

```jsx
export default function Field({ label, labelHidden = false, hint, error, mono = false, className = "", ...inputProps }) {
  const id = useId();
  return (
    <div className={`field ${className}`}>
      {label && <label className={`field__label ${labelHidden ? "visually-hidden" : ""}`} htmlFor={id}>{label}</label>}
```

`Select.jsx` 동일 패턴:

```jsx
export default function Select({ label, labelHidden = false, options = [], placeholder, error, className = "", ...selectProps }) {
  const id = useId();
  return (
    <div className={`field ${className}`}>
      {label && <label className={`field__label ${labelHidden ? "visually-hidden" : ""}`} htmlFor={id}>{label}</label>}
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → PASS

```bash
git add components/ui/field.css components/ui/Field.jsx components/ui/Select.jsx "app/project/[id]/optimize/optimize.css" components/optimize/comboCard.css components/report/report.css
git commit -m "fix: 입력 포커스 링 복원·labelHidden prop·모바일 입력 16px"
```

---

### Task 6: 반복 행 접근성 라벨 적용

**Files:**
- Modify: `app/project/[id]/info/page.js:93-96`, `components/calc/ScenarioEditor.jsx:54-60`

**Interfaces:**
- Consumes: Task 5의 `labelHidden` prop

- [ ] **Step 1: info 용도 행** — `label={i === 0 ? "…" : undefined}` 패턴 2곳을 교체

```jsx
<Select label="용도" labelHidden={i > 0} placeholder="용도 선택" options={toOptions(용도_OPTIONS)}
  value={row.용도} onChange={e => applyRow(i, { 용도: e.target.value })} />
<Field label="연면적 (㎡)" labelHidden={i > 0} type="number" mono value={row.연면적}
  onChange={e => applyRow(i, { 연면적: num(e.target.value) })} />
```

- [ ] **Step 2: ScenarioEditor 시스템 행** — 3곳 동일 패턴

```jsx
<Select label="에너지원" labelHidden={yi > 0} placeholder="선택" options={toOptions(lib.에너지원목록)} ... />
<Select label="형식" labelHidden={yi > 0} placeholder="선택" ... />
<Field label="적용용량 (kW)" labelHidden={yi > 0} type="number" mono ... />
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → PASS

```bash
git add "app/project/[id]/info/page.js" components/calc/ScenarioEditor.jsx
git commit -m "fix: 반복 행 입력에 시각 숨김 라벨 — 스크린리더 이름 부여"
```

---

### Task 7: Badge 신 톤 + 판정색 장식 남용 제거

**Files:**
- Modify: `components/ui/badge.css`, `app/project/[id]/info/page.js:75`, `components/optimize/ComboCard.jsx:29`, `components/optimize/comboCard.css`

**Interfaces:**
- Produces: Badge tone은 `pass`·`fail`·`na`·`brand`·`warm` (`action` 삭제). 판정색(pass/fail)은 판정 표시에만 사용

- [ ] **Step 1: badge.css 교체** (리터럴 색 제거 — 전부 토큰)

```css
.badge {
  display: inline-flex; align-items: center; gap: var(--sp-1);
  height: 22px; padding: 0 var(--sp-2); border-radius: 999px;
  font-size: var(--fs-12); font-weight: 700; white-space: nowrap;
}
.badge--pass { background: var(--color-pass-bg); color: var(--color-pass); }
.badge--fail { background: var(--color-fail-bg); color: var(--color-fail); }
.badge--na { background: var(--color-na-bg); color: var(--color-na); }
.badge--brand { background: #E6F7F5; color: var(--accent-action); }
.badge--warm { background: var(--accent-warm-bg); color: var(--accent-warm); }
```

주의: `#E6F7F5`(사내 surface-accent)는 토큰 신설이 맞다 — `tokens.css`에 `--accent-action-bg: #E6F7F5;` 추가하고 `background: var(--accent-action-bg)`로 쓴다. `tests/tokensContrast.test.mjs`의 `TEXT_PAIRS`에 `["accent-action", "accent-action-bg"]` 추가(실측 4.95:1 통과).

- [ ] **Step 2: tone="action" 치환 + 그린 장식 제거**

- `info/page.js:75` `<Badge tone="action">3경로 동기</Badge>` → `<Badge tone="brand">주소·지도·직접 선택 연동</Badge>` (문구 교체 겸)
- `ComboCard.jsx:29` 랭크1 `<Badge tone="pass">최적</Badge>` → `<Badge tone="brand">최적</Badge>` (판정 아님)
- `comboCard.css` 장식 그린 → 틸 계열 교체:
  - `.cc--best { border-color: var(--accent-graphic); box-shadow: 0 0 0 1px var(--accent-graphic); }`
  - `.cc__chip--champ { border-color: var(--accent-action); color: var(--accent-action); background: var(--accent-action-bg); }`
  - `.cc__barfill { background: var(--accent-graphic); }`
  - `.cc__reason { background: var(--bg-card-inner); }` (pass-bg 아님)
- 확인: `grep -rn "tone=\"action\"" app components` → 0건

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → PASS (대비 게이트에 신규 쌍 포함)

```bash
git add components/ui/badge.css styles/tokens.css tests/tokensContrast.test.mjs "app/project/[id]/info/page.js" components/optimize/ComboCard.jsx components/optimize/comboCard.css
git commit -m "feat: 배지 신 톤·판정색 장식 재사용 제거 — 한 색 한 의미"
```

---

### Task 8: Modal 접근성 — 포커스 트랩·복귀·inert

**Files:**
- Modify: `components/ui/Modal.jsx`, `components/ui/modal.css`

**Interfaces:**
- Produces: Modal API 불변(`open`·`onClose`·`title`·`footer`·`wide`). 5개 모달(생성·삭제·설정·제약·프롬프트) 전부 자동 적용

- [ ] **Step 1: Modal.jsx 교체**

```jsx
"use client";
import { useEffect, useId, useRef } from "react";
import { createPortal } from "react-dom";
import "./modal.css";

const FOCUSABLE = 'a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])';

export default function Modal({ open, onClose, title, footer, wide = false, children }) {
  const titleId = useId();
  const backdropRef = useRef(null);
  const dialogRef = useRef(null);

  useEffect(() => {
    if (!open) return;
    const trigger = document.activeElement;                 // 닫힐 때 복귀할 트리거
    const dialog = dialogRef.current;
    const first = dialog.querySelector("[autofocus]") ?? dialog.querySelector(FOCUSABLE) ?? dialog;
    first.focus();

    // 배경 비활성화: body 직계 중 모달 백드롭 제외 전부 inert
    const siblings = [...document.body.children].filter(el => el !== backdropRef.current);
    siblings.forEach(el => el.setAttribute("inert", ""));

    const onKey = e => {
      if (e.key === "Escape") { onClose(); return; }
      if (e.key !== "Tab") return;
      const items = [...dialog.querySelectorAll(FOCUSABLE)];
      if (items.length === 0) { e.preventDefault(); return; }
      const firstEl = items[0], lastEl = items[items.length - 1];
      if (e.shiftKey && document.activeElement === firstEl) { e.preventDefault(); lastEl.focus(); }
      else if (!e.shiftKey && (document.activeElement === lastEl || !dialog.contains(document.activeElement))) {
        e.preventDefault(); firstEl.focus();
      }
    };
    document.addEventListener("keydown", onKey);
    document.body.style.overflow = "hidden";
    return () => {
      document.removeEventListener("keydown", onKey);
      document.body.style.overflow = "";
      siblings.forEach(el => el.removeAttribute("inert"));
      trigger?.focus?.();
    };
  }, [open, onClose]);

  if (!open) return null;
  return createPortal(
    <div className="modal__backdrop" ref={backdropRef} onClick={onClose}>
      <div className={wide ? "modal modal--wide" : "modal"} ref={dialogRef} role="dialog" aria-modal="true"
        aria-labelledby={titleId} tabIndex={-1} onClick={e => e.stopPropagation()}>
        <header className="modal__head">
          <h2 className="modal__title" id={titleId}>{title}</h2>
          <button className="modal__close" onClick={onClose} aria-label="닫기">×</button>
        </header>
        <div className="modal__body">{children}</div>
        {footer && <footer className="modal__foot">{footer}</footer>}
      </div>
    </div>,
    document.body
  );
}
```

- [ ] **Step 2: modal.css 보강** — `.modal__body`에 `overscroll-behavior: contain;` 추가, `.modal__close`에 `:focus-visible { outline: 2px solid var(--border-focus); }` 추가

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → PASS (동작 검증은 Task 14 E2E의 Tab 순환 테스트)

```bash
git add components/ui/Modal.jsx components/ui/modal.css
git commit -m "fix: 모달 포커스 트랩·트리거 복귀·배경 inert — 5개 모달 공통 해소"
```

---

### Task 9: Toast — 오류 수동 닫기

**Files:**
- Modify: `components/ui/ToastProvider.jsx`, `components/ui/toast.css`

**Interfaces:**
- Produces: `push({message, tone})` API 불변. `tone:"fail"`만 자동 소멸 없음

- [ ] **Step 1: ToastProvider.jsx 교체**

```jsx
"use client";
import { createContext, useCallback, useContext, useRef, useState } from "react";
import "./toast.css";

const ToastContext = createContext(null);

export function useToast() {
  const ctx = useContext(ToastContext);
  if (!ctx) throw new Error("useToast는 ToastProvider 안에서만 사용할 수 있습니다");
  return ctx;
}

export default function ToastProvider({ children }) {
  const [toasts, setToasts] = useState([]);
  const seq = useRef(0);

  const dismiss = useCallback(id => setToasts(prev => prev.filter(t => t.id !== id)), []);

  const push = useCallback(({ message, tone = "info" }) => {
    const id = ++seq.current;
    setToasts(prev => [...prev, { id, message, tone }]);
    if (tone !== "fail") setTimeout(() => dismiss(id), 3000);  // 오류는 수동 닫기
  }, [dismiss]);

  return (
    <ToastContext.Provider value={{ push }}>
      {children}
      <div className="toast__stack" aria-live="polite">
        {toasts.map(t => (
          <div key={t.id} className={`toast toast--${t.tone}`}>
            {t.message}
            {t.tone === "fail" && (
              <button className="toast__close" onClick={() => dismiss(t.id)} aria-label="닫기">×</button>
            )}
          </div>
        ))}
      </div>
    </ToastContext.Provider>
  );
}
```

- [ ] **Step 2: toast.css 추가**

```css
.toast { display: flex; align-items: center; gap: var(--sp-3); }
.toast__close { background: none; border: none; color: inherit; font-size: var(--fs-16); cursor: pointer; line-height: 1; padding: 0; }
.toast__close:focus-visible { outline: 2px solid var(--text-invert); outline-offset: 2px; }
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → PASS

```bash
git add components/ui/ToastProvider.jsx components/ui/toast.css
git commit -m "fix: 오류 토스트 수동 닫기 — 자동 소멸 제거"
```

---

### Task 10: 스테퍼 3상태 — stepStatuses TDD + Stepper 재구성

**Files:**
- Create: `lib/stepStatus.js`, `tests/stepStatus.test.mjs`
- Modify: `lib/workspaceSteps.js`, `components/ui/Stepper.jsx`, `components/ui/stepper.css`, `app/project/[id]/WorkspaceShell.jsx`

**Interfaces:**
- Consumes: `canCalculate(input1)` (`lib/calcModel.js:37`, `{ok, missing}` 반환)
- Produces: `stepStatuses(project, activeSegment)` → `{info, calc, optimize, report}` 각 값 `"done" | "active" | "todo"`. `STEPS`에서 `dotVar` 제거

- [ ] **Step 1: 실패 테스트 작성**

```js
// tests/stepStatus.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import { stepStatuses } from "../lib/stepStatus.js";

const 완성input1 = {
  사업형태: "공공기관", 사업연도: "2026", 대지위치: "서울특별시", 연면적: 50000,
  용도별연면적목록: [{ 용도: "업무시설", 연면적: 50000 }],
};

test("빈 프로젝트: 활성 단계만 active, 나머지 todo", () => {
  const s = stepStatuses({ data: {} }, "info");
  assert.deepEqual(s, { info: "active", calc: "todo", optimize: "todo", report: "todo" });
});

test("사업정보 완성 시 info=done (다른 단계 활성 기준)", () => {
  const s = stepStatuses({ data: { input1: 완성input1 } }, "calc");
  assert.equal(s.info, "done");
  assert.equal(s.calc, "active");
});

test("시나리오에 유효 시스템이 있으면 calc=done", () => {
  const data = {
    input1: 완성input1,
    input2: { scenarios: [{ id: "ALT-1", systems: [{ 에너지원: "태양광", 형식: "고정식(수평)", 적용용량: 500 }] }] },
  };
  assert.equal(stepStatuses({ data }, "info").calc, "done");
});

test("형식 미선택 시스템뿐이면 calc=todo", () => {
  const data = { input2: { scenarios: [{ id: "ALT-1", systems: [{ 에너지원: "태양광", 형식: "", 적용용량: 500 }] }] } };
  assert.equal(stepStatuses({ data }, "info").calc, "todo");
});

test("input3 저장·검토의견 존재 시 optimize·report=done", () => {
  const data = { input3: { 건물유형: "" }, review: { text: "검토의견", at: 1 } };
  const s = stepStatuses({ data }, "info");
  assert.equal(s.optimize, "done");
  assert.equal(s.report, "done");
});

test("활성 단계는 완료여도 active 우선", () => {
  const s = stepStatuses({ data: { input1: 완성input1 } }, "info");
  assert.equal(s.info, "active");
});

test("project가 null이어도 안전", () => {
  assert.deepEqual(stepStatuses(null, "info"), { info: "active", calc: "todo", optimize: "todo", report: "todo" });
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/stepStatus.test.mjs`
Expected: FAIL — `lib/stepStatus.js` 없음

- [ ] **Step 3: 구현**

```js
// lib/stepStatus.js — 워크스페이스 단계 완료 판정 (표시 전용 파생값, 스키마 무변경)
import { canCalculate } from "./calcModel.js";

export function stepStatuses(project, activeSegment) {
  const d = project?.data ?? {};
  const done = {
    info: canCalculate(d.input1 ?? {}).ok,
    calc: !!d.input2?.scenarios?.some(sc =>
      (sc.systems ?? []).some(s => s.에너지원 && s.형식 && Number(s.적용용량) > 0)),
    optimize: d.input3 != null,
    report: !!d.review?.text,
  };
  const out = {};
  for (const seg of ["info", "calc", "optimize", "report"])
    out[seg] = seg === activeSegment ? "active" : (done[seg] ? "done" : "todo");
  return out;
}
```

주의: `canCalculate`의 필수 필드가 픽스처와 안 맞아 1번 테스트 계열이 실패하면 **픽스처를** `lib/calcModel.js`의 실제 검사 항목에 맞춰 수정한다 (구현을 바꾸지 말 것).

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/stepStatus.test.mjs` → PASS (7 tests)

- [ ] **Step 5: Stepper 재구성**

`lib/workspaceSteps.js` — `dotVar` 속성 제거:

```js
export const STEPS = [
  { segment: "info", label: "사업정보", desc: "사업형태·위치·연면적" },
  { segment: "calc", label: "검토 계산", desc: "에너지사용량·설치비율" },
  { segment: "optimize", label: "최적화·AI", desc: "설비조합·AI 추천" },
  { segment: "report", label: "보고서", desc: "미리보기·다운로드" },
];
```

`components/ui/Stepper.jsx` 교체:

```jsx
"use client";
import Link from "next/link";
import "./stepper.css";

export default function Stepper({ items = [], statuses = {} }) {
  return (
    <ol className="stepper">
      {items.map((it, i) => {
        const st = statuses[it.segment] ?? "todo";
        return (
          <li key={it.segment}>
            <Link href={it.href} className={`stepper__item stepper__item--${st}`}
              aria-current={st === "active" ? "step" : undefined}>
              <span className={`stepper__state stepper__state--${st}`} aria-hidden="true">
                {st === "done" ? "✓" : ""}
              </span>
              <span className="stepper__text">
                <span className="stepper__label">{i + 1}. {it.label}</span>
                <span className="stepper__desc">{it.desc}</span>
                {st === "done" && <span className="visually-hidden">완료됨</span>}
              </span>
            </Link>
          </li>
        );
      })}
    </ol>
  );
}
```

`components/ui/stepper.css` 교체 (다크 나브 위 3상태):

```css
.stepper { list-style: none; display: flex; flex-direction: column; gap: var(--sp-1); }
.stepper__item {
  display: flex; align-items: flex-start; gap: var(--sp-3);
  padding: var(--sp-3); border-radius: var(--radius-sm); color: var(--nav-text-dim);
  transition: background var(--dur-fast) var(--ease-out);
}
.stepper__item:hover { background: rgba(255, 255, 255, .06); text-decoration: none; }
.stepper__item:focus-visible { outline: 2px solid var(--nav-active); outline-offset: 2px; }
.stepper__item--active {
  background: rgba(255, 255, 255, .10); color: var(--nav-text);
  box-shadow: inset 3px 0 0 var(--nav-active);   /* 좌측 활성 바 */
}
.stepper__state {
  width: 18px; height: 18px; border-radius: 50%; flex: none; margin-top: 2px;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: 11px; font-weight: 700;
}
.stepper__state--done { border: 1.5px solid var(--nav-active); color: var(--nav-active); }
.stepper__state--active { background: var(--nav-text); }
.stepper__state--todo { border: 1.5px solid var(--nav-text-dim); }
.stepper__text { display: flex; flex-direction: column; }
.stepper__label { font-size: var(--fs-14); font-weight: 700; }
.stepper__desc { font-size: var(--fs-12); color: inherit; opacity: .75; }
```

`WorkspaceShell.jsx` — 상태 전달 + 경로 변경 시 재조회 (단계 이동 후 완료 상태 갱신):

```jsx
import { stepStatuses } from "@/lib/stepStatus";
// useEffect 의존성에 pathname 추가:
useEffect(() => {
  setState({ loading: false, project: getProject(projectId) });
}, [projectId, pathname]);
// Stepper 호출부:
<Stepper statuses={stepStatuses(state.project, activeSegment)}
  items={STEPS.map(s => ({ ...s, href: `/project/${projectId}/${s.segment}` }))} />
```

- [ ] **Step 6: 전체 테스트 + 커밋**

Run: `npm test` → 전부 PASS

```bash
git add lib/stepStatus.js tests/stepStatus.test.mjs lib/workspaceSteps.js components/ui/Stepper.jsx components/ui/stepper.css "app/project/[id]/WorkspaceShell.jsx"
git commit -m "feat: 스테퍼 3상태(완료·현재·미완) — 색 도트 폐기·완료 판정 TDD"
```

---

### Task 11: 다크 나브 패널 + 콘텐츠 중앙 정렬

**Files:**
- Modify: `app/project/[id]/workspace.css`

**Interfaces:**
- Consumes: Task 2의 `--nav-*` 토큰, Task 10의 스테퍼 다크 스타일

- [ ] **Step 1: workspace.css 교체**

```css
.ws { display: flex; min-height: 100vh; }
.ws__nav {
  width: var(--nav-width); flex: none;
  padding: var(--sp-6) var(--sp-4);
  background: var(--nav-bg); color: var(--nav-text);
  display: flex; flex-direction: column; gap: var(--sp-4);
  position: sticky; top: 0; height: 100vh;
}
.ws__back { font-size: var(--fs-13); color: var(--nav-text-dim); }
.ws__back:hover { color: var(--nav-text); }
.ws__project { font-size: var(--fs-16); font-weight: 800; word-break: keep-all; color: var(--nav-text); }
.ws__content { flex: 1; min-width: 0; padding: var(--sp-8) var(--sp-6); max-width: var(--content-max); margin-inline: auto; }
.ws--missing { display: block; max-width: 560px; margin: 0 auto; padding: var(--sp-10); }

/* 1024px 미만: 나브를 다크 상단 바로 전환 (태블릿·모바일) */
@media (max-width: 1023px) {
  .ws { flex-direction: column; }
  .ws__nav { width: 100%; height: auto; position: static; }
  /* 잘림 단서: 트레일링 여백 + 우측 페이드 — 마스크는 알파 기반이라 다크 배경에도 동일 동작 */
  .ws__nav .stepper {
    flex-direction: row; overflow-x: auto;
    padding-inline-end: var(--sp-10);
    mask-image: linear-gradient(to right, black calc(100% - 24px), transparent);
  }
  .ws__nav .stepper__desc { display: none; }
  .ws__nav .stepper__item { white-space: nowrap; }
}
```

(기존 대비 변경점: `border-right` 제거→다크 배경, back/project 색을 nav 토큰으로, `.ws__content`에 `margin-inline: auto` 추가, 모바일 `border-bottom` 제거)

- [ ] **Step 2: 테스트 + 커밋**

Run: `npm test` → PASS

```bash
git add "app/project/[id]/workspace.css"
git commit -m "feat: 다크 네이비 나브 패널(시그니처)·와이드 화면 콘텐츠 중앙 정렬"
```

---

### Task 12: 판정 밴드 + dl 유효 마크업 (Output2Panel)

**Files:**
- Modify: `components/calc/Output2Panel.jsx`, `components/calc/results.css`

**Interfaces:**
- Consumes: `alt = { id, 비율, 의무비율, 만족여부, 생산량합계, 총에너지사용량, systems }` (엔진 출력 — 불변), `fmtNum`(`lib/calcModel.js`), `Badge`, `Table`

- [ ] **Step 1: Output2Panel.jsx 교체**

```jsx
"use client";
import Table from "@/components/ui/Table";
import Badge from "@/components/ui/Badge";
import { fmtNum } from "@/lib/calcModel";
import "./results.css";

const SYS_COLS = [
  { key: "에너지원", header: "에너지원" },
  { key: "형식", header: "형식" },
  { key: "적용용량", header: "적용용량 (kW)", align: "right", mono: true, render: r => fmtNum(r.적용용량) },
  { key: "신재생에너지생산량", header: "생산량 (kWh/yr)", align: "right", mono: true, render: r => fmtNum(Math.round(r.신재생에너지생산량)) },
];

const 판정 = {
  Yes: { tone: "pass", label: "만족", sym: "✓" },
  No: { tone: "fail", label: "불만족", sym: "✕" },
  해당없음: { tone: "na", label: "해당없음", sym: "–" },
};

export default function Output2Panel({ alt }) {
  const j = 판정[alt.만족여부] ?? 판정.해당없음;
  return (
    <section className="res res__alt">
      <div className={`verdict verdict--${j.tone}`}>
        <span className="verdict__id mono">{alt.id}</span>
        <strong className="verdict__ratio mono">{alt.비율.toFixed(1)}%</strong>
        <span className="verdict__req">의무 {alt.의무비율 !== null ? `${alt.의무비율}%` : "-"}</span>
        <Badge tone={j.tone}>{j.sym} {j.label}</Badge>
      </div>
      <dl className="res__stats">
        <div><dt>신재생에너지 생산량</dt><dd className="mono">{fmtNum(Math.round(alt.생산량합계))} kWh/yr</dd></div>
        <div><dt>총에너지사용량</dt><dd className="mono">{fmtNum(alt.총에너지사용량)} kWh/yr</dd></div>
      </dl>
      <Table columns={SYS_COLS} rows={alt.systems} rowKey={(r) => r.에너지원 + r.형식 + r.적용용량}
        empty="에너지원·형식·적용용량을 입력하면 생산량이 계산됩니다" />
    </section>
  );
}
```

- [ ] **Step 2: results.css 교체** (`.res__kpi*` 삭제, 밴드 신설)

```css
.res { display: flex; flex-direction: column; gap: var(--sp-3); }
.res__total { font-size: var(--fs-13); color: var(--text-secondary); text-align: right; }
.res__alt { margin-top: var(--sp-2); }

/* 판정 밴드 — 이 앱의 최종 산출을 한 줄에 */
.verdict {
  display: flex; align-items: baseline; gap: var(--sp-3); flex-wrap: wrap;
  padding: var(--sp-4) var(--sp-5); border-radius: var(--radius-md);
  border-inline-start: 4px solid;
}
.verdict--pass { background: var(--color-pass-bg); border-color: var(--color-pass); }
.verdict--fail { background: var(--color-fail-bg); border-color: var(--color-fail); }
.verdict--na { background: var(--color-na-bg); border-color: var(--color-na); }
.verdict__id { font-size: var(--fs-13); color: var(--text-secondary); }
.verdict__ratio { font-size: var(--fs-28); font-weight: 800; line-height: 1.1; }
.verdict__req { font-size: var(--fs-13); color: var(--text-secondary); }

.res__stats { display: flex; gap: var(--sp-6); flex-wrap: wrap; }
.res__stats dt { font-size: var(--fs-12); color: var(--text-hint); }
.res__stats dd { font-size: var(--fs-14); }
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → PASS

```bash
git add components/calc/Output2Panel.jsx components/calc/results.css
git commit -m "feat: 판정 밴드 — 설치비율·의무비율·판정 한 줄, dl 유효 마크업"
```

---

### Task 13: 잔여 구조·문구 보수 (calc 카드·대시보드 Link·파일 입력)

**Files:**
- Modify: `app/project/[id]/calc/page.js`, `app/page.js`, `app/dashboard.css`, `components/report/ReportActions.jsx`, `components/report/report.css`

**Interfaces:**
- Consumes: 기존 `check`·`카테고리`·`notices` 로직 (계산 로직 무변경 — JSX 배치만 변경)

- [ ] **Step 1: calc 상태 카드 통합** — `calc/page.js`의 return 앞부분(② 카드 + Output1 카드)을 다음으로 교체. 정상 상태에선 상태 카드를 렌더하지 않는다:

```jsx
const notices = [
  !check.ok && `사업정보가 부족합니다: ${check.missing.join(", ")} — ① 사업정보에서 입력을 완료하세요.`,
  check.ok && !카테고리 && "공동주택 세대 수를 선택하면 규모등급이 판정됩니다 (① 사업정보).",
  engineError && `엔진 로드 실패: ${engineError}`,
  calcError && `계산 오류: ${calcError}`,
].filter(Boolean);
const loading = !ready && !engineError;

return (
  <div className="calc">
    {(notices.length > 0 || loading) && (
      <Card title="② 검토 계산">
        {notices.map(n => <p key={n} className="calc__notice" role="status">{n}</p>)}
        {loading && <p className="calc__hint">계산 엔진 로딩 중…</p>}
      </Card>
    )}

    <Card title="② 예상에너지사용량 (Output 1)" actions={
      <span className="calc__cat" title="주거: 세대수 기준 / 비주거: 용도별 연면적 합계 기준으로 자동 결정됩니다">
        규모등급 <Badge tone={카테고리 ? "brand" : "na"}>{카테고리 || "미판정"}</Badge>
      </span>
    }>
      {result
        ? <Output1Table output1={result.output1} />
        : <p className="calc__hint">① 사업정보 입력이 완료되면 용도별 예상에너지사용량이 여기 표시됩니다.</p>}
    </Card>
    {/* 시나리오 (ALT)·Output 2 카드는 기존 그대로 */}
```

(기존 첫 카드의 "규모등급(카테고리)" 문구는 이 이동으로 "규모등급"이 된다 — A안 #11)

- [ ] **Step 2: 대시보드 카드 열기를 Link로** — `app/page.js`

상단에 `import Link from "next/link";` 추가, 카드 내부 교체:

```jsx
<Link href={`/project/${p.id}/info`} className="dash__open">
  <span className="dash__name">{p.name}</span>
  <span className="dash__date mono">수정 {formatDate(p.updatedAt)}</span>
</Link>
```

`app/dashboard.css`의 `.dash__open` 교체 (버튼 리셋 → 링크 스타일):

```css
.dash__open {
  display: flex; flex-direction: column; gap: var(--sp-1);
  color: inherit; text-decoration: none;
}
.dash__open:hover { text-decoration: none; }
.dash__open:focus-visible { outline: 2px solid var(--border-focus); outline-offset: 2px; border-radius: var(--radius-sm); }
```

- [ ] **Step 3: 파일 입력 스타일 트리거** — `ReportActions.jsx`의 `<input type="file" …>` 줄을 교체:

```jsx
<label className="btn btn--ghost btn--sm ra__filebtn">
  표지 선택
  <input type="file" accept="image/*" onChange={pickCover} className="visually-hidden" />
</label>
```

`report.css`에 추가:

```css
.ra__filebtn { position: relative; }
.ra__filebtn:focus-within { outline: 2px solid var(--border-focus); outline-offset: 2px; }
```

- [ ] **Step 4: 개발 용어 잔여 확인**

Run: `grep -rn "3경로\|카테고리)" app components` → UI 노출 문구 0건 (코드 내부 변수명 `카테고리`는 무관)

- [ ] **Step 5: 테스트 + 커밋**

Run: `npm test` → PASS

```bash
git add "app/project/[id]/calc/page.js" app/page.js app/dashboard.css components/report/ReportActions.jsx components/report/report.css
git commit -m "fix: 빈 상태 카드 제거·대시보드 Link 내비게이션·파일 입력 버튼화·문구 정리"
```

---

### Task 14: E2E 게이트 — 프리뷰 검증

**Files:**
- Create: `verify/e2e_redesign.mjs`
- 배포: `git push -u origin redesign/teal` → Vercel Preview

**Interfaces:**
- Consumes: Vercel Preview URL (푸시 후 Vercel 대시보드 또는 `renewable-proposal-git-redesign-teal-*.vercel.app` 형식으로 확보)
- Produces: 게이트 판정 — ① 오버플로 0 (5폭 × 5화면) ② 모달 Tab 순환 이탈 0 + Escape 후 트리거 복귀 ③ 스크린샷 육안

- [ ] **Step 1: E2E 스크립트 작성**

```js
// verify/e2e_redesign.mjs — UI 개편 게이트: 오버플로·모달 포커스 트랩·스크린샷
// 실행: node verify/e2e_redesign.mjs <PREVIEW_URL> <OUT_DIR>
import { chromium } from "playwright";

const BASE = process.argv[2];
const OUT = process.argv[3] ?? "verify/e2e_out";
if (!BASE) { console.error("사용법: node verify/e2e_redesign.mjs <PREVIEW_URL> [OUT_DIR]"); process.exit(1); }

const seed = [{
  id: "e2e-seed", name: "E2E검증-개편", createdAt: 1, updatedAt: 1,
  data: {
    input1: {
      사업형태: "공공기관", 사업연도: "2026", 대지위치: "서울특별시", 연면적: 50000,
      용도별연면적목록: [{ 용도: "업무시설", 연면적: 50000 }],
      대지면적: 10000, 건축면적: 5000, 건폐율: 50, 용적률: 500, 위치정보: null,
    },
    input2: { scenarios: [{ id: "ALT-1", systems: [{ 에너지원: "태양광", 형식: "고정식(수평)", 적용용량: 500, 단위에너지생산량: 1358, 원별보정계수: 1.56 }] }] },
  },
}];

const { mkdirSync } = await import("node:fs");
mkdirSync(OUT, { recursive: true });
const browser = await chromium.launch({ channel: "msedge" });
let fails = 0;
const bad = m => { fails++; console.error("FAIL:", m); };

for (const vw of [320, 375, 768, 1440, 1920]) {
  const ctx = await browser.newContext({ viewport: { width: vw, height: 900 } });
  const page = await ctx.newPage();
  await page.goto(BASE, { waitUntil: "networkidle" });
  await page.evaluate(s => localStorage.setItem("rp.projects.v1", JSON.stringify(s)), seed);
  for (const [name, path] of [["dash", "/"], ["info", "/project/e2e-seed/info"], ["calc", "/project/e2e-seed/calc"], ["optimize", "/project/e2e-seed/optimize"], ["report", "/project/e2e-seed/report"]]) {
    await page.goto(BASE + path, { waitUntil: "networkidle" });
    await page.waitForTimeout(600);
    const ov = await page.evaluate(() => document.documentElement.scrollWidth - document.documentElement.clientWidth);
    if (ov > 0) bad(`${name}@${vw}px 가로 오버플로 ${ov}px`);
    if (vw === 375 || vw === 1440) await page.screenshot({ path: `${OUT}/${name}-${vw}.png`, fullPage: true });
  }
  await ctx.close();
}

// 모달 포커스 트랩 (1440px)
{
  const ctx = await browser.newContext({ viewport: { width: 1440, height: 900 } });
  const page = await ctx.newPage();
  await page.goto(BASE, { waitUntil: "networkidle" });
  const trigger = page.getByRole("button", { name: "새 프로젝트" });
  await trigger.click();
  await page.waitForTimeout(400);
  for (let i = 0; i < 15; i++) {
    await page.keyboard.press("Tab");
    const inside = await page.evaluate(() => document.querySelector(".modal")?.contains(document.activeElement));
    if (!inside) { bad(`모달 Tab ${i + 1}회째 포커스 이탈`); break; }
  }
  await page.keyboard.press("Escape");
  await page.waitForTimeout(300);
  const restored = await page.evaluate(() => document.activeElement?.textContent?.includes("새 프로젝트"));
  if (!restored) bad("모달 닫힘 후 트리거 포커스 복귀 실패");
  await ctx.close();
}

await browser.close();
console.log(fails === 0 ? "PASS: 오버플로 0 · 모달 트랩 통과" : `FAIL ${fails}건`);
process.exit(fails === 0 ? 0 : 1);
```

- [ ] **Step 2: 커밋 + 푸시**

```bash
git add verify/e2e_redesign.mjs
git commit -m "test: 개편 E2E 게이트 스크립트 — 오버플로·모달 포커스 트랩"
git push -u origin redesign/teal
```

- [ ] **Step 3: 프리뷰 URL 확보 후 실행**

Vercel이 redesign/teal 프리뷰를 빌드할 때까지 대기(약 1분) 후:

Run: `node verify/e2e_redesign.mjs https://renewable-proposal-git-redesign-teal-<계정>.vercel.app`
Expected: `PASS: 오버플로 0 · 모달 트랩 통과` (exit 0)

- [ ] **Step 4: 스크린샷 육안 검토**

`verify/e2e_out/*.png` 10장을 열어 확인: 다크 나브·스테퍼 3상태·판정 밴드·네이비 CTA·빈 카드 부재. 어긋난 화면은 해당 태스크로 돌아가 수정 후 재실행

- [ ] **Step 5: Lighthouse 접근성**

Run: `npx lighthouse <프리뷰URL>/project/e2e-seed/calc --only-categories=accessibility --chrome-flags="--headless" --quiet --output=json --output-path=stdout | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>console.log('a11y:',JSON.parse(d).categories.accessibility.score*100))"`
Expected: `a11y: 95` 이상

- [ ] **Step 6: 최종 보고**

게이트 4종(테스트 46+/대비/E2E/Lighthouse) 결과를 정리해 사용자에게 보고. **main 머지는 사용자 승인 후 별도 진행** (`git checkout main && git merge --no-ff redesign/teal`)

---

## Self-Review 기록

- 스펙 §2(토큰)→Task 1·2, §3(타이포)→Task 3·5, §4(컴포넌트)→Task 4~10, §5(구조)→Task 11~13, §6(게이트)→Task 1·14. 커버리지 갭 없음
- 배지 틴트 실측 게이트(스펙의 보류 항목)는 Task 1 테스트로 상시화 — pass-bg를 #E4F5E9(그린 틴트)로 조정해 brand 민트(#E6F7F5)와 의미 분리
- 타입 일관성: `stepStatuses(project, activeSegment)` (Task 10 정의 = WorkspaceShell 사용), `labelHidden` (Task 5 정의 = Task 6 사용), `--accent-action-bg` (Task 7 신설 = badge/chip 사용) 확인
