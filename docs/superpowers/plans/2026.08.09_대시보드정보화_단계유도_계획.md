# 대시보드 정보화 + 단계 유도 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 대시보드 카드에 진행 단계·판정 요약을 표시하고, 워크스페이스 하단에 "다음 단계" 유도 바를 추가한다.

**Architecture:** 순수 요약 모듈(`lib/projectSummary.js`, TDD) + 대시보드에서 엔진 실시간 계산(파생값 미저장 계약 유지) + `WorkspaceShell` 공통 CTA 바. 엔진·스키마 무변경.

**Tech Stack:** Next.js(App Router, JS), Vanilla CSS(기존 토큰만), `node --test`, Playwright(msedge).

**스펙:** `docs/superpowers/specs/2026.08.09_대시보드정보화_단계유도_설계.md` (승인됨)

## Global Constraints

- `legacy/`·`engine/`·`public/` 무수정, 프로젝트 데이터 스키마 무변경(파생값 저장 금지)
- 신규 색·크기 리터럴 금지 — 기존 토큰만 사용 (`--accent-graphic`은 비텍스트 전용)
- 테스트: `npm test` = `node --test "tests/*.test.mjs"`. 현재 53/53 통과 상태에서 시작
- 로컬 `npm run build`/`dev` 금지(Dropbox 느림) — 시각 검증은 Vercel Preview
- 브랜치 `feature/dashboard-flow`. 커밋 `<type>: <한국어 설명>`. main 머지는 계획 밖(사용자 승인 후)
- 기존 인터페이스(변경 금지): `stepStatuses(project, activeSegment)` → `{info,calc,optimize,report}` 각 `"done"|"active"|"todo"` (`lib/stepStatus.js`) · `canCalculate`/`calc규모등급`/`buildEngineInput2`/`fmtNum` (`lib/calcModel.js`) · `useEngineReady()` (`lib/useEngineReady.js`) · `STEPS` (`lib/workspaceSteps.js`, `{segment,label,desc}`) · Badge tone pass/fail/na
- E2E 프리뷰 보호 우회: `.env.local`의 `VERCEL_AUTOMATION_BYPASS_SECRET` (스크립트가 자동 사용)

---

### Task 1: projectSummary 모듈 (TDD)

**Files:**
- Create: `lib/projectSummary.js`
- Test: `tests/projectSummary.test.mjs`

**Interfaces:**
- Produces: `pickRepresentative(output2)` → `{id, 비율, 의무비율, 만족여부} | null` · `nextSegment(statuses)` → segment 문자열 · `VERDICT_UI` 맵. Task 2·3이 사용

- [ ] **Step 1: 실패 테스트 작성**

```js
// tests/projectSummary.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import { pickRepresentative, nextSegment, VERDICT_UI } from "../lib/projectSummary.js";

test("pickRepresentative: 빈 배열이면 null", () => {
  assert.equal(pickRepresentative([]), null);
  assert.equal(pickRepresentative(undefined), null);
});

test("pickRepresentative: 단일 ALT 그대로", () => {
  const alt = { id: "ALT-1", 비율: 3.2, 의무비율: 14, 만족여부: "No" };
  assert.deepEqual(pickRepresentative([alt]), alt);
});

test("pickRepresentative: 비율 최대 ALT 선택", () => {
  const a = { id: "ALT-1", 비율: 3.2, 의무비율: 14, 만족여부: "No" };
  const b = { id: "ALT-2", 비율: 15.1, 의무비율: 14, 만족여부: "Yes" };
  assert.deepEqual(pickRepresentative([a, b]), b);
});

test("pickRepresentative: 동률이면 앞 순서", () => {
  const a = { id: "ALT-1", 비율: 5, 의무비율: 14, 만족여부: "No" };
  const b = { id: "ALT-2", 비율: 5, 의무비율: 14, 만족여부: "No" };
  assert.equal(pickRepresentative([a, b]).id, "ALT-1");
});

test("nextSegment: 전부 미완이면 info", () => {
  assert.equal(nextSegment({ info: "todo", calc: "todo", optimize: "todo", report: "todo" }), "info");
});

test("nextSegment: 첫 미완 단계 반환 (active도 미완 취급)", () => {
  assert.equal(nextSegment({ info: "done", calc: "active", optimize: "todo", report: "todo" }), "calc");
  assert.equal(nextSegment({ info: "done", calc: "done", optimize: "todo", report: "todo" }), "optimize");
});

test("nextSegment: 전부 done이면 report", () => {
  assert.equal(nextSegment({ info: "done", calc: "done", optimize: "done", report: "done" }), "report");
});

test("VERDICT_UI: 세 판정 모두 tone·label·sym 보유", () => {
  for (const k of ["Yes", "No", "해당없음"]) {
    assert.ok(VERDICT_UI[k].tone && VERDICT_UI[k].label && VERDICT_UI[k].sym, k);
  }
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/projectSummary.test.mjs`
Expected: FAIL — 모듈 없음

- [ ] **Step 3: 구현**

```js
// lib/projectSummary.js — 대시보드 요약용 순수 함수 (파생값 저장 없음)
const ORDER = ["info", "calc", "optimize", "report"];

// output2(엔진 출력 배열)에서 대표 ALT = 설치비율 최대 (동률이면 앞 순서)
export function pickRepresentative(output2) {
  if (!Array.isArray(output2) || output2.length === 0) return null;
  return output2.reduce((best, alt) => (alt.비율 > best.비율 ? alt : best));
}

// 첫 미완료(done 아님) 단계 — 전부 완료면 report
export function nextSegment(statuses) {
  return ORDER.find(seg => statuses[seg] !== "done") ?? "report";
}

// 판정 표시 문법 — Output2Panel과 동일
export const VERDICT_UI = {
  Yes: { tone: "pass", label: "만족", sym: "✓" },
  No: { tone: "fail", label: "불만족", sym: "✕" },
  해당없음: { tone: "na", label: "해당없음", sym: "–" },
};
```

- [ ] **Step 4: 통과 확인 + 전체 테스트**

Run: `node --test tests/projectSummary.test.mjs` → PASS (8 tests)
Run: `npm test` → 61/61 PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/projectSummary.js tests/projectSummary.test.mjs
git commit -m "feat: 프로젝트 요약 순수 모듈 — 대표 ALT·다음 단계 선택 TDD"
```

---

### Task 2: 대시보드 카드 정보화

**Files:**
- Modify: `app/page.js`, `app/dashboard.css`

**Interfaces:**
- Consumes: Task 1의 `pickRepresentative`/`nextSegment`/`VERDICT_UI`, `stepStatuses`(lib/stepStatus.js), `canCalculate`/`calc규모등급`/`buildEngineInput2`(lib/calcModel.js), `useEngineReady`(lib/useEngineReady.js), `STEPS`(lib/workspaceSteps.js), `Badge`

- [ ] **Step 1: app/page.js 수정**

임포트 추가 (기존 임포트 유지):

```js
import { useEngineReady } from "@/lib/useEngineReady";
import { stepStatuses } from "@/lib/stepStatus";
import { canCalculate, calc규모등급, buildEngineInput2 } from "@/lib/calcModel";
import { pickRepresentative, nextSegment, VERDICT_UI } from "@/lib/projectSummary";
import { STEPS } from "@/lib/workspaceSteps";
import Badge from "@/components/ui/Badge";
```

컴포넌트 상단에 판정 계산 훅 추가 (기존 state 아래):

```js
const { ready } = useEngineReady();
const [verdicts, setVerdicts] = useState({});   // { [projectId]: 대표 ALT }

useEffect(() => {
  if (!ready || !projects?.length) return;
  let alive = true;
  (async () => {
    const { runCalculation } = await import("@/engine/index.js");
    const out = {};
    for (const p of projects) {
      const input1 = p.data?.input1, input2 = p.data?.input2;
      if (!input1 || !input2?.scenarios?.length || !canCalculate(input1).ok) continue;
      const 카테고리 = calc규모등급(input1.용도별연면적목록 ?? []);
      if (!카테고리) continue;
      try {
        const r = runCalculation(input1, buildEngineInput2(input2.scenarios), 카테고리);
        const rep = pickRepresentative(r.output2);
        if (rep) out[p.id] = rep;
      } catch { /* 판정 계산 실패 시 해당 카드만 요약 생략 */ }
    }
    if (alive) setVerdicts(out);
  })();
  return () => { alive = false; };
}, [ready, projects]);
```

카드 렌더(`.dash__grid` 내부)를 다음으로 교체:

```jsx
{(projects ?? []).map(p => {
  const st = stepStatuses(p, null);
  const doneCount = ["info", "calc", "optimize", "report"].filter(s => st[s] === "done").length;
  const next = nextSegment(st);
  const nextStep = STEPS.find(s => s.segment === next);
  const rep = verdicts[p.id];
  const ui = rep ? (VERDICT_UI[rep.만족여부] ?? VERDICT_UI.해당없음) : null;
  return (
    <Card key={p.id} className="dash__item">
      <Link href={`/project/${p.id}/${next}`} className="dash__open">
        <span className="dash__name">{p.name}</span>
        <span className="dash__meta">
          <span className="dash__dots" role="img" aria-label={`4단계 중 ${doneCount}단계 완료`}>
            {["info", "calc", "optimize", "report"].map(s => (
              <i key={s} className={`dash__dot ${st[s] === "done" ? "dash__dot--done" : ""}`} />
            ))}
          </span>
          <span className="dash__date mono">수정 {formatDate(p.updatedAt)}</span>
        </span>
        {rep && ui && (
          <span className="dash__verdict">
            <Badge tone={ui.tone}>{ui.sym} {ui.label}</Badge>
            <span className="mono">설치 {rep.비율.toFixed(1)}% / 의무 {rep.의무비율 !== null ? `${rep.의무비율}%` : "-"}</span>
          </span>
        )}
        <span className="dash__continue">이어서: {nextStep.label} →</span>
      </Link>
      <div className="dash__row-actions">
        <Button size="sm" variant="danger" onClick={() => setDeleteTarget(p)}>삭제</Button>
      </div>
    </Card>
  );
})}
```

- [ ] **Step 2: dashboard.css 추가** (기존 규칙 유지, 아래 추가)

```css
.dash__meta { display: flex; align-items: center; gap: var(--sp-3); }
.dash__dots { display: inline-flex; gap: var(--sp-1); }
.dash__dot {
  width: 8px; height: 8px; border-radius: 50%;
  border: 1.5px solid var(--border-strong); display: inline-block;
}
.dash__dot--done { background: var(--accent-graphic); border-color: var(--accent-graphic); }
.dash__verdict { display: flex; align-items: center; gap: var(--sp-2); font-size: var(--fs-13); color: var(--text-secondary); }
.dash__continue { font-size: var(--fs-13); color: var(--accent-action); font-weight: 700; }
.dash__open:hover .dash__continue { text-decoration: underline; }
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → 61/61 PASS

```bash
git add app/page.js app/dashboard.css
git commit -m "feat: 대시보드 카드 정보화 — 단계 인디케이터·판정 요약·이어서 딥링크"
```

---

### Task 3: 단계 유도 바 (WorkspaceShell)

**Files:**
- Modify: `app/project/[id]/WorkspaceShell.jsx`, `app/project/[id]/workspace.css`

**Interfaces:**
- Consumes: `STEPS`(lib/workspaceSteps.js), 기존 `activeSegment`·`projectId`, button.css의 `.btn .btn--primary .btn--ghost .btn--md`

- [ ] **Step 1: WorkspaceShell.jsx — `{children}` 아래 CTA 바 추가**

`<main className="ws__content">{children}</main>` 를 다음으로 교체:

```jsx
<main className="ws__content">
  {children}
  <div className="ws__next">
    {(() => {
      const idx = STEPS.findIndex(s => s.segment === activeSegment);
      const next = STEPS[idx + 1] ?? null;
      return next ? (
        <>
          <span className="ws__next-hint">다음 단계 — {next.desc}</span>
          <Link className="btn btn--primary btn--md" href={`/project/${projectId}/${next.segment}`}>
            다음: {next.label} →
          </Link>
        </>
      ) : (
        <>
          <span className="ws__next-hint">검토 절차의 마지막 단계입니다</span>
          <Link className="btn btn--ghost btn--md" href="/">완료 — 대시보드로</Link>
        </>
      );
    })()}
  </div>
</main>
```

- [ ] **Step 2: workspace.css 추가**

```css
.ws__next {
  display: flex; align-items: center; justify-content: space-between; gap: var(--sp-4);
  margin-top: var(--sp-8); padding-top: var(--sp-5);
  border-top: 1px solid var(--border-default);
}
.ws__next-hint { font-size: var(--fs-13); color: var(--text-secondary); }
.ws__next .btn { text-decoration: none; }
@media (max-width: 767px) {
  .ws__next { flex-direction: column; align-items: stretch; text-align: center; }
}
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → 61/61 PASS

```bash
git add "app/project/[id]/WorkspaceShell.jsx" "app/project/[id]/workspace.css"
git commit -m "feat: 단계 유도 바 — 워크스페이스 하단 다음 단계 CTA (마지막은 대시보드 복귀)"
```

---

### Task 4: E2E 게이트 (컨트롤러 실행)

**Files:**
- 변경 없음 — 기존 `verify/e2e_redesign.mjs` 재사용

- [ ] **Step 1: 푸시** — `git push -u origin feature/dashboard-flow`, Vercel 프리뷰 빌드 대기(commit status success)
- [ ] **Step 2: E2E 실행** — `export $(grep VERCEL_AUTOMATION_BYPASS_SECRET .env.local) && node verify/e2e_redesign.mjs <프리뷰URL> verify/e2e_out` → Expected: `PASS: 오버플로 0 · 모달 트랩 통과`
- [ ] **Step 3: 스크린샷 육안** — dash-1440/dash-375(단계 점·판정 요약·이어서 링크), calc-1440(하단 CTA 바) 확인
- [ ] **Step 4: 결과 보고** — 게이트 결과 정리 후 사용자에게 보고. main 머지는 사용자 승인 후

## Self-Review 기록

- 스펙 §2→Task 1, §3→Task 2, §4→Task 3, §5 게이트→Task 1·4. 갭 없음
- 타입 일관성: `pickRepresentative`/`nextSegment`/`VERDICT_UI` (T1 정의 = T2 사용), `stepStatuses(p, null)` — activeSegment에 null 전달 시 어떤 segment와도 불일치 → active 없이 done/todo만 반환(기존 구현이 `seg === activeSegment` 비교이므로 안전)
- 대시보드 판정 훅의 `projects` 의존성: 삭제·가져오기 시 재계산됨(listProjects 새 배열) — 의도된 동작
