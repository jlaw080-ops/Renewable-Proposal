# Phase 3 — 검토 계산 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 마스터플랜: `docs/superpowers/plans/2026.07.26_전면개편_마스터플랜.md` (Global Constraints 포함)

**Goal:** 시나리오(ALT) 편집기 + `runCalculation` 훅 연결 + Output1·Output2 결과 표시 + 의무비율 판정 + 카테고리(규모등급) 자동 판정으로 워크스페이스 ② 검토 계산 화면을 완성한다.

**Architecture:** 규모등급 판정·시나리오 모델·엔진 입력 변환은 순수 함수 라이브러리(`lib/calcModel.js`)로 분리해 node:test로 검증한다. UI는 시나리오 편집기·결과 패널 컴포넌트로 나누고, calc 페이지가 `useEngineReady()` 후 `@/engine/index.js`의 `runCalculation`을 직접 호출한다(자동 재계산, 결과는 파생값이라 저장하지 않음). 시나리오는 `data.input2` 봉투에 엔진과 동일 형태로 저장한다. **카테고리는 사용자 결정(2026-07-28)에 따라 legacy와 동일하게 자동 판정·읽기 전용**이며, 이를 위해 사업정보 ① 공동주택 행에 세대수 선택지를 추가한다.

**Tech Stack:** Next.js App Router·기존 계산 엔진(`engine/` 무수정)·Phase 1 공통 컴포넌트·node:test

## Global Constraints (마스터플랜 전체 적용 + Phase 3 추가)

- **엔진 무수정**: `engine/` `public/*` 수정 금지. `lib/engineScripts.js`·`lib/useEngineReady.js`·`app/engine-check/`·`legacy/`도 수정 금지
- **계산 동일성**: 최종 Task에서 `node verify/verify_all.js public`·`node verify/verify_output12.js public` 결과가 `verify/snapshots/` 기준과 IDENTICAL
- **브랜치**: `revamp/next`만. main 푸시 금지. **`git add -A`·`git add .` 절대 금지** — 명시 경로만 add
- JavaScript(TS 금지), plain CSS + 토큰만(`var(--…)` — 리터럴 색·크기 금지), 파일 200~400줄 지향, UI 문구 한국어
- localStorage 접근은 useEffect 안에서만 (하이드레이션 안전)
- **엔진 호출 계약**: `runCalculation(input1, input2, 카테고리)` → `{ output1, output2 }` (`engine/index.js:24`). input2 = `{ scenarios: [{ id, systems: [{ 에너지원, 형식, 적용용량, 단위에너지생산량, 원별보정계수 }] }] }`. **엔진은 계수를 조회하지 않음** — UI가 `get신재생에너지계수()`로 채워서 전달해야 함 (`engine/output2Calculator.js:28`이 `?? 0` 폴백)
- **환경 교훈**: 로컬 build/dev 매우 느림(Dropbox) — 통합 검증은 푸시 → Vercel Preview + 로컬 Playwright(`node_modules/playwright`, `chromium.launch({ channel: "msedge" })`, Vercel MCP `get_access_to_vercel_url`로 share URL 획득) 방식. 작업 후 잔류 node 프로세스 정리

## 파일 구조 (이번 Phase 산출물)

```
lib/calcModel.js                  규모등급 자동판정·시나리오 모델·엔진 input2 변환·검증·포맷 (순수 함수)
tests/calcModel.test.mjs          위 단위테스트
lib/formOptions.js                수정: 세대수_OPTIONS 추가
app/project/[id]/info/page.js     수정: 공동주택 행에 세대수 선택지 (규모등급 판정 입력)
app/project/[id]/info/info.css    수정: 세대수 행 스타일 몇 줄
components/calc/ScenarioEditor.jsx|scenarioEditor.css   ALT·시스템 편집 (계수 자동 반영)
components/calc/Output1Table.jsx  예상에너지사용량 표 (합계 포함)
components/calc/Output2Panel.jsx  ALT별 KPI 3장 + 판정 Badge + 시스템 상세 표
components/calc/results.css       결과 패널 공용 스타일
app/project/[id]/calc/page.js     ② 검토 계산 화면 전면 재작성 (자리 페이지 대체)
app/project/[id]/calc/calc.css
```

## 데이터 계약 (Phase 4~5가 의존)

- `data.input1.용도별연면적목록[i].세대수`: `"" | "1000세대 이상" | "300세대 이상~1000세대 미만" | "30세대 이상~300세대 미만" | "30세대 미만"` — 공동주택 행에서만 의미. 기존 저장 프로젝트에는 없음 → `?? ""` 처리 (마이그레이션 불필요)
- `data.input2 = { scenarios: [{ id: "ALT-1", systems: [{ 에너지원: "", 형식: "", 적용용량: ""|number, 단위에너지생산량: number, 원별보정계수: number }] }] }` — 적용용량은 UI에서 `""` 허용, 엔진 전달 시 `buildEngineInput2`가 `Number()` 변환
- **카테고리·output1·output2는 저장하지 않는다** (파생값 — 매 렌더 재계산). Phase 5 보고서도 재계산으로 얻는다
- 만족여부 표시 매핑: `"Yes"` → Badge pass "만족", `"No"` → Badge fail "불만족", `"해당없음"` → Badge na "해당없음"

---

### Task 1: lib/calcModel.js — 규모등급·시나리오 모델 (TDD)

**Files:**
- Create: `lib/calcModel.js`, `tests/calcModel.test.mjs`
- Modify: `lib/formOptions.js` (세대수_OPTIONS 추가)

**Interfaces:**
- Produces:
  - `calc규모등급(용도별연면적목록): "가"|"나"|"다"|"라"|""` — legacy `index.html:710-730` 검증본 이식. 전 행이 공동주택(주거)이면 세대수 문자열 기준, 하나라도 비주거면 연면적 합계 기준(≥100000 가, ≥10000 나, ≥3000 다, 미만 라). 주거인데 세대수 미선택 → `""`
  - `EMPTY_SYSTEM`, `newScenario(altNo): { id: "ALT-"+altNo, systems: [{...EMPTY_SYSTEM}] }`, `EMPTY_INPUT2`
  - `nextAltNo(scenarios): number` — 기존 id의 숫자 접미사 최댓값+1 (중간 삭제 후에도 중복 없음)
  - `canCalculate(input1): { ok: boolean, missing: string[] }` — 필수: 사업형태·사업연도·대지위치·유효 용도행(용도 선택 + 연면적>0) 1개 이상
  - `buildEngineInput2(scenarios): { scenarios }` — 적용용량 `Number()||0` 변환한 엔진 입력 (원본 불변)
  - `fmtNum(n): string` — null/undefined/NaN → `"-"`, 그 외 `toLocaleString("ko-KR")`
- `lib/formOptions.js`에 추가: `세대수_OPTIONS = ["1000세대 이상", "300세대 이상~1000세대 미만", "30세대 이상~300세대 미만", "30세대 미만"]` (legacy `index.html:1094-1099` 순서 그대로)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/calcModel.test.mjs` (전문)

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import {
  calc규모등급, EMPTY_SYSTEM, newScenario, EMPTY_INPUT2,
  nextAltNo, canCalculate, buildEngineInput2, fmtNum,
} from "../lib/calcModel.js";
import { 세대수_OPTIONS } from "../lib/formOptions.js";

test("세대수_OPTIONS는 legacy 4구간과 일치한다", () => {
  assert.deepEqual(세대수_OPTIONS, [
    "1000세대 이상", "300세대 이상~1000세대 미만", "30세대 이상~300세대 미만", "30세대 미만",
  ]);
});

test("비주거 포함 시 연면적 합계 기준으로 규모등급을 판정한다", () => {
  const rows = [{ 용도: "공동주택", 연면적: 15000 }, { 용도: "판매 및 영업시설", 연면적: 5000 }];
  assert.equal(calc규모등급(rows), "나");                                    // 합계 20000
  assert.equal(calc규모등급([{ 용도: "업무시설(상업)", 연면적: 100000 }]), "가");
  assert.equal(calc규모등급([{ 용도: "업무시설(상업)", 연면적: 3000 }]), "다");
  assert.equal(calc규모등급([{ 용도: "업무시설(상업)", 연면적: 2999 }]), "라");
});

test("주거(공동주택만)는 세대수 기준으로 판정한다", () => {
  assert.equal(calc규모등급([{ 용도: "공동주택", 연면적: 50000, 세대수: "1000세대 이상" }]), "가");
  assert.equal(calc규모등급([{ 용도: "공동주택", 연면적: 50000, 세대수: "300세대 이상~1000세대 미만" }]), "나");
  assert.equal(calc규모등급([{ 용도: "공동주택", 연면적: 50000, 세대수: "30세대 이상~300세대 미만" }]), "다");
  assert.equal(calc규모등급([{ 용도: "공동주택", 연면적: 50000, 세대수: "30세대 미만" }]), "라");
  assert.equal(calc규모등급([{ 용도: "공동주택", 연면적: 50000 }]), "");     // 세대수 미선택
});

test("연면적이 빈 문자열이어도 숫자 변환해 판정한다", () => {
  assert.equal(calc규모등급([{ 용도: "업무시설(상업)", 연면적: "" }]), "라"); // 0
  assert.equal(calc규모등급([{ 용도: "업무시설(상업)", 연면적: "10000" }]), "나");
});

test("newScenario·EMPTY_INPUT2·nextAltNo", () => {
  assert.deepEqual(newScenario(1), { id: "ALT-1", systems: [{ ...EMPTY_SYSTEM }] });
  assert.deepEqual(EMPTY_INPUT2, { scenarios: [newScenario(1)] });
  assert.equal(nextAltNo([{ id: "ALT-1" }, { id: "ALT-3" }]), 4);           // 중간 삭제 후 중복 방지
  assert.equal(nextAltNo([]), 1);
});

test("canCalculate는 필수값 누락을 알려준다", () => {
  const ok = canCalculate({
    사업형태: "민간", 사업연도: "2025", 대지위치: "서울특별시",
    용도별연면적목록: [{ 용도: "공동주택", 연면적: 15000 }],
  });
  assert.equal(ok.ok, true);
  assert.deepEqual(ok.missing, []);

  const bad = canCalculate({ 사업형태: "", 사업연도: "2025", 대지위치: "", 용도별연면적목록: [{ 용도: "", 연면적: "" }] });
  assert.equal(bad.ok, false);
  assert.ok(bad.missing.includes("사업형태"));
  assert.ok(bad.missing.includes("대지위치"));
  assert.ok(bad.missing.includes("용도별 연면적"));
});

test("buildEngineInput2는 적용용량을 숫자로 변환하고 원본을 바꾸지 않는다", () => {
  const scenarios = [{ id: "ALT-1", systems: [
    { 에너지원: "태양광", 형식: "태양광-고정식", 적용용량: "50", 단위에너지생산량: 1358, 원별보정계수: 0.95 },
    { 에너지원: "", 형식: "", 적용용량: "", 단위에너지생산량: 0, 원별보정계수: 0 },
  ] }];
  const built = buildEngineInput2(scenarios);
  assert.equal(built.scenarios[0].systems[0].적용용량, 50);
  assert.equal(built.scenarios[0].systems[1].적용용량, 0);
  assert.equal(scenarios[0].systems[0].적용용량, "50");                      // 불변
});

test("fmtNum", () => {
  assert.equal(fmtNum(5492250), "5,492,250");
  assert.equal(fmtNum(null), "-");
  assert.equal(fmtNum(undefined), "-");
  assert.equal(fmtNum(NaN), "-");
});
```

- [ ] **Step 2: 실행해 실패 확인** — Run: `npm test` / Expected: 신규 테스트 FAIL (모듈 없음)

- [ ] **Step 3: 최소 구현** — `lib/formOptions.js` 하단에 추가:

```js
// 공동주택 세대수 구간 — 규모등급(카테고리) 자동 판정용, legacy와 일치 (변경 금지)
export const 세대수_OPTIONS = [
  "1000세대 이상", "300세대 이상~1000세대 미만", "30세대 이상~300세대 미만", "30세대 미만",
];
```

`lib/calcModel.js` (전문 — 판정 로직은 legacy `index.html:710-730` 이식):

```js
// 검토 계산 순수 함수 모델 — 규모등급 판정·시나리오 구조·엔진 입력 변환 (legacy 검증 로직 이식)

export function calc규모등급(용도별연면적목록 = []) {
  const 주거 = !용도별연면적목록.some(x => x.용도 !== "공동주택");
  if (주거) {
    const apt = 용도별연면적목록.find(x => x.용도 === "공동주택");
    const 세대수 = apt?.세대수 ?? "";
    if (세대수 === "1000세대 이상") return "가";
    if (세대수 === "300세대 이상~1000세대 미만") return "나";
    if (세대수 === "30세대 이상~300세대 미만") return "다";
    if (세대수 === "30세대 미만") return "라";
    return "";
  }
  const 합계 = 용도별연면적목록.reduce((s, x) => s + (Number(x.연면적) || 0), 0);
  if (합계 >= 100000) return "가";
  if (합계 >= 10000) return "나";
  if (합계 >= 3000) return "다";
  return "라";
}

export const EMPTY_SYSTEM = { 에너지원: "", 형식: "", 적용용량: "", 단위에너지생산량: 0, 원별보정계수: 0 };

export function newScenario(altNo) {
  return { id: `ALT-${altNo}`, systems: [{ ...EMPTY_SYSTEM }] };
}

export const EMPTY_INPUT2 = { scenarios: [newScenario(1)] };

export function nextAltNo(scenarios = []) {
  const max = scenarios.reduce((m, sc) => {
    const n = Number(String(sc.id).replace(/^ALT-/, ""));
    return Number.isFinite(n) && n > m ? n : m;
  }, 0);
  return max + 1;
}

export function canCalculate(input1 = {}) {
  const missing = [];
  if (!input1.사업형태) missing.push("사업형태");
  if (!input1.사업연도) missing.push("사업연도");
  if (!input1.대지위치) missing.push("대지위치");
  const rows = input1.용도별연면적목록 ?? [];
  if (!rows.some(r => r.용도 && (Number(r.연면적) || 0) > 0)) missing.push("용도별 연면적");
  return { ok: missing.length === 0, missing };
}

export function buildEngineInput2(scenarios = []) {
  return {
    scenarios: scenarios.map(sc => ({
      ...sc,
      systems: sc.systems.map(s => ({ ...s, 적용용량: Number(s.적용용량) || 0 })),
    })),
  };
}

export function fmtNum(n) {
  if (n === null || n === undefined || Number.isNaN(n)) return "-";
  return Number(n).toLocaleString("ko-KR");
}
```

- [ ] **Step 4: 테스트 통과 확인** — Run: `npm test` / Expected: 기존 21건 + 신규 8건 = 29건 전부 PASS, 스킵 0

- [ ] **Step 5: 커밋**

```bash
git add lib/calcModel.js tests/calcModel.test.mjs lib/formOptions.js
git commit -m "feat: calcModel — 규모등급 자동판정·시나리오 모델·엔진 input2 변환 (legacy 이식, TDD 8건)"
```

---

### Task 2: 사업정보 ① — 공동주택 행 세대수 선택지

**Files:**
- Modify: `app/project/[id]/info/page.js` (공동주택 행에 세대수 select 추가)
- Modify: `app/project/[id]/info/info.css` (세대수 행 스타일)

**Interfaces:**
- Consumes: `세대수_OPTIONS`(Task 1), 기존 `applyRow(i, patch)` 패턴
- Produces: `input1.용도별연면적목록[i].세대수` 저장 (데이터 계약 참조). 용도가 공동주택이 아니면 select 숨김(값은 유지 — legacy 동작 동일, 판정은 공동주택일 때만 사용하므로 무해)

- [ ] **Step 1: page.js 수정** — import에 `세대수_OPTIONS` 추가:

```js
import { 사업형태_OPTIONS, 사업연도_OPTIONS, 용도_OPTIONS, EMPTY_INPUT1, 세대수_OPTIONS } from "@/lib/formOptions";
```

용도별 행 렌더(`.info__row` 블록)를 다음으로 교체 — 각 행을 fragment로 감싸고 공동주택일 때 세대수 서브행 추가:

```jsx
{input1.용도별연면적목록.map((row, i) => (
  <div className="info__rowgroup" key={i}>
    <div className="info__row">
      <Select label={i === 0 ? "용도" : undefined} placeholder="용도 선택" options={toOptions(용도_OPTIONS)}
        value={row.용도} onChange={e => applyRow(i, { 용도: e.target.value })} />
      <Field label={i === 0 ? "연면적 (㎡)" : undefined} type="number" mono value={row.연면적}
        onChange={e => applyRow(i, { 연면적: num(e.target.value) })} />
      <Button size="sm" variant="danger" disabled={input1.용도별연면적목록.length <= 1}
        onClick={() => apply({ 용도별연면적목록: input1.용도별연면적목록.filter((_, idx) => idx !== i) })}>
        삭제
      </Button>
    </div>
    {row.용도 === "공동주택" && (
      <div className="info__seda">
        <Select label="세대 수 (규모등급 판정용)" placeholder="선택하세요" options={toOptions(세대수_OPTIONS)}
          value={row.세대수 ?? ""} onChange={e => applyRow(i, { 세대수: e.target.value })} />
      </div>
    )}
  </div>
))}
```

- [ ] **Step 2: info.css에 추가**

```css
.info__rowgroup { display: flex; flex-direction: column; gap: var(--sp-2); }
.info__seda { max-width: 320px; }
```

- [ ] **Step 3: 확인** — `npm test` 29건 PASS (회귀). UI 확인은 Task 6 Preview에서.

- [ ] **Step 4: 커밋**

```bash
git add app/project/[id]/info/page.js app/project/[id]/info/info.css
git commit -m "feat: 사업정보 — 공동주택 행 세대수 선택지 (규모등급 자동 판정 입력)"
```

---

### Task 3: ScenarioEditor — ALT·시스템 편집 + 계수 자동 반영

**Files:**
- Create: `components/calc/ScenarioEditor.jsx`, `components/calc/scenarioEditor.css`

**Interfaces:**
- Consumes: Phase 1 `Select`·`Field`·`Button`·`Card`, `EMPTY_SYSTEM`·`newScenario`·`nextAltNo`·`fmtNum`(Task 1)
- Produces: `<ScenarioEditor scenarios={...} lib={{ 에너지원목록, 형식목록, 계수 }} onChange={nextScenarios => ...}>`
  - `lib.에너지원목록: string[]`, `lib.형식목록: (에너지원) => string[]`, `lib.계수: (에너지원, 형식) => { 단위에너지생산량, 원별보정계수 } | null` — calc 페이지가 `@/engine/libraryLoader`의 `get에너지원목록`·`get형식목록`·`get신재생에너지계수`를 감싸 전달
  - 동작 계약(legacy `index.html:1289-1315` 동일): 에너지원 변경 → 형식·계수 리셋 / 형식 변경 → 계수 자동 채움 / 적용용량 입력 → 행별 생산량(적용용량×단위에너지생산량×원별보정계수) 실시간 표시

- [ ] **Step 1: ScenarioEditor.jsx** (전문)

```jsx
"use client";
import Card from "@/components/ui/Card";
import Field from "@/components/ui/Field";
import Select from "@/components/ui/Select";
import Button from "@/components/ui/Button";
import { EMPTY_SYSTEM, newScenario, nextAltNo, fmtNum } from "@/lib/calcModel";
import "./scenarioEditor.css";

const toOptions = arr => arr.map(v => ({ value: v, label: v }));

export default function ScenarioEditor({ scenarios, lib, onChange }) {
  function patchSystem(si, yi, patch) {
    onChange(scenarios.map((sc, i) => i !== si ? sc : {
      ...sc,
      systems: sc.systems.map((s, j) => (j === yi ? { ...s, ...patch } : s)),
    }));
  }

  function changeEnergy(si, yi, 에너지원) {
    patchSystem(si, yi, { 에너지원, 형식: "", 단위에너지생산량: 0, 원별보정계수: 0 });
  }

  function changeType(si, yi, 형식) {
    const sys = scenarios[si].systems[yi];
    const c = lib.계수(sys.에너지원, 형식);
    patchSystem(si, yi, {
      형식,
      단위에너지생산량: c?.단위에너지생산량 ?? 0,
      원별보정계수: c?.원별보정계수 ?? 0,
    });
  }

  return (
    <div className="se">
      {scenarios.map((sc, si) => (
        <Card key={sc.id} title={sc.id} inner actions={
          <div className="se__actions">
            <Button size="sm" variant="ghost"
              onClick={() => onChange(scenarios.map((s, i) => i !== si ? s : { ...s, systems: [...s.systems, { ...EMPTY_SYSTEM }] }))}>
              시스템 추가
            </Button>
            <Button size="sm" variant="danger" disabled={scenarios.length <= 1}
              onClick={() => onChange(scenarios.filter((_, i) => i !== si))}>
              ALT 삭제
            </Button>
          </div>
        }>
          <div className="se__systems">
            {sc.systems.map((sys, yi) => {
              const 생산량 = (Number(sys.적용용량) || 0) * sys.단위에너지생산량 * sys.원별보정계수;
              return (
                <div className="se__system" key={yi}>
                  <div className="se__row">
                    <Select label={yi === 0 ? "에너지원" : undefined} placeholder="선택" options={toOptions(lib.에너지원목록)}
                      value={sys.에너지원} onChange={e => changeEnergy(si, yi, e.target.value)} />
                    <Select label={yi === 0 ? "형식" : undefined} placeholder="선택"
                      options={toOptions(sys.에너지원 ? lib.형식목록(sys.에너지원) : [])}
                      value={sys.형식} onChange={e => changeType(si, yi, e.target.value)} />
                    <Field label={yi === 0 ? "적용용량 (kW)" : undefined} type="number" mono value={sys.적용용량}
                      onChange={e => patchSystem(si, yi, { 적용용량: e.target.value === "" ? "" : Number(e.target.value) })} />
                    <Button size="sm" variant="danger" disabled={sc.systems.length <= 1}
                      onClick={() => onChange(scenarios.map((s, i) => i !== si ? s : { ...s, systems: s.systems.filter((_, j) => j !== yi) }))}>
                      삭제
                    </Button>
                  </div>
                  <p className="se__meta mono">
                    단위에너지생산량 {fmtNum(sys.단위에너지생산량)} kWh/kW·yr · 원별보정계수 {sys.원별보정계수 || "-"} ·
                    생산량 {생산량 > 0 ? `${fmtNum(Math.round(생산량))} kWh/yr` : "-"}
                  </p>
                </div>
              );
            })}
          </div>
        </Card>
      ))}
      <Button variant="ghost" onClick={() => onChange([...scenarios, newScenario(nextAltNo(scenarios))])}>
        + ALT 추가
      </Button>
    </div>
  );
}
```

- [ ] **Step 2: scenarioEditor.css** (전문)

```css
.se { display: flex; flex-direction: column; gap: var(--sp-4); align-items: flex-start; }
.se > * { width: 100%; }
.se > button { width: auto; }
.se__actions { display: flex; gap: var(--sp-2); }
.se__systems { display: flex; flex-direction: column; gap: var(--sp-3); }
.se__system { display: flex; flex-direction: column; gap: var(--sp-1); }
.se__row { display: grid; grid-template-columns: 1fr 1.4fr 1fr auto; gap: var(--sp-3); align-items: end; }
.se__meta { font-size: var(--fs-12); color: var(--text-hint); }
@media (max-width: 767px) { .se__row { grid-template-columns: 1fr; } }
```

- [ ] **Step 3: 확인** — `npm test` 29건 PASS (회귀). 컴포넌트 동작은 Task 6 Preview에서.

- [ ] **Step 4: 커밋**

```bash
git add components/calc/ScenarioEditor.jsx components/calc/scenarioEditor.css
git commit -m "feat: ScenarioEditor — ALT·시스템 편집, 에너지원→형식 캐스케이드·계수 자동 반영"
```

---

### Task 4: 결과 패널 — Output1 표 + Output2 KPI·판정

**Files:**
- Create: `components/calc/Output1Table.jsx`, `components/calc/Output2Panel.jsx`, `components/calc/results.css`

**Interfaces:**
- Consumes: Phase 1 `Table`·`Badge`·`Card`, `fmtNum`(Task 1)
- Produces:
  - `<Output1Table output1={...}>` — `output1 = { 용도별결과: [{용도,연면적,지역계수,단위에너지사용량,예상에너지사용량}], 총연면적, 총예상에너지사용량 }` (엔진 반환 그대로)
  - `<Output2Panel alt={...}>` — `alt = { id, systems:[{에너지원,형식,적용용량,신재생에너지생산량}], 생산량합계, 총에너지사용량, 비율, 의무비율, 만족여부 }` (엔진 반환 그대로). 비율은 소수 1자리(legacy 동일), 판정 Badge 매핑은 데이터 계약 참조

- [ ] **Step 1: Output1Table.jsx** (전문)

```jsx
"use client";
import Table from "@/components/ui/Table";
import { fmtNum } from "@/lib/calcModel";
import "./results.css";

const COLS = [
  { key: "용도", header: "용도" },
  { key: "연면적", header: "연면적 (㎡)", align: "right", mono: true, render: r => fmtNum(r.연면적) },
  { key: "지역계수", header: "지역계수", align: "right", mono: true },
  { key: "단위에너지사용량", header: "단위에너지 (kWh/㎡·yr)", align: "right", mono: true, render: r => fmtNum(r.단위에너지사용량) },
  { key: "예상에너지사용량", header: "예상에너지사용량 (kWh/yr)", align: "right", mono: true, render: r => fmtNum(r.예상에너지사용량) },
];

export default function Output1Table({ output1 }) {
  return (
    <div className="res">
      <Table columns={COLS} rows={output1.용도별결과} rowKey={(r) => r.용도 + r.연면적} />
      <p className="res__total mono">
        합계 — 연면적 {fmtNum(output1.총연면적)} ㎡ · 총예상에너지사용량 {fmtNum(output1.총예상에너지사용량)} kWh/yr
      </p>
    </div>
  );
}
```

- [ ] **Step 2: Output2Panel.jsx** (전문)

```jsx
"use client";
import Table from "@/components/ui/Table";
import Badge from "@/components/ui/Badge";
import { fmtNum } from "@/lib/calcModel";
import "./results.css";

const SYS_COLS = [
  { key: "에너지원", header: "에너지원" },
  { key: "형식", header: "형식" },
  { key: "적용용량", header: "적용용량 (kW)", align: "right", mono: true, render: r => fmtNum(r.적용용량) },
  { key: "신재생에너지생산량", header: "생산량 (kWh/yr)", align: "right", mono: true, render: r => fmtNum(Math.round(r.신재생에너지생산량)) },
];

const 판정 = {
  Yes: { tone: "pass", label: "만족" },
  No: { tone: "fail", label: "불만족" },
  해당없음: { tone: "na", label: "해당없음" },
};

export default function Output2Panel({ alt }) {
  const j = 판정[alt.만족여부] ?? 판정.해당없음;
  return (
    <div className="res res__alt">
      <div className="res__kpis">
        <div className="res__kpi">
          <dt>신재생에너지 설치비율</dt>
          <dd className="mono">{alt.비율.toFixed(1)}%</dd>
          <p>생산량 {fmtNum(Math.round(alt.생산량합계))} kWh/yr</p>
        </div>
        <div className="res__kpi">
          <dt>의무설치비율</dt>
          <dd className="mono">{alt.의무비율 !== null ? `${alt.의무비율}%` : "-"}</dd>
          <p>총에너지 {fmtNum(alt.총에너지사용량)} kWh/yr</p>
        </div>
        <div className={`res__kpi res__kpi--${j.tone}`}>
          <dt>의무비율 만족여부</dt>
          <dd><Badge tone={j.tone}>{j.label}</Badge></dd>
          <p>{alt.id}</p>
        </div>
      </div>
      <Table columns={SYS_COLS} rows={alt.systems} rowKey={(r, i) => `${r.에너지원}-${r.형식}-${i}`}
        empty="에너지원·형식·적용용량을 입력하면 생산량이 계산됩니다" />
    </div>
  );
}
```

주의: `Table`의 `rowKey`가 인덱스 인자를 지원하지 않으면(`components/ui/Table.jsx` 확인) `rowKey={r => r.에너지원 + r.형식 + r.적용용량}`로 대체하고 보고서에 명기.

- [ ] **Step 3: results.css** (전문)

```css
.res { display: flex; flex-direction: column; gap: var(--sp-3); }
.res__total { font-size: var(--fs-13); color: var(--text-secondary); text-align: right; }
.res__alt { margin-top: var(--sp-2); }
.res__kpis { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: var(--sp-3); }
.res__kpi { border: 1px solid var(--border-default); border-radius: var(--radius-md); padding: var(--sp-4); display: flex; flex-direction: column; gap: var(--sp-1); border-top-width: 3px; }
.res__kpi dt { font-size: var(--fs-12); color: var(--text-hint); }
.res__kpi dd { font-size: var(--fs-20, 20px); font-weight: 700; }
.res__kpi p { font-size: var(--fs-12); color: var(--text-secondary); }
.res__kpi--pass { border-top-color: var(--color-pass); }
.res__kpi--fail { border-top-color: var(--color-fail); }
.res__kpi--na { border-top-color: var(--color-na); }
```

주의: `--fs-20` 토큰이 `styles/tokens.css`에 없으면 존재하는 상위 폰트 토큰(예: `--fs-18` 등 실제 값 확인)으로 대체 — 리터럴 크기 금지 원칙 유지, 대체 시 보고서에 명기.

- [ ] **Step 4: 확인** — `npm test` 29건 PASS (회귀).

- [ ] **Step 5: 커밋**

```bash
git add components/calc/Output1Table.jsx components/calc/Output2Panel.jsx components/calc/results.css
git commit -m "feat: 결과 패널 — Output1 표·합계, ALT별 KPI 3장·판정 Badge·시스템 상세"
```

---

### Task 5: calc 페이지 조립 — 자동 재계산 + 카테고리 자동 판정

**Files:**
- Modify: `app/project/[id]/calc/page.js` (자리 페이지 → 전면 재작성)
- Create: `app/project/[id]/calc/calc.css`

**Interfaces:**
- Consumes: `useEngineReady`(수정 금지 — 사용만), `@/engine/index.js runCalculation`, `@/engine/libraryLoader`의 `get에너지원목록`·`get형식목록`·`get신재생에너지계수`, `projectStore`, Task 1·3·4 산출물
- Produces: `data.input2` 저장. 계산 결과는 저장하지 않음. 카테고리는 `calc규모등급(input1.용도별연면적목록)`으로 자동 판정해 읽기 전용 표시(legacy `index.html:333` 툴팁 문구 유지)

- [ ] **Step 1: page.js 전면 재작성** (전문)

```jsx
"use client";
import { useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { getProject, updateProject } from "@/lib/projectStore";
import { useEngineReady } from "@/lib/useEngineReady";
import { calc규모등급, canCalculate, buildEngineInput2, EMPTY_INPUT2 } from "@/lib/calcModel";
import Card from "@/components/ui/Card";
import Badge from "@/components/ui/Badge";
import ScenarioEditor from "@/components/calc/ScenarioEditor";
import Output1Table from "@/components/calc/Output1Table";
import Output2Panel from "@/components/calc/Output2Panel";
import "./calc.css";

export default function CalcPage() {
  const { id } = useParams();
  const { ready, error: engineError } = useEngineReady();
  const [input1, setInput1] = useState(null);
  const [input2, setInput2] = useState(null);
  const [lib, setLib] = useState(null);          // { 에너지원목록, 형식목록, 계수 }
  const [result, setResult] = useState(null);    // { output1, output2 } | null
  const [calcError, setCalcError] = useState(null);

  useEffect(() => {
    const p = getProject(id);
    if (!p) return;
    setInput1(p.data.input1 ?? null);
    setInput2(p.data.input2 ?? structuredClone(EMPTY_INPUT2));
  }, [id]);

  useEffect(() => {
    if (!ready) return;
    let alive = true;
    import("@/engine/libraryLoader.js").then(m => {
      if (!alive) return;
      setLib({ 에너지원목록: m.get에너지원목록(), 형식목록: m.get형식목록, 계수: m.get신재생에너지계수 });
    });
    return () => { alive = false; };
  }, [ready]);

  const check = canCalculate(input1 ?? {});
  const 카테고리 = calc규모등급(input1?.용도별연면적목록 ?? []);

  useEffect(() => {
    if (!ready || !input1 || !input2 || !check.ok) { setResult(null); return; }
    let alive = true;
    import("@/engine/index.js")
      .then(({ runCalculation }) => runCalculation(input1, buildEngineInput2(input2.scenarios), 카테고리))
      .then(r => { if (alive) { setResult(r); setCalcError(null); } })
      .catch(e => { if (alive) { setResult(null); setCalcError(e.message); } });
    return () => { alive = false; };
  }, [ready, input1, input2, 카테고리, check.ok]); // eslint-disable-line react-hooks/exhaustive-deps

  function applyInput2(nextScenarios) {
    const next = { scenarios: nextScenarios };
    setInput2(next);
    updateProject(id, { data: { input2: next } });
  }

  if (input1 === null && input2 === null) return null; // 프로젝트 로딩(가드는 WorkspaceShell 담당)

  return (
    <div className="calc">
      <Card title="② 검토 계산" actions={
        <span className="calc__cat" title="주거: 세대수 기준 / 비주거: 용도별 연면적 합계 기준으로 자동 결정됩니다">
          규모등급(카테고리) <Badge tone={카테고리 ? "brand" : "na"}>{카테고리 || "미판정"}</Badge>
        </span>
      }>
        {!check.ok && (
          <p className="calc__notice" role="status">
            사업정보가 부족합니다: {check.missing.join(", ")} — ① 사업정보에서 입력을 완료하세요.
          </p>
        )}
        {check.ok && !카테고리 && (
          <p className="calc__notice" role="status">공동주택 세대 수를 선택하면 규모등급이 판정됩니다 (① 사업정보).</p>
        )}
        {engineError && <p className="calc__notice" role="status">엔진 로드 실패: {engineError}</p>}
        {!ready && !engineError && <p className="calc__hint">계산 엔진 로딩 중…</p>}
        {calcError && <p className="calc__notice" role="status">계산 오류: {calcError}</p>}
      </Card>

      <Card title="시나리오 (ALT)">
        {lib && input2
          ? <ScenarioEditor scenarios={input2.scenarios} lib={lib} onChange={applyInput2} />
          : <p className="calc__hint">계산 엔진 로딩 중…</p>}
      </Card>

      {result && (
        <>
          <Card title="예상에너지사용량 (Output 1)">
            <Output1Table output1={result.output1} />
          </Card>
          <Card title="신재생에너지 설치비율 (Output 2)">
            {result.output2.map(alt => <Output2Panel key={alt.id} alt={alt} />)}
          </Card>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 2: calc.css** (전문)

```css
.calc { display: flex; flex-direction: column; gap: var(--sp-5); }
.calc__cat { display: flex; align-items: center; gap: var(--sp-2); font-size: var(--fs-13); color: var(--text-secondary); }
.calc__notice { font-size: var(--fs-13); color: var(--accent-warm-dim); }
.calc__hint { font-size: var(--fs-13); color: var(--text-secondary); }
```

주의: `structuredClone` 미지원 우려 없음(모던 브라우저·Node 18+). `카테고리`가 계산 의존성에 문자열로 들어가므로 참조 안정성 문제 없음.

- [ ] **Step 3: 확인** — `npm test` 29건 PASS. `node --check`는 JSX라 불가 — 세심한 재독으로 JSX 균형 확인. 동작 검증은 Task 6.

- [ ] **Step 4: 커밋**

```bash
git add app/project/[id]/calc/page.js app/project/[id]/calc/calc.css
git commit -m "feat: 검토 계산 화면 — 시나리오 편집·자동 재계산·Output1/2·의무비율 판정·규모등급 자동"
```

---

### Task 6: 통합 검증 — 회귀 + Preview E2E (구앱 값 동일성)

**Files:** 원칙적으로 수정 없음 (결함 발견 시 이번 Phase 산출 파일만 fix 커밋)

- [ ] **Step 1: 회귀** — `npm test`(29건 PASS) + verify 스냅샷: `node verify/verify_all.js public`·`node verify/verify_output12.js public` 결과를 임시 파일로 받아 `verify/snapshots/baseline_verify_all.txt`·`baseline_output12.json`과 diff → IDENTICAL 확인

- [ ] **Step 2: 기대값 산출** — 하드코딩 대신 엔진으로 직접 계산해 E2E 기대값 확보 (Node 스크립트, sdd 워크스페이스에 저장·미커밋):

```js
// expected.mjs — fixture input1 + 태양광-고정식 50kW(계수 채움) + 카테고리 "나"(비주거 20000㎡ 자동판정)로 기대값 산출
import fs from "node:fs";
const setLib = (name, file) => { globalThis.window ??= globalThis; window[name] = JSON.parse(fs.readFileSync(file, "utf8")); };
setLib("LIB_신재생에너지계수", "public/data/신재생에너지계수라이브러리.js".replace(".js", ".json") /* 실제 경로는 verify_output12.js 로드 방식을 그대로 복제 */);
// → 가장 간단한 방법: verify/verify_output12.js를 읽고 동일한 로드 코드를 복사한 뒤
//    input2.scenarios[0].systems[0]에 단위에너지생산량 1358·원별보정계수 0.95를 채우고 카테고리 "나"로 runCalculation 실행
```

기대값(수기 검산): 생산량 = 50×1358×0.95 = **64,505** kWh/yr, 총예상에너지사용량 = **5,492,250**, 비율 = 64505/5492250×100 = **1.1745…% → 표시 "1.2%"**. 의무비율·만족여부는 스크립트 출력(서울·비주거·나·2025)을 기대값으로 사용.

- [ ] **Step 3: 푸시 + 배포** — `git push origin revamp/next` → Vercel MCP로 최신 배포 READY 확인 (빌드 실패 시 로그 확인 후 이번 Phase 파일만 fix)

- [ ] **Step 4: Preview E2E** — 로컬 Playwright(`node_modules/playwright`, msedge 채널, Vercel MCP share URL). 검증 항목:
  1. 프로젝트 시드(localStorage `rp.projects.v1`, input1 = fixture 값: 민간/2025/서울특별시/공동주택 15000+판매 및 영업시설 5000) → `/project/{id}/calc` 진입
  2. 규모등급 Badge = **"나"** (비주거 합계 20000 자동판정, 읽기 전용)
  3. 시나리오: ALT-1에 태양광 → 태양광-고정식 선택 시 메타행에 단위에너지생산량 1,358·원별보정계수 0.95 자동 표시 → 적용용량 50 입력
  4. 결과 자동 갱신: Output1 합계 **5,492,250** 표시, ALT-1 생산량 **64,505**, 설치비율 **1.2%**, 의무비율·판정 = Step 2 스크립트 출력과 일치
  5. ALT 추가 → ALT-2 생성(빈 시스템, 판정 "해당없음" 아님 — 비율 0%로 의무비율 대비 판정), ALT 삭제 동작, 시스템 행 추가/삭제 동작
  6. 새로고침 → input2(시나리오·용량) 유지, 결과 재계산 표시
  7. ① 사업정보에서 공동주택 단일 구성 + 세대수 "1000세대 이상" 시나리오: 별도 시드 프로젝트로 규모등급 "가" 확인, 세대수 미선택 시 "미판정" + 안내 문구
  8. `/engine-check` = 5492250 유지, 콘솔 에러 0 (favicon 404·네이버 지도 인증 실패 제외)
- [ ] **Step 5: 반응형 스팟체크** — 1024·768·375에서 calc 페이지 가로 스크롤 없음, se__row 1열 전환(767 이하) 스크린샷
- [ ] **Step 6: 정리** — 브라우저·잔류 프로세스 종료, 스크린샷은 sdd 워크스페이스에만 저장(레포 오염 금지), main 무영향 확인

Expected: 구앱과 동일한 계산 값(Output1 합계·생산량·비율) + 판정 표시 (마스터플랜 Phase 3 완료 기준: verify_output12 스냅샷 100% 일치 + 구앱 화면과 동일 값).

---

## Self-Review 결과

- **스펙 커버리지**: 마스터플랜 Phase 3 행 전 항목 매핑 — 시나리오(ALT) 편집기·계수 자동 반영(Task 3), runCalculation 훅 연결(Task 5), Output1·Output2 결과 테이블(Task 4), 의무비율 충족 판정 표시(Task 4), 카테고리 가~라(Task 1·2·5 — 사용자 결정으로 legacy 동일 자동 판정, 세대수 입력 추가)
- **플레이스홀더 스캔**: Task 6 Step 2의 expected.mjs는 의도적으로 "verify_output12.js 로드 방식 복제" 지시 — 실행 시 해당 파일이 레포에 존재하므로 실행자가 참조 가능, 구체 절차 명시됨. 그 외 전 코드 전문 포함
- **타입 일관성**: `lib` prop 형태(에너지원목록 배열·형식목록 함수·계수 함수) ↔ ScenarioEditor 사용 ↔ libraryLoader 시그니처 일치. `alt` 필드명 ↔ `engine/output2Calculator.js` 반환 일치. `EMPTY_SYSTEM` 필드 ↔ 데이터 계약 ↔ buildEngineInput2 일치. Task 4 두 곳의 토큰 존재 여부 확인 지시(--fs-20, rowKey 인덱스) 포함
- **주의 승계**: 결과 미저장(파생값) 원칙, 컴포넌트 CSS 토큰만, localStorage는 useEffect에서만
