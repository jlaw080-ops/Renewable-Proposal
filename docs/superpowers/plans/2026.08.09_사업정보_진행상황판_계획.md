# 사업정보 입력 진행 상황판 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ① 사업정보 화면 최상단에 "계산에 필수인 항목" 체크리스트를 띄워 무엇이 남았는지 즉시 보이게 한다.

**Architecture:** 판정은 기존 `canCalculate`/`calc규모등급` 재사용(중복 정의 금지). 신규 표시 컴포넌트 1개 + 라벨 계약 테스트. 폼·저장 로직 무변경.

**Tech Stack:** Next.js(App Router, JS), Vanilla CSS(기존 토큰), `node --test`, Playwright(msedge).

**스펙:** `docs/superpowers/specs/2026.08.09_사업정보_진행상황판_설계.md` (승인됨)

## Global Constraints

- `legacy/`·`engine/`·`public/` 무수정, 데이터 스키마 무변경
- 신규 색·크기 리터럴 금지 — 기존 토큰만
- 테스트: `npm test` = `node --test "tests/*.test.mjs"`, 현재 74/74 통과 상태에서 시작
- 로컬 `npm run build`/`dev` 금지 — 시각 검증은 Vercel Preview
- 브랜치 `feature/info-checklist`. 커밋 `<type>: <한국어 설명>`. main 머지는 계획 밖(사용자 승인 후)
- 기존 인터페이스(변경 금지): `canCalculate(input1)` → `{ok, missing}` (`lib/calcModel.js:37`, missing 라벨 = `"사업형태"`·`"사업연도"`·`"대지위치"`·`"용도별 연면적"`) · `calc규모등급(용도별연면적목록)` → 등급 문자열 또는 `""` · `Card({title, inner, className, children})`

---

### Task 1: 라벨 계약 테스트

체크리스트가 `canCalculate`의 `missing` 라벨에 의존하므로, 라벨이 바뀌면 상황판이 조용히 깨진다. 그 계약을 테스트로 고정한다.

**Files:**
- Create: `tests/requiredLabels.test.mjs`

**Interfaces:**
- Consumes: `canCalculate` (lib/calcModel.js)
- Produces: 라벨 4종의 정확한 문자열 보장. Task 2가 이 라벨을 그대로 사용

- [ ] **Step 1: 테스트 작성**

```js
// tests/requiredLabels.test.mjs — 진행 상황판이 의존하는 canCalculate missing 라벨 계약
import { test } from "node:test";
import assert from "node:assert/strict";
import { canCalculate } from "../lib/calcModel.js";

const REQUIRED_LABELS = ["사업형태", "사업연도", "대지위치", "용도별 연면적"];

test("빈 입력이면 필수 라벨 4종 전부 missing", () => {
  const { ok, missing } = canCalculate({});
  assert.equal(ok, false);
  assert.deepEqual(missing, REQUIRED_LABELS);
});

test("전부 채우면 ok=true·missing 빈 배열", () => {
  const { ok, missing } = canCalculate({
    사업형태: "공공기관", 사업연도: "2026", 대지위치: "서울특별시",
    용도별연면적목록: [{ 용도: "업무시설(공공)", 연면적: 50000 }],
  });
  assert.equal(ok, true);
  assert.deepEqual(missing, []);
});

test("일부만 채우면 남은 라벨만 missing (부분 진행 표시 근거)", () => {
  const { missing } = canCalculate({ 사업형태: "공공기관", 사업연도: "2026" });
  assert.deepEqual(missing, ["대지위치", "용도별 연면적"]);
});

test("용도 행이 있어도 연면적 0이면 용도별 연면적은 미충족", () => {
  const { missing } = canCalculate({
    사업형태: "공공기관", 사업연도: "2026", 대지위치: "서울특별시",
    용도별연면적목록: [{ 용도: "업무시설(공공)", 연면적: 0 }],
  });
  assert.deepEqual(missing, ["용도별 연면적"]);
});

test("missing 라벨은 항상 REQUIRED_LABELS의 부분집합", () => {
  const cases = [{}, { 사업형태: "공공기관" }, { 대지위치: "서울특별시" },
    { 용도별연면적목록: [{ 용도: "판매 및 영업시설", 연면적: 10 }] }];
  for (const c of cases) {
    for (const m of canCalculate(c).missing) assert.ok(REQUIRED_LABELS.includes(m), m);
  }
});
```

- [ ] **Step 2: 실행**

Run: `node --test tests/requiredLabels.test.mjs`
Expected: PASS (5 tests) — 기존 `canCalculate`가 이미 이 계약을 만족한다. **하나라도 실패하면 라벨이 계획과 다르다는 뜻이므로 구현을 바꾸지 말고 실패 내용을 그대로 보고**하라(Task 2의 라벨을 실제 값에 맞춰야 함)

- [ ] **Step 3: 전체 테스트 + 커밋**

Run: `npm test` → 79/79 PASS

```bash
git add tests/requiredLabels.test.mjs
git commit -m "test: 필수 항목 라벨 계약 고정 — 진행 상황판 의존 보호"
```

---

### Task 2: 진행 상황판 컴포넌트

**Files:**
- Create: `components/info/RequiredChecklist.jsx`, `components/info/requiredChecklist.css`

**Interfaces:**
- Consumes: `canCalculate`·`calc규모등급` (lib/calcModel.js), `Card` (components/ui/Card.jsx)
- Produces: `<RequiredChecklist input1={input1} />` — Task 3이 사용

- [ ] **Step 1: 컴포넌트 작성**

```jsx
// components/info/RequiredChecklist.jsx — 계산 필수 항목 충족 현황 (판정은 canCalculate 재사용)
"use client";
import Card from "@/components/ui/Card";
import { canCalculate, calc규모등급 } from "@/lib/calcModel";
import "./requiredChecklist.css";

const REQUIRED = ["사업형태", "사업연도", "대지위치", "용도별 연면적"];

export default function RequiredChecklist({ input1 }) {
  const { ok, missing } = canCalculate(input1 ?? {});
  const done = REQUIRED.length - missing.length;
  const 세대수미선택 = (input1?.용도별연면적목록 ?? []).some(r => r.용도 === "공동주택")
    && !calc규모등급(input1?.용도별연면적목록 ?? []);

  return (
    <Card inner className="rq">
      {ok ? (
        <p className="rq__done">✓ 필수 항목을 모두 입력했습니다 — ② 검토 계산에서 결과를 확인하세요.</p>
      ) : (
        <>
          <p className="rq__head">
            계산에 필수인 항목 <span className="mono">{done}/{REQUIRED.length} 입력됨</span>
          </p>
          <ul className="rq__list">
            {REQUIRED.map(label => {
              const filled = !missing.includes(label);
              return (
                <li key={label} className={filled ? "rq__item rq__item--done" : "rq__item"}>
                  <span aria-hidden="true">{filled ? "✓" : "○"}</span>
                  <span>{label}</span>
                  <span className="visually-hidden">{filled ? "입력됨" : "미입력"}</span>
                </li>
              );
            })}
          </ul>
        </>
      )}
      {세대수미선택 && (
        <p className="rq__note">공동주택 세대 수를 선택하면 규모등급이 판정됩니다.</p>
      )}
    </Card>
  );
}
```

- [ ] **Step 2: CSS 작성**

```css
/* components/info/requiredChecklist.css — 필수 항목 진행 상황판 */
.rq .card__body { display: flex; flex-direction: column; gap: var(--sp-2); }
.rq__head { font-size: var(--fs-13); font-weight: 700; color: var(--text-secondary); }
.rq__head .mono { margin-inline-start: var(--sp-2); color: var(--text-primary); }
.rq__list { list-style: none; display: flex; flex-wrap: wrap; gap: var(--sp-2) var(--sp-5); }
.rq__item { display: inline-flex; align-items: center; gap: var(--sp-1); font-size: var(--fs-13); color: var(--text-hint); }
.rq__item--done { color: var(--text-primary); }
.rq__item--done span[aria-hidden] { color: var(--color-pass); }
.rq__done { font-size: var(--fs-13); color: var(--color-pass); font-weight: 700; }
.rq__note { font-size: var(--fs-12); color: var(--accent-warm); }
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → 79/79 PASS

```bash
git add components/info/RequiredChecklist.jsx components/info/requiredChecklist.css
git commit -m "feat: 필수 항목 진행 상황판 컴포넌트 — 충족 여부·세대수 안내"
```

---

### Task 3: 사업정보 화면에 배치

**Files:**
- Modify: `app/project/[id]/info/page.js`

**Interfaces:**
- Consumes: Task 2의 `RequiredChecklist`

- [ ] **Step 1: 임포트 추가**

```js
import RequiredChecklist from "@/components/info/RequiredChecklist";
```

- [ ] **Step 2: 첫 카드로 렌더** — `return (`의 `<div className="info">` 바로 다음 줄에 삽입

```jsx
      <RequiredChecklist input1={input1} />
```

`<Card title="① 사업정보">` 이하 기존 마크업·핸들러는 **일절 변경하지 않는다**.

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → 79/79 PASS

```bash
git add "app/project/[id]/info/page.js"
git commit -m "feat: 사업정보 최상단에 진행 상황판 배치"
```

---

### Task 4: E2E 게이트 (컨트롤러 실행)

**Files:** 변경 없음

- [ ] **Step 1: 푸시** — `git push -u origin feature/info-checklist`, 프리뷰 빌드 대기. 도메인은 `gh api repos/jlaw080-ops/Renewable-Proposal/commits/<sha>/check-runs --jq '.check_runs[].output.summary'`에서 추출
- [ ] **Step 2: 기존 E2E** — `export $(grep VERCEL_AUTOMATION_BYPASS_SECRET .env.local) && node verify/e2e_redesign.mjs <프리뷰URL> verify/e2e_out` → PASS
- [ ] **Step 3: 3상태 실측** — 빈 프로젝트(0/4 표시) → 사업형태·사업연도만 입력(2/4, 남은 2개 ○) → 전부 입력(축약 문구) / 공동주택 행 추가 시 세대수 안내 표시
- [ ] **Step 4: 스크린샷 육안** — 1440·375px
- [ ] **Step 5: 보고** — 게이트 결과 정리. main 머지는 사용자 승인 후

## Self-Review 기록

- 스펙 §1 표시 내용→Task 2, §2 판정 근거→Task 1·2, §3 구현 방식→Task 2·3, §4 게이트→Task 1·4. 갭 없음
- 타입 일관성: `REQUIRED` 라벨 배열(Task 2)이 Task 1 테스트의 `REQUIRED_LABELS`와 동일 문자열 — 테스트가 이 일치를 강제
- 신규 순수 로직 없음(기존 `canCalculate` 재사용) → 테스트는 라벨 계약만 검증. 스펙 §2의 "중복 정의 금지"와 일치
