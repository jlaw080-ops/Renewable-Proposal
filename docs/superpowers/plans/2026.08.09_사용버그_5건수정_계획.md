# 실사용 버그 5건 수정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 실사용에서 보고된 5건을 수정한다 — ① 모달 타이핑 시 포커스 튐 ② 토지정보 오류 은폐 ③ 시나리오 행 오버플로 ④ 예상에너지사용량 위치 ⑤ 조합 카드 높이 불균일.

**Architecture:** ①은 Modal의 effect 의존성·초기 포커스 대상 수정(회귀). ②는 도로명주소 API 오류를 그대로 노출(키 재발급은 사용자 조치). ③⑤는 CSS, ④는 calc 페이지 마크업 재배치.

**Tech Stack:** Next.js(App Router, JS), Vanilla CSS(기존 토큰), `node --test`, Playwright(msedge).

## 진단 근거 (컨트롤러 실측)

- ①: `Modal.jsx:43` deps `[open, onClose]` + 호출부 인라인 `onClose={() => …}` → 렌더마다 effect 재실행 → `Modal.jsx:17`이 `[autofocus]`를 못 찾고(React `autoFocus`는 DOM 속성 미생성) 첫 포커스 대상 = 헤더 닫기 버튼으로 이동
- ②: 프로덕션 `/api/landinfo` 4개 주소 형식 전부 `{"juso":null,"land":null}`. 도로명주소 API 직접 호출 결과 `errorCode:"E0001", errorMessage:"승인되지 않은 KEY 입니다."` — `route.js:22`가 이 오류를 후보 없음과 동일 처리
- ③: `scenarioEditor.css:7` `grid-template-columns: 1fr 1.4fr 1fr auto` (minmax 없음)
- ⑤: `.opt__grid`가 `repeat(auto-fill, minmax(320px, 1fr))` — 행별 높이만 정렬되고 카드 내부 밑단이 불균일

## Global Constraints

- `legacy/`·`engine/`·`public/` 무수정, 데이터 스키마 무변경
- 신규 색·크기 리터럴 금지 — 기존 토큰만
- 테스트: `npm test` = `node --test "tests/*.test.mjs"`, 현재 79/79 통과 상태에서 시작
- 로컬 `npm run build`/`dev` 금지 — 검증은 Vercel Preview
- 브랜치 `fix/usability-batch`. 커밋 `<type>: <한국어 설명>`. main 머지는 계획 밖(사용자 승인 후)
- 기존 인터페이스(변경 금지): Modal props `{open, onClose, title, footer, wide, children}` — 5개 호출부가 의존 · `pickJusoCandidate`/`bldrgstParams`/`aggregateLandInfo` (lib/landinfo.js) · `Output1Table({output1})` · `Output2Panel({alt})` · `VerdictSticky({output2})`

---

### Task 1: 모달 포커스 튐 수정 (①)

**Files:**
- Modify: `components/ui/Modal.jsx`

**Interfaces:**
- Props 불변. 동작 변경: 열릴 때 **본문 첫 포커스 대상**으로 이동하고, 이후 리렌더로 재실행되지 않는다

- [ ] **Step 1: Modal.jsx의 effect 교체**

`useEffect` 전체와 그 위 ref 선언부를 다음으로 교체 (import 줄에 `useCallback` 불필요, `useRef` 이미 사용 중):

```jsx
  const titleId = useId();
  const backdropRef = useRef(null);
  const dialogRef = useRef(null);
  const bodyRef = useRef(null);
  const onCloseRef = useRef(onClose);
  onCloseRef.current = onClose;          // 최신 핸들러를 ref로 — effect 의존성에서 제외해 리렌더 재실행 방지

  useEffect(() => {
    if (!open) return;
    const trigger = document.activeElement;                 // 닫힐 때 복귀할 트리거
    const dialog = dialogRef.current;
    // 본문 우선: 헤더 닫기 버튼이 첫 포커스가 되면 입력 대신 닫기에 포커스가 간다
    const first = bodyRef.current?.querySelector(FOCUSABLE)
      ?? dialog.querySelector(FOCUSABLE)
      ?? dialog;
    first.focus();

    // 배경 비활성화: body 직계 중 모달 백드롭 제외 전부 inert
    const siblings = [...document.body.children].filter(el => el !== backdropRef.current);
    siblings.forEach(el => el.setAttribute("inert", ""));

    const onKey = e => {
      if (e.key === "Escape") { onCloseRef.current(); return; }
      if (e.key !== "Tab") return;
      const items = [...dialog.querySelectorAll(FOCUSABLE)];
      if (items.length === 0) { e.preventDefault(); return; }
      const firstEl = items[0], lastEl = items[items.length - 1];
      if (e.shiftKey && document.activeElement === firstEl) { e.preventDefault(); lastEl.focus(); }
      else if (!e.shiftKey && (document.activeElement === lastEl || !dialog.contains(document.activeElement))) {
        e.preventDefault(); firstEl.focus();
      }
    };
    document.addEventListener("keydown", onKey);
    document.body.style.overflow = "hidden";
    return () => {
      document.removeEventListener("keydown", onKey);
      document.body.style.overflow = "";
      siblings.forEach(el => el.removeAttribute("inert"));
      trigger?.focus?.();
    };
  }, [open]);   // onClose는 ref 경유 — 의존성에 넣으면 렌더마다 재실행되어 포커스가 튄다
```

- [ ] **Step 2: 본문에 ref 연결** — `<div className="modal__body">{children}</div>` 를 다음으로 교체

```jsx
        <div className="modal__body" ref={bodyRef}>{children}</div>
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → 79/79 PASS (동작 검증은 Task 5 E2E)

```bash
git add components/ui/Modal.jsx
git commit -m "fix: 모달 입력 중 포커스가 닫기 버튼으로 튀는 회귀 — onClose ref화·본문 우선 포커스"
```

---

### Task 2: 시나리오 행 오버플로 + 조합 카드 높이 (③⑤)

**Files:**
- Modify: `components/calc/scenarioEditor.css`, `app/project/[id]/optimize/optimize.css`, `components/optimize/comboCard.css`

- [ ] **Step 1: scenarioEditor.css — 그리드 열에 minmax(0,…) 적용**

`.se__row` 규칙을 다음으로 교체:

```css
/* minmax(0,…): select의 긴 옵션 텍스트가 고유 최소폭을 만들어 우측 칸을 밀어내는 것 방지 */
.se__row { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1.4fr) minmax(0, 1fr) auto; gap: var(--sp-3); align-items: end; }
```

- [ ] **Step 2: optimize.css — 카드 행 높이 균일화**

`.opt__grid` 규칙을 다음으로 교체 (기존 `@media (max-width: 767px)`의 `.opt__grid { grid-template-columns: 1fr; }`는 그대로 둔다):

```css
.opt__grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: var(--sp-4); grid-auto-rows: 1fr; }
```

- [ ] **Step 3: comboCard.css — 카드가 슬롯을 채우고 메모를 밑단에 고정**

파일 끝(모바일 미디어 쿼리 앞)에 추가:

```css
/* 카드가 그리드 슬롯 전체를 채우고, 메모는 항상 카드 하단에 정렬 (높이 통일) */
.cc { height: 100%; }
.cc__memo { margin-top: auto; }
```

- [ ] **Step 4: 테스트 + 커밋**

Run: `npm test` → 79/79 PASS

```bash
git add components/calc/scenarioEditor.css "app/project/[id]/optimize/optimize.css" components/optimize/comboCard.css
git commit -m "fix: 시나리오 행 오버플로(minmax 0)·조합 카드 높이 균일화(메모 하단 정렬)"
```

---

### Task 3: 예상에너지사용량을 최상단 전폭으로 (④)

**Files:**
- Modify: `app/project/[id]/calc/page.js`

**Interfaces:**
- 계산 훅·상태·엔진 호출 무변경. `return (…)` 내부 배치만 변경

- [ ] **Step 1: Output1 카드를 2열 영역 밖·최상단으로 이동**

현재 구조는 `상태 안내 → VerdictSticky → .calc__cols(좌: 시나리오 / 우: Output2 + Output1)`이다. 이를 다음 순서로 바꾼다 — **Output1을 전폭으로 올리고, 우측 열에는 Output2만 남긴다**:

```jsx
  return (
    <div className="calc">
      {(notices.length > 0 || loading) && (
        <Card title="② 검토 계산" className="calc__full">
          {notices.map(n => <p key={n} className="calc__notice" role="status">{n}</p>)}
          {loading && <p className="calc__hint">계산 엔진 로딩 중…</p>}
        </Card>
      )}

      <Card title="② 예상에너지사용량 (Output 1) — 모든 시나리오 공통 전제" actions={
        <span className="calc__cat" title="주거: 세대수 기준 / 비주거: 용도별 연면적 합계 기준으로 자동 결정됩니다">
          규모등급 <Badge tone={카테고리 ? "brand" : "na"}>{카테고리 || "미판정"}</Badge>
        </span>
      }>
        {result
          ? <Output1Table output1={result.output1} />
          : <p className="calc__hint">① 사업정보 입력이 완료되면 용도별 예상에너지사용량이 여기 표시됩니다.</p>}
      </Card>

      {result && <VerdictSticky output2={result.output2} />}

      <div className="calc__cols">
        <div className="calc__col calc__col--input">
          <Card title="시나리오 (ALT)">
            {lib && input2
              ? <ScenarioEditor scenarios={input2.scenarios} lib={lib} onChange={applyInput2} />
              : <p className="calc__hint">계산 엔진 로딩 중…</p>}
          </Card>
        </div>

        <div className="calc__col calc__col--result">
          <Card title="신재생에너지 설치비율 (Output 2)">
            {result
              ? result.output2.map(alt => <Output2Panel key={alt.id} alt={alt} />)
              : <p className="calc__hint">시나리오 구성과 계산이 완료되면 의무비율 대비 판정 결과가 여기 표시됩니다.</p>}
          </Card>
        </div>
      </div>
    </div>
  );
}
```

주의: Output1 카드는 이제 판정 고정 바보다 **위**에 있다. 고정 바는 `position: sticky; top: 0`이라 스크롤 시 Output1을 지나쳐 계속 상단에 붙는다(의도된 동작).

- [ ] **Step 2: 테스트 + 커밋**

Run: `npm test` → 79/79 PASS

```bash
git add "app/project/[id]/calc/page.js"
git commit -m "refactor: 예상에너지사용량을 최상단 전폭으로 — 모든 시나리오의 공통 전제임을 반영"
```

---

### Task 4: 토지정보 API 오류 노출 (②)

**Files:**
- Modify: `app/api/landinfo/route.js`, `components/map/LandInfoCard.jsx`

**Interfaces:**
- 응답 계약 확장: 오류 시 `{error, code?}` + 4xx/5xx. 성공 계약(`{juso, land}`)은 불변

- [ ] **Step 1: route.js — 도로명주소 API의 errorCode를 오류로 승격**

`const cand = pickJusoCandidate(await jusoResp.json());` 부터 `if (!cand) …` 까지를 다음으로 교체:

```js
    const jusoJson = await jusoResp.json();
    // 도로명주소 API는 키 오류·쿼터 초과도 HTTP 200 + errorCode로 반환한다 — 후보 없음과 구분해 노출
    const common = jusoJson?.results?.common ?? {};
    if (common.errorCode && common.errorCode !== "0") {
      return NextResponse.json(
        { error: `도로명주소 API 오류: ${common.errorMessage ?? "알 수 없는 오류"}`, code: common.errorCode },
        { status: 502 });
    }
    const cand = pickJusoCandidate(jusoJson);
    if (!cand) return NextResponse.json({ juso: null, land: null });
```

- [ ] **Step 2: LandInfoCard.jsx — 오류 문구에 관리자 조치 힌트 추가**

`if (state.status === "error")` 반환 줄을 다음으로 교체:

```jsx
  if (state.status === "error") {
    return (
      <div className="lic">
        <p className="lp__notice" role="status">토지정보 조회 오류: {state.message}</p>
        <p className="lp__fallback">주소 조회 서비스 문제일 수 있습니다 — 아래 대지 현황에 직접 입력하세요.</p>
      </div>
    );
  }
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → 79/79 PASS

```bash
git add app/api/landinfo/route.js components/map/LandInfoCard.jsx
git commit -m "fix: 도로명주소 API 키 오류를 조회 결과 없음으로 삼키던 문제 — 원인 노출"
```

---

### Task 5: E2E 게이트 + 모달 포커스 회귀 테스트 추가 (컨트롤러 실행)

**Files:**
- Modify: `verify/e2e_redesign.mjs`

- [ ] **Step 1: E2E에 타이핑 중 포커스 유지 검사 추가** — 모달 포커스 트랩 블록의 `await trigger.click();` 다음에 삽입

```js
  // 회귀 방지: 입력 중 포커스가 닫기 버튼으로 튀지 않아야 한다 (Modal effect 의존성 결함)
  await page.keyboard.type("포커스테스트");
  const focusKept = await page.evaluate(() => document.activeElement?.tagName === "INPUT");
  if (!focusKept) bad("모달 입력 중 포커스가 입력칸을 벗어남");
  const typed = await page.evaluate(() => document.querySelector(".modal__body input")?.value ?? "");
  if (typed !== "포커스테스트") bad(`모달 입력값 불일치: "${typed}"`);
```

- [ ] **Step 2: 푸시·프리뷰 대기·E2E 실행** — `git push -u origin fix/usability-batch` → 프리뷰 도메인 추출 → `node verify/e2e_redesign.mjs <URL> verify/e2e_out` → `PASS`
- [ ] **Step 3: 실측** — ③ 시나리오 행: 에너지원 선택 후 형식 select가 넓어져도 적용용량이 잘리지 않는지(1024·1280·1440px) / ④ Output1이 최상단 전폭인지 / ⑤ 카드 높이 균일한지 / ② 오류 문구가 원인을 표시하는지
- [ ] **Step 4: 커밋·보고** — E2E 스크립트 커밋, 결과 정리. main 머지는 사용자 승인 후

## Self-Review 기록

- 5건 각각 Task 1·2(③⑤)·3·4에 매핑, 검증은 Task 5. 갭 없음
- ①의 근본 원인은 effect 의존성이므로 `[open]`으로 축소 + `onCloseRef`로 최신 핸들러 유지 — Escape·배경 클릭 동작은 유지된다(배경 클릭은 prop을 직접 쓰므로 영향 없음)
- ④에서 Output1이 고정 바 위로 가지만 고정 바는 `top: 0` sticky라 스크롤 시 계속 상단 유지 — 기능 손실 없음
- ②의 키 재발급은 사용자 조치(계획 범위 밖). 코드는 원인을 드러내는 것까지만 담당
