# Phase 2 — 사업정보 + 지도 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 마스터플랜: `docs/superpowers/plans/2026.07.26_전면개편_마스터플랜.md` (Global Constraints 포함)

**Goal:** 사업정보 입력 폼 + 3경로 지역 확정(드롭다운/주소검색/지도클릭) + 토지정보 조회·연면적 자동 채움을 구현해 워크스페이스 ① 단계를 완성한다.

**Architecture:** 지역 판정·토지정보 파싱은 순수 함수 라이브러리(`lib/regionResolver.js`·`lib/landinfo.js`)로 분리해 node:test로 검증하고, API 라우트 3종(geocode 포팅·reverse-geocode·landinfo)은 얇은 오케스트레이터로 유지한다. UI는 Phase 1 공통 컴포넌트만 사용하며 `projectStore`의 `data.input1` 봉투에 저장한다.

**Tech Stack:** Next.js Route Handlers·네이버 지도 JS SDK(v3)·네이버 Geocoding/Reverse-Geocoding(NCP)·juso.go.kr 주소 API·data.go.kr 건축물대장 표제부 API·node:test

## 선행 조건 (사용자 액션 — Task 5 전까지)

- [x] Vercel 환경변수: `DATA_GO_KR_KEY`(디코딩 키)·`JUSO_CONFIRM_KEY` 등록 완료 (2026-07-27, 사용자 확인)
- [ ] Vercel 환경변수: `NEXT_PUBLIC_NCP_CLIENT_ID` 추가 — 기존 `NCP_CLIENT_ID`와 **같은 값** (지도 SDK는 브라우저에서 키 필요, 도메인 제한으로 보호)
- [ ] NCP 콘솔(console.ncloud.com > Maps): **Web Dynamic Map** 서비스 활성화 + 웹 서비스 URL에 `https://renewable-proposal-git-revamp-next-jlaw080-4229s-projects.vercel.app`, `https://renewable-proposal.vercel.app`, `http://localhost:3000` 등록
- [ ] 로컬 `.env.local`에 `NCP_CLIENT_ID`·`NCP_CLIENT_SECRET`·`JUSO_CONFIRM_KEY`·`NEXT_PUBLIC_NCP_CLIENT_ID` 추가 (로컬 통합 확인용 — 미등록 시 해당 확인은 Vercel Preview로 대체, DATA_GO_KR_KEY는 이미 있음)

미완료여도 Task 1~4는 진행 가능. Task 5~7의 지도·API 통합 확인만 영향받는다.

## Global Constraints (마스터플랜 전체 적용 + Phase 2 추가)

- **엔진 무수정**: `engine/` `public/*` 수정 금지. `lib/engineScripts.js`·`lib/useEngineReady.js`·`app/engine-check/`·`legacy/`도 수정 금지
- **계산 동일성**: 최종 Task에서 verify 스냅샷 일치 필수
- **브랜치**: `revamp/next`만. main 푸시 금지
- **`git add -A`·`git add .` 절대 금지** — 명시 경로만 add
- JavaScript(TS 금지), plain CSS + 토큰만(리터럴 색·크기 금지), 파일 200~400줄 지향, UI 문구 한국어
- **API 키 하드코딩 금지** — 서버 키는 `process.env.*`(라우트 핸들러에서만), 브라우저 노출은 `NEXT_PUBLIC_NCP_CLIENT_ID` 하나뿐
- API 라우트는 실패 시에도 JSON으로 응답(`{ error }`), 조회 결과 없음은 에러가 아니라 정상 응답(`{ land: null }`)
- **환경 교훈**: 로컬 build/dev 매우 느림(Dropbox) — 통합 검증은 푸시 → Vercel Preview + 로컬 Playwright(`node_modules/playwright`, msedge 채널) 방식 사용. `npm run dev` 실행 시 `.next` 프로덕션 빌드가 덮어써짐. 작업 후 잔류 node 프로세스 정리 필수

## 파일 구조 (이번 Phase 산출물)

```
lib/regionResolver.js            시도→의무비율 지역 판정 (순수 함수, legacy addressSearch에서 이식)
lib/landinfo.js                  juso 응답 파싱·건축물대장 파라미터·토지정보 집계 (순수 함수)
lib/formOptions.js               폼 옵션 상수 (사업형태·연도·용도 19종)
tests/regionResolver.test.mjs    지역 판정 단위테스트
tests/landinfo.test.mjs          토지정보 파싱 단위테스트
app/api/geocode/route.js         네이버 Geocoding 프록시 (legacy/api/geocode.js 포팅)
app/api/reverse-geocode/route.js 네이버 Reverse Geocoding 프록시 (신규)
app/api/landinfo/route.js        juso→건축물대장 토지정보 (신규)
lib/useNaverMap.js               네이버 지도 SDK 로더 훅 (멱등)
components/map/LocationPicker.jsx|locationPicker.css   지도+주소검색+클릭 위치 선택
components/map/LandInfoCard.jsx  토지정보 카드 ("현황 건축물 기준")
app/project/[id]/info/page.js    ① 사업정보 화면 전면 재작성 (폼+지도+토지정보 조립)
app/project/[id]/info/info.css
```

## 데이터 계약 (Phase 3~5가 의존)

`project.data.input1` (엔진 `runCalculation`의 input1과 동일 형태 — `public/fixtures/Input1-사업정보.json` 기준):

```js
{
  사업형태: "민간" | "공공" | "",
  사업연도: "2024"~"2030" | "",       // 문자열
  대지위치: REGION_OPTIONS 중 하나 | "",
  대지면적: number | "", 건축면적: number | "", 연면적: number | "",
  건폐율: number | "", 용적률: number | "",
  용도별연면적목록: [{ 용도: string, 연면적: number | "" }],
  // Phase 2 부가 (엔진 미사용, UI 상태)
  위치정보: { address: string, lat: number, lng: number } | undefined
}
```

숫자 필드는 입력 시 `Number()` 변환해 저장하고 빈 입력은 `""`. Phase 3 계산 진입 시 필수값 검증 책임은 Phase 3.

---

### Task 1: lib/regionResolver.js — 지역 판정 (TDD)

**Files:**
- Create: `lib/regionResolver.js`, `tests/regionResolver.test.mjs`

**Interfaces:**
- Produces:
  - `REGION_OPTIONS: string[]` — 의무비율 드롭다운 17개 값(legacy sel-대지위치와 동일): `["강원 영동","강원 영서","경기도","경상남도","경상북도","광주광역시","대구광역시","대전광역시","부산광역시","서울특별시","울산광역시","인천광역시","전라남도","전라북도","제주특별자치도","충청남도·세종특별자치시","충청북도"]`
  - `resolveRegion(sido: string, sigungu?: string): string | null` — 시도명(축약/정식 모두)→지역, 강원은 시군구로 영동/영서 구분, 매핑 불가 null
  - `regionFromNaverElements(addressElements: Array): string | null` — 네이버 Geocoding `addressElements`(types SIDO/SIGUGUN)에서 추출
  - `regionFromReverseGeocode(results: Array): { region: string|null, address: string }` — Reverse Geocoding `results[].region.area1~area4.name` 사용

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/regionResolver.test.mjs` (전문)

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import {
  REGION_OPTIONS, resolveRegion, regionFromNaverElements, regionFromReverseGeocode,
} from "../lib/regionResolver.js";

test("REGION_OPTIONS는 의무비율 드롭다운 17개와 일치한다", () => {
  assert.equal(REGION_OPTIONS.length, 17);
  assert.ok(REGION_OPTIONS.includes("충청남도·세종특별자치시"));
  assert.ok(REGION_OPTIONS.includes("강원 영동") && REGION_OPTIONS.includes("강원 영서"));
});

test("시도 정식·축약 명칭 모두 매핑된다", () => {
  assert.equal(resolveRegion("서울특별시"), "서울특별시");
  assert.equal(resolveRegion("서울"), "서울특별시");
  assert.equal(resolveRegion("경기도"), "경기도");
  assert.equal(resolveRegion("전북특별자치도"), "전라북도");
});

test("강원은 시군구로 영동/영서를 구분한다", () => {
  assert.equal(resolveRegion("강원특별자치도", "강릉시"), "강원 영동");
  assert.equal(resolveRegion("강원특별자치도", "속초시"), "강원 영동");
  assert.equal(resolveRegion("강원특별자치도", "춘천시"), "강원 영서");
  assert.equal(resolveRegion("강원도", ""), "강원 영서"); // 시군구 불명 시 영서(legacy 동작 유지)
});

test("세종·충남은 통합 지역으로 매핑된다", () => {
  assert.equal(resolveRegion("세종특별자치시"), "충청남도·세종특별자치시");
  assert.equal(resolveRegion("충청남도"), "충청남도·세종특별자치시");
});

test("매핑 불가 시도는 null", () => {
  assert.equal(resolveRegion("도쿄도"), null);
  assert.equal(resolveRegion(""), null);
});

test("네이버 Geocoding addressElements에서 지역을 추출한다", () => {
  const elements = [
    { types: ["SIDO"], longName: "강원특별자치도" },
    { types: ["SIGUGUN"], longName: "동해시" },
  ];
  assert.equal(regionFromNaverElements(elements), "강원 영동");
  assert.equal(regionFromNaverElements([]), null);
});

test("Reverse Geocoding results에서 지역·주소를 추출한다", () => {
  const results = [{
    name: "legalcode",
    region: {
      area1: { name: "서울특별시" }, area2: { name: "강남구" },
      area3: { name: "역삼동" }, area4: { name: "" },
    },
  }];
  const r = regionFromReverseGeocode(results);
  assert.equal(r.region, "서울특별시");
  assert.equal(r.address, "서울특별시 강남구 역삼동");
  assert.deepEqual(regionFromReverseGeocode([]), { region: null, address: "" });
});
```

- [ ] **Step 2: 실행해 실패 확인** — Run: `npm test` / Expected: FAIL (모듈 없음)

- [ ] **Step 3: 최소 구현** — `lib/regionResolver.js` (전문 — 매핑 값은 legacy `public/map/addressSearch.js` 검증본 이식)

```js
// 시도/시군구 → 의무비율 지역 판정 — legacy map/addressSearch.js의 검증된 매핑 이식 (순수 함수)
export const REGION_OPTIONS = [
  "강원 영동", "강원 영서", "경기도", "경상남도", "경상북도",
  "광주광역시", "대구광역시", "대전광역시", "부산광역시", "서울특별시",
  "울산광역시", "인천광역시", "전라남도", "전라북도", "제주특별자치도",
  "충청남도·세종특별자치시", "충청북도",
];

const 시도매핑 = {
  "서울": "서울특별시", "서울특별시": "서울특별시", "서울시": "서울특별시",
  "부산": "부산광역시", "부산광역시": "부산광역시",
  "대구": "대구광역시", "대구광역시": "대구광역시",
  "인천": "인천광역시", "인천광역시": "인천광역시",
  "광주": "광주광역시", "광주광역시": "광주광역시",
  "대전": "대전광역시", "대전광역시": "대전광역시",
  "울산": "울산광역시", "울산광역시": "울산광역시",
  "세종": "충청남도·세종특별자치시", "세종특별자치시": "충청남도·세종특별자치시",
  "경기": "경기도", "경기도": "경기도",
  "강원": "강원 영서", "강원도": "강원 영서", "강원특별자치도": "강원 영서",
  "충북": "충청북도", "충청북도": "충청북도",
  "충남": "충청남도·세종특별자치시", "충청남도": "충청남도·세종특별자치시",
  "전북": "전라북도", "전라북도": "전라북도", "전북특별자치도": "전라북도",
  "전남": "전라남도", "전라남도": "전라남도",
  "경북": "경상북도", "경상북도": "경상북도",
  "경남": "경상남도", "경상남도": "경상남도",
  "제주": "제주특별자치도", "제주특별자치도": "제주특별자치도", "제주도": "제주특별자치도",
};

const 강원영동_시군 = ["강릉시", "동해시", "삼척시", "속초시", "양양군", "고성군", "태백시"];

export function resolveRegion(sido, sigungu = "") {
  if (!sido) return null;
  if (sido.includes("강원")) {
    return 강원영동_시군.some(s => sigungu.includes(s)) ? "강원 영동" : "강원 영서";
  }
  if (sido.includes("세종")) return "충청남도·세종특별자치시";
  return 시도매핑[sido] ?? null;
}

export function regionFromNaverElements(addressElements = []) {
  let sido = "", sigungu = "";
  for (const el of addressElements) {
    const types = el.types ?? [];
    if (types.includes("SIDO")) sido = el.longName;
    if (types.includes("SIGUGUN")) sigungu = el.longName;
  }
  return resolveRegion(sido, sigungu);
}

export function regionFromReverseGeocode(results = []) {
  const r = results[0];
  if (!r?.region) return { region: null, address: "" };
  const parts = [r.region.area1?.name, r.region.area2?.name, r.region.area3?.name, r.region.area4?.name]
    .filter(Boolean).filter(s => s.trim() !== "");
  return {
    region: resolveRegion(r.region.area1?.name ?? "", r.region.area2?.name ?? ""),
    address: parts.join(" "),
  };
}
```

- [ ] **Step 4: 테스트 통과 확인** — Run: `npm test` / Expected: 기존 8건 + 신규 7건 전부 PASS, 스킵 0

- [ ] **Step 5: 커밋**

```bash
git add lib/regionResolver.js tests/regionResolver.test.mjs
git commit -m "feat: regionResolver — 시도→의무비율 지역 판정 (legacy 매핑 이식, TDD 7건)"
```

---

### Task 2: lib/landinfo.js — 토지정보 파싱 (TDD)

**Files:**
- Create: `lib/landinfo.js`, `tests/landinfo.test.mjs`

**Interfaces:**
- Produces (Task 3의 landinfo 라우트가 사용):
  - `pickJusoCandidate(jusoJson): { admCd, bun, ji, mtYn, roadAddr, jibunAddr } | null` — juso.go.kr 응답의 첫 후보
  - `bldrgstParams(cand): { sigunguCd, bjdongCd, bun, ji, platGbCd }` — 건축물대장 표제부 API 파라미터 (bun/ji 4자리 0패딩)
  - `aggregateLandInfo(bldJson): { 대지면적, 건축면적, 연면적, 건폐율, 용적률, 동수 } | null` — 표제부 목록 집계: 연면적·건축면적은 전 동 합산, 대지면적·건폐율·용적률은 최댓값(동별 0 기재 관행 대응). 항목 없으면 null

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/landinfo.test.mjs` (전문)

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { pickJusoCandidate, bldrgstParams, aggregateLandInfo } from "../lib/landinfo.js";

const jusoOk = {
  results: {
    common: { errorCode: "0" },
    juso: [{
      admCd: "1168010300", lnbrMnnm: "12", lnbrSlno: "0", mtYn: "0",
      roadAddr: "서울특별시 강남구 개포로109길 21", jibunAddr: "서울특별시 강남구 개포동 12",
    }],
  },
};

test("juso 응답에서 첫 후보를 추출한다", () => {
  const c = pickJusoCandidate(jusoOk);
  assert.equal(c.admCd, "1168010300");
  assert.equal(c.roadAddr, "서울특별시 강남구 개포로109길 21");
});

test("juso 결과 없음·오류는 null", () => {
  assert.equal(pickJusoCandidate({ results: { common: { errorCode: "0" }, juso: [] } }), null);
  assert.equal(pickJusoCandidate({ results: { common: { errorCode: "E0001" }, juso: null } }), null);
  assert.equal(pickJusoCandidate(null), null);
});

test("건축물대장 파라미터를 만든다 (bun/ji 4자리 패딩, 산=platGbCd 1)", () => {
  const p = bldrgstParams(pickJusoCandidate(jusoOk));
  assert.deepEqual(p, { sigunguCd: "11680", bjdongCd: "10300", bun: "0012", ji: "0000", platGbCd: "0" });
  const mountain = bldrgstParams({ admCd: "1168010300", lnbrMnnm: "5", lnbrSlno: "3", mtYn: "1" });
  assert.equal(mountain.platGbCd, "1");
  assert.equal(mountain.bun, "0005");
  assert.equal(mountain.ji, "0003");
});

test("표제부 목록을 집계한다 — 연면적·건축면적 합산, 대지면적·건폐율·용적률 최댓값", () => {
  const bld = { response: { header: { resultCode: "00" }, body: { items: { item: [
    { platArea: 0, archArea: 592.93, totArea: 8969.43, bcRat: 15.96, vlRat: 201.85 },
    { platArea: 43158, archArea: 600.5, totArea: 9100.57, bcRat: 0, vlRat: 0 },
  ] } } } };
  const agg = aggregateLandInfo(bld);
  assert.equal(agg.연면적, 18070);            // 8969.43 + 9100.57
  assert.equal(agg.건축면적, 1193.43);        // 592.93 + 600.5
  assert.equal(agg.대지면적, 43158);
  assert.equal(agg.건폐율, 15.96);
  assert.equal(agg.용적률, 201.85);
  assert.equal(agg.동수, 2);
});

test("item이 단일 객체여도 배열로 처리한다", () => {
  const bld = { response: { header: { resultCode: "00" }, body: { items: { item:
    { platArea: 100, archArea: 50, totArea: 200, bcRat: 50, vlRat: 200 } } } } };
  assert.equal(aggregateLandInfo(bld).동수, 1);
});

test("결과 없음(나대지)·오류 코드는 null", () => {
  assert.equal(aggregateLandInfo({ response: { header: { resultCode: "00" }, body: { items: "" } } }), null);
  assert.equal(aggregateLandInfo({ response: { header: { resultCode: "03" }, body: {} } }), null);
  assert.equal(aggregateLandInfo(null), null);
});
```

- [ ] **Step 2: 실행해 실패 확인** — Run: `npm test` / Expected: 신규 테스트 FAIL

- [ ] **Step 3: 최소 구현** — `lib/landinfo.js` (전문)

```js
// juso.go.kr·건축물대장 응답 파싱 (순수 함수 — 라우트 핸들러에서 사용)
const round2 = n => Math.round(n * 100) / 100;

export function pickJusoCandidate(jusoJson) {
  const results = jusoJson?.results;
  if (!results || results.common?.errorCode !== "0") return null;
  const j = Array.isArray(results.juso) ? results.juso[0] : null;
  if (!j) return null;
  return { admCd: j.admCd, bun: j.lnbrMnnm, ji: j.lnbrSlno, mtYn: j.mtYn, roadAddr: j.roadAddr, jibunAddr: j.jibunAddr };
}

export function bldrgstParams(cand) {
  return {
    sigunguCd: cand.admCd.slice(0, 5),
    bjdongCd: cand.admCd.slice(5, 10),
    bun: String(cand.bun ?? "0").padStart(4, "0"),
    ji: String(cand.ji ?? "0").padStart(4, "0"),
    platGbCd: cand.mtYn === "1" ? "1" : "0",
  };
}

export function aggregateLandInfo(bldJson) {
  const resp = bldJson?.response;
  if (!resp || resp.header?.resultCode !== "00") return null;
  let items = resp.body?.items?.item;
  if (!items) return null;
  if (!Array.isArray(items)) items = [items];
  if (items.length === 0) return null;
  const num = v => (typeof v === "number" && Number.isFinite(v) ? v : Number(v) || 0);
  return {
    연면적: round2(items.reduce((s, it) => s + num(it.totArea), 0)),
    건축면적: round2(items.reduce((s, it) => s + num(it.archArea), 0)),
    대지면적: round2(Math.max(...items.map(it => num(it.platArea)))),
    건폐율: round2(Math.max(...items.map(it => num(it.bcRat)))),
    용적률: round2(Math.max(...items.map(it => num(it.vlRat)))),
    동수: items.length,
  };
}
```

- [ ] **Step 4: 테스트 통과 확인** — Run: `npm test` / Expected: 전체 PASS (기존 15건 + 신규 6건)

- [ ] **Step 5: 커밋**

```bash
git add lib/landinfo.js tests/landinfo.test.mjs
git commit -m "feat: landinfo 파서 — juso 후보·건축물대장 파라미터·표제부 집계 (TDD 6건)"
```

---

### Task 3: API 라우트 3종 — geocode 포팅 + reverse-geocode + landinfo

**Files:**
- Create: `app/api/geocode/route.js`, `app/api/reverse-geocode/route.js`, `app/api/landinfo/route.js`

**Interfaces:**
- Consumes: `lib/landinfo.js`(Task 2), 환경변수 `NCP_CLIENT_ID`·`NCP_CLIENT_SECRET`·`JUSO_CONFIRM_KEY`·`DATA_GO_KR_KEY`
- Produces (클라이언트 계약):
  - `GET /api/geocode?query=주소` → 네이버 Geocoding 원본 JSON (`{ addresses: [...] }`) — legacy와 동일 응답 형태
  - `GET /api/reverse-geocode?lat=..&lng=..` → 네이버 gc 원본 JSON (`{ results: [...] }`)
  - `GET /api/landinfo?address=주소` → `{ juso: { roadAddr, jibunAddr } | null, land: {대지면적,건축면적,연면적,건폐율,용적률,동수} | null }` — juso 미발견이면 `{ juso: null, land: null }` (200), 건물 없음(나대지)이면 `land: null` (200)
  - 오류: 키 미설정 500 `{ error: "..." }`, query 누락 400, 외부 API 실패는 상태코드 전달

- [ ] **Step 1: geocode 라우트** — `app/api/geocode/route.js` (legacy Edge Function을 Route Handler로 포팅 — CORS 헤더는 동일 출처라 제거)

```js
import { NextResponse } from "next/server";

export async function GET(request) {
  const clientId = process.env.NCP_CLIENT_ID;
  const clientSecret = process.env.NCP_CLIENT_SECRET;
  if (!clientId || !clientSecret) {
    return NextResponse.json({ error: "네이버 지도 API 키가 설정되지 않았습니다" }, { status: 500 });
  }
  const query = request.nextUrl.searchParams.get("query");
  if (!query) return NextResponse.json({ error: "query 파라미터가 필요합니다" }, { status: 400 });

  try {
    const url = "https://naveropenapi.apigw.ntruss.com/map-geocode/v2/geocode?query=" + encodeURIComponent(query);
    const resp = await fetch(url, {
      headers: { "X-NCP-APIGW-API-KEY-ID": clientId, "X-NCP-APIGW-API-KEY": clientSecret },
    });
    if (!resp.ok) {
      return NextResponse.json({ error: "네이버 API 오류", detail: await resp.text() }, { status: resp.status });
    }
    return NextResponse.json(await resp.json());
  } catch (err) {
    return NextResponse.json({ error: "프록시 오류", message: err.message }, { status: 500 });
  }
}
```

- [ ] **Step 2: reverse-geocode 라우트** — `app/api/reverse-geocode/route.js`

```js
import { NextResponse } from "next/server";

export async function GET(request) {
  const clientId = process.env.NCP_CLIENT_ID;
  const clientSecret = process.env.NCP_CLIENT_SECRET;
  if (!clientId || !clientSecret) {
    return NextResponse.json({ error: "네이버 지도 API 키가 설정되지 않았습니다" }, { status: 500 });
  }
  const lat = request.nextUrl.searchParams.get("lat");
  const lng = request.nextUrl.searchParams.get("lng");
  if (!lat || !lng) return NextResponse.json({ error: "lat·lng 파라미터가 필요합니다" }, { status: 400 });

  try {
    const url = "https://naveropenapi.apigw.ntruss.com/map-reversegeocode/v2/gc"
      + `?coords=${encodeURIComponent(lng + "," + lat)}&orders=legalcode&output=json`;
    const resp = await fetch(url, {
      headers: { "X-NCP-APIGW-API-KEY-ID": clientId, "X-NCP-APIGW-API-KEY": clientSecret },
    });
    if (!resp.ok) {
      return NextResponse.json({ error: "네이버 API 오류", detail: await resp.text() }, { status: resp.status });
    }
    return NextResponse.json(await resp.json());
  } catch (err) {
    return NextResponse.json({ error: "프록시 오류", message: err.message }, { status: 500 });
  }
}
```

- [ ] **Step 3: landinfo 라우트** — `app/api/landinfo/route.js`

```js
import { NextResponse } from "next/server";
import { pickJusoCandidate, bldrgstParams, aggregateLandInfo } from "@/lib/landinfo";

export async function GET(request) {
  const jusoKey = process.env.JUSO_CONFIRM_KEY;
  const dataKey = process.env.DATA_GO_KR_KEY;
  if (!jusoKey || !dataKey) {
    return NextResponse.json({ error: "토지정보 API 키가 설정되지 않았습니다" }, { status: 500 });
  }
  const address = request.nextUrl.searchParams.get("address");
  if (!address) return NextResponse.json({ error: "address 파라미터가 필요합니다" }, { status: 400 });

  try {
    // 1) 도로명주소 API → 법정동코드·지번
    const jusoUrl = "https://business.juso.go.kr/addrlink/addrLinkApi.do?"
      + new URLSearchParams({ confmKey: jusoKey, currentPage: "1", countPerPage: "1", keyword: address, resultType: "json" });
    const jusoResp = await fetch(jusoUrl);
    const cand = pickJusoCandidate(await jusoResp.json());
    if (!cand) return NextResponse.json({ juso: null, land: null });

    // 2) 건축물대장 표제부 조회 (전 동, 최대 100)
    const p = bldrgstParams(cand);
    const bldUrl = "https://apis.data.go.kr/1613000/BldRgstHubService/getBrTitleInfo?"
      + new URLSearchParams({
        serviceKey: dataKey, sigunguCd: p.sigunguCd, bjdongCd: p.bjdongCd,
        platGbCd: p.platGbCd, bun: p.bun, ji: p.ji,
        numOfRows: "100", pageNo: "1", _type: "json",
      });
    const bldResp = await fetch(bldUrl);
    const land = aggregateLandInfo(await bldResp.json());
    return NextResponse.json({ juso: { roadAddr: cand.roadAddr, jibunAddr: cand.jibunAddr }, land });
  } catch (err) {
    return NextResponse.json({ error: "토지정보 조회 오류", message: err.message }, { status: 500 });
  }
}
```

- [ ] **Step 4: 회귀 + 로컬 통합 확인(키 있는 경우만)**

Run: `npm test` (전체 PASS 확인). `.env.local`에 4개 키가 모두 있으면: `npm run build && npx next start -p 3100` 후
`curl -s "http://127.0.0.1:3100/api/landinfo?address=서울특별시 강남구 개포로109길 21"` → `land.연면적`이 숫자인지 확인, 서버 종료. **키가 없으면 이 확인은 생략하고 보고서에 "로컬 키 부재 — Preview에서 확인 예정" 명기** (빌드가 느린 환경이므로 생략 시 빌드도 생략 가능 — Task 7에서 Preview로 검증).

- [ ] **Step 5: 커밋**

```bash
git add app/api
git commit -m "feat: API 라우트 3종 — geocode 포팅, reverse-geocode·landinfo 신규"
```

---

### Task 4: 사업정보 폼 — input1 입력·저장

**Files:**
- Create: `lib/formOptions.js`, `app/project/[id]/info/info.css`
- Modify: `app/project/[id]/info/page.js` (자리 페이지 → 전면 재작성)

**Interfaces:**
- Consumes: `projectStore.getProject/updateProject`, Phase 1 컴포넌트(Card·Field·Select·Button·useToast), `REGION_OPTIONS`(Task 1)
- Produces: `data.input1` 저장(위 데이터 계약), `window` 미사용. Task 5·6이 이 페이지에 지도·토지 카드를 끼워 넣을 수 있도록 `handleInput1Change(patch)` 패턴 유지

- [ ] **Step 1: 폼 옵션 상수** — `lib/formOptions.js` (전문 — 값은 legacy 폼·라이브러리 검증본)

```js
// 사업정보 폼 옵션 — legacy 폼·의무비율/단위에너지사용량 라이브러리와 일치해야 함 (변경 금지)
export const 사업형태_OPTIONS = ["공공", "민간"];

export const 사업연도_OPTIONS = ["2024", "2025", "2026", "2027", "2028", "2029", "2030"];

export const 용도_OPTIONS = [
  "교정 및 군사시설", "방송통신시설", "업무시설(공공)", "문화 및 집회시설", "종교시설",
  "의료시설", "교육연구시설", "노유자시설", "수련시설", "운동시설",
  "묘지관련시설", "관광휴게시설", "장례식장", "판매 및 영업시설", "운수시설",
  "업무시설(상업)", "숙박시설", "위락시설", "공동주택",
];

export const EMPTY_INPUT1 = {
  사업형태: "", 사업연도: "", 대지위치: "",
  대지면적: "", 건축면적: "", 연면적: "", 건폐율: "", 용적률: "",
  용도별연면적목록: [{ 용도: "", 연면적: "" }],
};
```

- [ ] **Step 2: info 페이지 전면 재작성** — `app/project/[id]/info/page.js` (전문)

```jsx
"use client";
import { useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { getProject, updateProject } from "@/lib/projectStore";
import { REGION_OPTIONS } from "@/lib/regionResolver";
import { 사업형태_OPTIONS, 사업연도_OPTIONS, 용도_OPTIONS, EMPTY_INPUT1 } from "@/lib/formOptions";
import Card from "@/components/ui/Card";
import Field from "@/components/ui/Field";
import Select from "@/components/ui/Select";
import Button from "@/components/ui/Button";
import "./info.css";

const toOptions = arr => arr.map(v => ({ value: v, label: v }));
const num = v => (v === "" ? "" : Number(v));

export default function InfoPage() {
  const { id } = useParams();
  const [input1, setInput1] = useState(null);

  useEffect(() => {
    const p = getProject(id);
    if (p) setInput1({ ...EMPTY_INPUT1, ...(p.data.input1 ?? {}) });
  }, [id]);

  if (!input1) return null; // 로딩(프로젝트 가드는 WorkspaceShell 담당)

  function apply(patch) {
    const next = { ...input1, ...patch };
    setInput1(next);
    updateProject(id, { data: { input1: next } });
  }

  function applyRow(i, patch) {
    const rows = input1.용도별연면적목록.map((r, idx) => (idx === i ? { ...r, ...patch } : r));
    apply({ 용도별연면적목록: rows });
  }

  const 용도합 = input1.용도별연면적목록.reduce((s, r) => s + (Number(r.연면적) || 0), 0);
  const 총연면적 = Number(input1.연면적) || 0;
  const 합계불일치 = 총연면적 > 0 && Math.abs(용도합 - 총연면적) > 0.01;

  return (
    <div className="info">
      <Card title="① 사업정보">
        <div className="info__grid">
          <Select label="사업형태" placeholder="선택하세요" options={toOptions(사업형태_OPTIONS)}
            value={input1.사업형태} onChange={e => apply({ 사업형태: e.target.value })} />
          <Select label="사업연도" placeholder="선택하세요" options={toOptions(사업연도_OPTIONS)}
            value={input1.사업연도} onChange={e => apply({ 사업연도: e.target.value })} />
          <Select label="대지위치 (의무비율 지역)" placeholder="선택하세요" options={toOptions(REGION_OPTIONS)}
            value={input1.대지위치} onChange={e => apply({ 대지위치: e.target.value })} />
          <Field label="총 연면적 (㎡)" type="number" mono value={input1.연면적}
            onChange={e => apply({ 연면적: num(e.target.value) })} />
        </div>
      </Card>

      <Card title="용도별 연면적" actions={
        <Button size="sm" variant="ghost"
          onClick={() => apply({ 용도별연면적목록: [...input1.용도별연면적목록, { 용도: "", 연면적: "" }] })}>
          행 추가
        </Button>
      }>
        <div className="info__rows">
          {input1.용도별연면적목록.map((row, i) => (
            <div className="info__row" key={i}>
              <Select label={i === 0 ? "용도" : undefined} placeholder="용도 선택" options={toOptions(용도_OPTIONS)}
                value={row.용도} onChange={e => applyRow(i, { 용도: e.target.value })} />
              <Field label={i === 0 ? "연면적 (㎡)" : undefined} type="number" mono value={row.연면적}
                onChange={e => applyRow(i, { 연면적: num(e.target.value) })} />
              <Button size="sm" variant="danger" disabled={input1.용도별연면적목록.length <= 1}
                onClick={() => apply({ 용도별연면적목록: input1.용도별연면적목록.filter((_, idx) => idx !== i) })}>
                삭제
              </Button>
            </div>
          ))}
        </div>
        <p className={`info__sum mono ${합계불일치 ? "info__sum--warn" : ""}`}>
          용도별 합계 {용도합.toLocaleString()} ㎡ / 총 연면적 {총연면적.toLocaleString()} ㎡
          {합계불일치 && " — 합계가 총 연면적과 다릅니다"}
        </p>
      </Card>

      <Card title="대지 현황" inner>
        <div className="info__grid">
          <Field label="대지면적 (㎡)" type="number" mono value={input1.대지면적}
            onChange={e => apply({ 대지면적: num(e.target.value) })} />
          <Field label="건축면적 (㎡)" type="number" mono value={input1.건축면적}
            onChange={e => apply({ 건축면적: num(e.target.value) })} />
          <Field label="건폐율 (%)" type="number" mono value={input1.건폐율}
            onChange={e => apply({ 건폐율: num(e.target.value) })} />
          <Field label="용적률 (%)" type="number" mono value={input1.용적률}
            onChange={e => apply({ 용적률: num(e.target.value) })} />
        </div>
      </Card>
    </div>
  );
}
```

- [ ] **Step 3: `app/project/[id]/info/info.css`** (전문)

```css
.info { display: flex; flex-direction: column; gap: var(--sp-5); }
.info__grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: var(--sp-4); }
.info__rows { display: flex; flex-direction: column; gap: var(--sp-3); }
.info__row { display: grid; grid-template-columns: 2fr 1fr auto; gap: var(--sp-3); align-items: end; }
.info__sum { margin-top: var(--sp-3); font-size: var(--fs-13); color: var(--text-secondary); }
.info__sum--warn { color: var(--accent-warm-dim); }
@media (max-width: 767px) { .info__row { grid-template-columns: 1fr; } }
```

- [ ] **Step 4: 동작 확인** — `npm test` PASS 확인 후, 로컬 빌드는 생략(환경 교훈)하고 lint 차원에서 `npx next build`는 Task 7 Preview로 대체 가능. 단, 이 Task에서는 최소한 `node --check` 대체로 페이지가 import 가능한지 빠른 확인: `node -e "import('./lib/formOptions.js').then(m => console.log('OK', m.용도_OPTIONS.length))"` → `OK 19`

- [ ] **Step 5: 커밋**

```bash
git add lib/formOptions.js app/project/[id]/info
git commit -m "feat: 사업정보 폼 — input1 입력·저장, 용도별 연면적 동적 행, 합계 검증 안내"
```

---

### Task 5: 네이버 지도 위치 선택 — 3경로 지역 확정

**Files:**
- Create: `lib/useNaverMap.js`, `components/map/LocationPicker.jsx`, `components/map/locationPicker.css`
- Modify: `app/project/[id]/info/page.js` (LocationPicker 삽입)

**Interfaces:**
- Consumes: `/api/geocode`·`/api/reverse-geocode`(Task 3), `regionFromNaverElements`·`regionFromReverseGeocode`(Task 1), `NEXT_PUBLIC_NCP_CLIENT_ID`
- Produces: `<LocationPicker value={위치정보|undefined} onResolve({ region, address, lat, lng })>` — 검색·클릭 어느 경로든 onResolve 1회 호출. SDK 로드 실패 시 지도는 숨기고 주소 검색만 동작(우아한 축소). info 페이지는 onResolve에서 `대지위치`(드롭다운과 동일 값 체계)·`위치정보`를 apply — **3경로 동기화**: 드롭다운 직접 선택은 위치정보를 건드리지 않고, 지도 경로는 드롭다운 값을 덮어씀

- [ ] **Step 1: SDK 로더** — `lib/useNaverMap.js` (전문)

```js
"use client";
import { useEffect, useState } from "react";

let sdkPromise = null;

export function loadNaverMapSdk() {
  if (typeof window !== "undefined" && window.naver?.maps) return Promise.resolve();
  if (!sdkPromise) {
    sdkPromise = new Promise((resolve, reject) => {
      const keyId = process.env.NEXT_PUBLIC_NCP_CLIENT_ID;
      if (!keyId) { reject(new Error("NEXT_PUBLIC_NCP_CLIENT_ID 미설정")); return; }
      const s = document.createElement("script");
      s.src = `https://oapi.map.naver.com/openapi/v3/maps.js?ncpKeyId=${keyId}`;
      s.async = true;
      s.onload = () => (window.naver?.maps ? resolve() : reject(new Error("네이버 지도 SDK 초기화 실패")));
      s.onerror = () => reject(new Error("네이버 지도 SDK 로드 실패"));
      document.head.appendChild(s);
    });
    sdkPromise.catch(() => { sdkPromise = null; }); // 실패 시 재시도 허용
  }
  return sdkPromise;
}

export function useNaverMapReady() {
  const [state, setState] = useState({ ready: false, error: null });
  useEffect(() => {
    let alive = true;
    loadNaverMapSdk()
      .then(() => alive && setState({ ready: true, error: null }))
      .catch(e => alive && setState({ ready: false, error: e.message }));
    return () => { alive = false; };
  }, []);
  return state;
}
```

- [ ] **Step 2: LocationPicker** — `components/map/LocationPicker.jsx` (전문)

```jsx
"use client";
import { useEffect, useRef, useState } from "react";
import { useNaverMapReady } from "@/lib/useNaverMap";
import { regionFromNaverElements, regionFromReverseGeocode } from "@/lib/regionResolver";
import Field from "@/components/ui/Field";
import Button from "@/components/ui/Button";
import Badge from "@/components/ui/Badge";
import "./locationPicker.css";

const SEOUL = { lat: 37.5665, lng: 126.978 };

export default function LocationPicker({ value, onResolve }) {
  const { ready, error } = useNaverMapReady();
  const mapEl = useRef(null);
  const mapRef = useRef(null);
  const markerRef = useRef(null);
  const [keyword, setKeyword] = useState("");
  const [busy, setBusy] = useState(false);
  const [notice, setNotice] = useState(null);

  function placeMarker(lat, lng) {
    if (!mapRef.current) return;
    const pos = new window.naver.maps.LatLng(lat, lng);
    mapRef.current.setCenter(pos);
    if (mapRef.current.getZoom() < 15) mapRef.current.setZoom(15);
    if (markerRef.current) markerRef.current.setPosition(pos);
    else markerRef.current = new window.naver.maps.Marker({ map: mapRef.current, position: pos });
  }

  useEffect(() => {
    if (!ready || mapRef.current || !mapEl.current) return;
    const center = value ? { lat: value.lat, lng: value.lng } : SEOUL;
    mapRef.current = new window.naver.maps.Map(mapEl.current, {
      center: new window.naver.maps.LatLng(center.lat, center.lng),
      zoom: value ? 15 : 11,
    });
    if (value) placeMarker(value.lat, value.lng);
    window.naver.maps.Event.addListener(mapRef.current, "click", async e => {
      const lat = e.coord.lat(), lng = e.coord.lng();
      setBusy(true); setNotice(null);
      try {
        const res = await fetch(`/api/reverse-geocode?lat=${lat}&lng=${lng}`);
        const data = await res.json();
        if (!res.ok) throw new Error(data.error ?? "역지오코딩 실패");
        const { region, address } = regionFromReverseGeocode(data.results ?? []);
        if (!region) { setNotice("지역을 판정할 수 없는 위치입니다"); return; }
        placeMarker(lat, lng);
        onResolve({ region, address, lat, lng });
      } catch (err) { setNotice(err.message); } finally { setBusy(false); }
    });
  }, [ready]); // eslint-disable-line react-hooks/exhaustive-deps

  async function search() {
    if (!keyword.trim()) return;
    setBusy(true); setNotice(null);
    try {
      const res = await fetch("/api/geocode?query=" + encodeURIComponent(keyword));
      const data = await res.json();
      if (!res.ok) throw new Error(data.error ?? "주소 검색 실패");
      const addr = (data.addresses ?? [])[0];
      if (!addr) { setNotice("검색 결과가 없습니다: " + keyword); return; }
      const lat = parseFloat(addr.y), lng = parseFloat(addr.x);
      const region = regionFromNaverElements(addr.addressElements ?? []);
      if (!region) { setNotice("지역을 판정할 수 없습니다 — 대지위치를 직접 선택하세요"); return; }
      if (ready) placeMarker(lat, lng);
      onResolve({ region, address: addr.roadAddress || addr.jibunAddress || keyword, lat, lng });
    } catch (err) { setNotice(err.message); } finally { setBusy(false); }
  }

  return (
    <div className="lp">
      <div className="lp__search">
        <Field label="주소 검색" placeholder="예: 서울특별시 강남구 테헤란로 152" value={keyword}
          onChange={e => setKeyword(e.target.value)}
          onKeyDown={e => { if (e.key === "Enter") search(); }} />
        <Button onClick={search} disabled={busy}>{busy ? "검색 중…" : "검색"}</Button>
      </div>
      {error
        ? <p className="lp__fallback">지도를 불러올 수 없습니다 — 주소 검색과 대지위치 직접 선택은 계속 사용할 수 있습니다.</p>
        : <div ref={mapEl} className="lp__map" aria-label="위치 선택 지도" />}
      {value?.address && (
        <p className="lp__result">
          <Badge tone="brand">확정 위치</Badge> <span>{value.address}</span>
        </p>
      )}
      {notice && <p className="lp__notice" role="status">{notice}</p>}
    </div>
  );
}
```

- [ ] **Step 3: `components/map/locationPicker.css`** (전문)

```css
.lp { display: flex; flex-direction: column; gap: var(--sp-3); }
.lp__search { display: grid; grid-template-columns: 1fr auto; gap: var(--sp-3); align-items: end; }
.lp__map { width: 100%; height: 320px; border: 1px solid var(--border-default); border-radius: var(--radius-md); }
.lp__fallback, .lp__notice { font-size: var(--fs-13); color: var(--text-secondary); }
.lp__notice { color: var(--accent-warm-dim); }
.lp__result { display: flex; align-items: center; gap: var(--sp-2); font-size: var(--fs-14); }
```

- [ ] **Step 4: info 페이지에 삽입** — `app/project/[id]/info/page.js` 수정: import 추가 후 "① 사업정보" 카드 아래에 다음 카드 삽입. `handleLocation`은 대지위치 드롭다운과 동일 값 체계로 동기화한다.

```jsx
import LocationPicker from "@/components/map/LocationPicker";
// InfoPage 안에 추가:
function handleLocation({ region, address, lat, lng }) {
  apply({ 대지위치: region, 위치정보: { address, lat, lng } });
}
// JSX — "① 사업정보" Card 다음:
<Card title="위치 선택 (지도)" actions={<Badge tone="action">3경로 동기</Badge>}>
  <LocationPicker value={input1.위치정보} onResolve={handleLocation} />
</Card>
```
(Badge import 추가. 드롭다운 직접 변경 시 위치정보는 유지 — 주소와 지역이 달라질 수 있음은 사용자 책임 영역으로 두고 별도 경고 없음.)

- [ ] **Step 5: 동작 확인** — `npm test` PASS. UI 통합 확인은 Task 7(Preview)에서 수행 — 이 Task에서는 `node -e "import('./lib/useNaverMap.js')"`가 오류 없이 로드되는지 정도만 (use client 모듈이라 react 해석 실패 시 생략 가능, 실패해도 무방하다고 보고서에 명기).

- [ ] **Step 6: 커밋**

```bash
git add lib/useNaverMap.js components/map app/project/[id]/info/page.js
git commit -m "feat: 네이버 지도 위치 선택 — 주소검색·지도클릭·드롭다운 3경로 지역 확정"
```

---

### Task 6: 토지정보 카드 — 조회·연면적 자동 채움

**Files:**
- Create: `components/map/LandInfoCard.jsx`
- Modify: `app/project/[id]/info/page.js` (카드 삽입 + 자동 채움 연동), `components/map/locationPicker.css`(공용 스타일 몇 줄 추가)

**Interfaces:**
- Consumes: `/api/landinfo`(Task 3), Phase 1 Table·Badge·Button·useToast
- Produces: `<LandInfoCard address={string|null} onApply(land)>` — address 변경 시 자동 조회. `onApply`는 조회 성공 시 info 페이지가 input1에 반영: **연면적은 항상 자동 채움**(수정 가능 필드), 대지면적·건축면적·건폐율·용적률은 **빈 값일 때만** 채움

- [ ] **Step 1: LandInfoCard** — `components/map/LandInfoCard.jsx` (전문)

```jsx
"use client";
import { useEffect, useRef, useState } from "react";
import Badge from "@/components/ui/Badge";
import "./locationPicker.css";

export default function LandInfoCard({ address, onApply }) {
  const [state, setState] = useState({ status: "idle", land: null, juso: null });
  const lastApplied = useRef(null);

  useEffect(() => {
    if (!address) return;
    let alive = true;
    setState({ status: "loading", land: null, juso: null });
    fetch("/api/landinfo?address=" + encodeURIComponent(address))
      .then(async res => {
        const data = await res.json();
        if (!res.ok) throw new Error(data.error ?? "조회 실패");
        return data;
      })
      .then(data => {
        if (!alive) return;
        setState({ status: "done", land: data.land, juso: data.juso });
        if (data.land && lastApplied.current !== address) {
          lastApplied.current = address;
          onApply(data.land);
        }
      })
      .catch(err => alive && setState({ status: "error", land: null, juso: null, message: err.message }));
    return () => { alive = false; };
  }, [address]); // eslint-disable-line react-hooks/exhaustive-deps

  if (!address) return <p className="lp__fallback">지도에서 위치를 확정하면 현황 건축물의 토지정보를 조회합니다.</p>;
  if (state.status === "loading") return <p className="lp__fallback">토지정보 조회 중…</p>;
  if (state.status === "error") return <p className="lp__notice" role="status">토지정보 조회 오류: {state.message} — 아래 대지 현황에 직접 입력하세요.</p>;
  if (state.status === "done" && !state.land) {
    return <p className="lp__fallback">조회 결과 없음 (나대지 등) — 아래 대지 현황에 직접 입력하세요.</p>;
  }
  if (state.status !== "done") return null;

  const L = state.land;
  const rows = [
    ["대지면적", L.대지면적, "㎡"], ["건축면적", L.건축면적, "㎡"], ["연면적", L.연면적, "㎡"],
    ["건폐율", L.건폐율, "%"], ["용적률", L.용적률, "%"],
  ];
  return (
    <div className="lic">
      <p className="lic__head">
        <Badge tone="warm">현황 건축물 기준</Badge>
        <span className="lic__addr">{state.juso?.roadAddr ?? address}{L.동수 > 1 ? ` · ${L.동수}개 동 합산` : ""}</span>
      </p>
      <dl className="lic__grid">
        {rows.map(([k, v, unit]) => (
          <div className="lic__item" key={k}>
            <dt>{k}</dt>
            <dd className="mono">{v > 0 ? `${v.toLocaleString()} ${unit}` : "-"}</dd>
          </div>
        ))}
      </dl>
      <p className="lp__fallback">연면적은 사업정보에 자동 반영되었습니다 (신축 검토 시 계획 값으로 수정 가능).</p>
    </div>
  );
}
```

- [ ] **Step 2: 스타일 추가** — `components/map/locationPicker.css` 하단에 추가

```css
.lic { display: flex; flex-direction: column; gap: var(--sp-3); }
.lic__head { display: flex; align-items: center; gap: var(--sp-2); }
.lic__addr { font-size: var(--fs-13); color: var(--text-secondary); }
.lic__grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: var(--sp-3); }
.lic__item dt { font-size: var(--fs-12); color: var(--text-hint); }
.lic__item dd { font-size: var(--fs-16); }
```

- [ ] **Step 3: info 페이지 연동** — `app/project/[id]/info/page.js` 수정:

```jsx
import LandInfoCard from "@/components/map/LandInfoCard";
import { useToast } from "@/components/ui/ToastProvider";
// InfoPage 안:
const { push } = useToast();
function handleLandApply(land) {
  const patch = { 연면적: land.연면적 };                       // 연면적은 항상 채움
  for (const k of ["대지면적", "건축면적", "건폐율", "용적률"]) {
    if (input1[k] === "" || input1[k] == null) patch[k] = land[k] > 0 ? land[k] : "";
  }
  apply(patch);
  push({ message: "현황 건축물 연면적을 반영했습니다", tone: "pass" });
}
// JSX — "위치 선택 (지도)" Card 다음:
<Card title="토지정보 조회">
  <LandInfoCard address={input1.위치정보?.address ?? null} onApply={handleLandApply} />
</Card>
```
주의: `handleLandApply`의 `input1` 클로저 최신성 — `apply(patch)`가 최신 state 기반이 되도록 `setInput1(prev => ...)` 함수형 갱신으로 `apply`를 리팩터링해도 좋다(선택). 단순 유지 시에도 onApply는 조회 직후 1회 호출이라 실사용 문제 없음.

- [ ] **Step 4: 확인** — `npm test` PASS. 통합은 Task 7.

- [ ] **Step 5: 커밋**

```bash
git add components/map app/project/[id]/info/page.js
git commit -m "feat: 토지정보 카드 — juso→건축물대장 조회, 연면적 자동 채움, 조회실패 수기입력 안내"
```

---

### Task 7: 통합 검증 — 회귀 + Preview E2E (3경로 일치)

**Files:** 원칙적으로 수정 없음 (결함 발견 시 이번 Phase 산출 파일만 fix 커밋)

- [ ] **Step 1: 회귀** — `npm test`(전체 PASS: 기존 8 + Task 1·2의 13) + verify 스냅샷: `node verify/verify_all.js public > /tmp/p2_all.txt && diff verify/snapshots/baseline_verify_all.txt /tmp/p2_all.txt && node verify/verify_output12.js public > /tmp/p2_o12.json && diff verify/snapshots/baseline_output12.json /tmp/p2_o12.json && echo IDENTICAL`

- [ ] **Step 2: 푸시 + 배포** — `git push origin revamp/next` → Vercel MCP로 branchAlias 최신 배포 READY 확인 (빌드 실패 시 로그 확인 후 fix)

- [ ] **Step 3: Preview E2E** — Phase 1의 검증 방식 재사용: 로컬 Playwright 스크립트(`node_modules/playwright`, `chromium.launch({ channel: "msedge" })`, share URL로 인증 쿠키) 작성·실행. 검증 항목:
  1. 프로젝트 생성(localStorage 시드) → `/project/{id}/info` 진입
  2. **경로 1 (드롭다운)**: 대지위치 select에서 "부산광역시" 선택 → input1 저장 확인(새로고침 후 유지)
  3. **경로 2 (주소 검색)**: "서울특별시 강남구 테헤란로 152" 검색 → 확정 위치 표시 + 대지위치 select 값 = "서울특별시" (드롭다운과 일치 확인)
  4. **경로 3 (지도 클릭)**: 지도 캔버스 좌표 클릭 → 대지위치가 REGION_OPTIONS 중 하나로 갱신되는지 확인. SDK 미로드 환경이면 `/api/reverse-geocode?lat=37.5665&lng=126.978` 직접 호출로 "서울특별시" 판정 확인으로 대체하고 보고서에 명기
  5. **토지정보**: 검색 경로 확정 후 토지정보 카드에 연면적 값(>0) 표시 + 사업정보 총 연면적 필드에 자동 반영 확인
  6. **강원 영동/영서**: `/api/geocode?query=강릉시 경강로 2007` 호출 → regionFromNaverElements 결과 "강원 영동" (API 레벨 확인)
  7. 용도별 행 추가/삭제 동작, 합계 불일치 안내 표시
  8. `/engine-check` = 5492250 유지, 콘솔 에러 0 (favicon 404 제외)
- [ ] **Step 4: 반응형 스팟체크** — 1024·768에서 info 페이지 가로 스크롤 없음 + info__row 1열 전환(767px 이하) 스크린샷
- [ ] **Step 5: 정리** — 서버·브라우저·잔류 프로세스 종료. 프로덕션(main) 무영향 확인.

Expected: 3경로 모두 동일 지역 체계로 확정 + 토지정보 표시·자동 채움 (마스터플랜 Phase 2 완료 기준).

---

## Self-Review 결과

- **스펙 커버리지**: 마스터플랜 Phase 2 행 전 항목 매핑 — 폼(Task 4), SDK 로더(Task 5), geocode 포팅(Task 3), reverse-geocode 신규(Task 3), regionResolver 추출+영동/영서(Task 1), landinfo 신규(Task 2·3), 카드+자동 채움+라벨+수기입력(Task 6), 3경로 일치 검증(Task 7)
- **플레이스홀더 스캔**: 없음 — 모든 코드 전문 포함. Task 5 Step 5·Task 6 Step 4의 "통합은 Task 7"은 빌드가 느린 환경에 대한 의도된 검증 이연이며 Task 7에 구체 절차 존재
- **타입 일관성**: `onResolve({region,address,lat,lng})` ↔ `handleLocation` 일치, `land` 필드명(대지면적·건축면적·연면적·건폐율·용적률·동수) ↔ `aggregateLandInfo` 반환 ↔ LandInfoCard rows ↔ `handleLandApply` 일치, `REGION_OPTIONS` 값 = legacy sel-대지위치 17개 = 시도매핑 치역 일치 확인
- **주의 승계**: localStorage는 useEffect에서만(하이드레이션), 컴포넌트 CSS는 토큰만, 숫자는 mono 클래스
