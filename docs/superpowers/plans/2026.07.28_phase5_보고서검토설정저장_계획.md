# Phase 5 — 보고서·검토·설정·저장 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 마스터플랜: `docs/superpowers/plans/2026.07.26_전면개편_마스터플랜.md`

**Goal:** ④ 보고서 화면(AI 검토의견 스트리밍·프롬프트 관리·미리보기·PDF/Word/HTML·표지 이미지) + Phase 4 이월분(카드별 AI 설명·제약조건 모달·조합 보고서) + 설정 모달(SettingsUI 재사용) + 구버전 프로젝트 마이그레이션으로 기능 전수를 완성한다.

**Architecture:** legacy 자산 최대 재사용 — `public/review/*`는 ES 모듈이라 직접 import, `public/report/*`·`public/optimize/optimizeExplain·optimizeReport`·`public/settings/settingsUI`는 클래식 스크립트라 멱등 로더로 window 전역 로드(모두 수정 금지·로드만). 마이그레이션은 순수 함수(TDD). 신규 API 라우트는 `/api/review`(gemini-2.5-pro SSE 프록시) 1개.

**Tech Stack:** 기존 report/review/settings 모듈(무수정)·CDN(jsPDF·html2canvas·docx)·Gemini 2.5 Pro/Flash SSE·Phase 1 Modal 컴포넌트·node:test

## Global Constraints (마스터플랜 + Phase 5 추가)

- **엔진·자산 무수정**: `engine/` `public/*` 수정 금지 (Phase 4의 recommendPrompt 예외는 종료 — 이번 Phase는 public/ 수정 0건). `lib/engineScripts.js`·`lib/useEngineReady.js`·`app/engine-check/`·`legacy/` 수정 금지. **public/ 스크립트의 로드·import·window 브리지는 허용**(수정이 아님)
- **계산 동일성**: 최종 Task에서 verify 스냅샷 IDENTICAL
- **브랜치**: `revamp/next`만. `git add -A`·`git add .` 금지 — 명시 경로만
- JavaScript(TS 금지), plain CSS + 토큰만(단, Task 7의 `.set-*` 규칙은 legacy 스타일 **복사본**이라 리터럴 허용 — 출처 주석 필수), UI 문구 한국어, localStorage는 useEffect/핸들러에서만
- **API 키**: `GEMINI_API_KEY` 서버 라우트 전용
- 검토의견 표시·저장은 **plain text 기준**(XSS 안전 — legacy도 reviewText 우선). HTML 렌더는 escape + `\n→<br>`만
- **환경 교훈**: 로컬 build/dev 금지 — Preview + Playwright(msedge, share URL) 검증. **베테랑 교훈: 에이전트 E2E 판정은 스크린샷·네트워크 로그 1차 증거로 컨트롤러가 재확인. Vercel 웹훅 누락 시 빈 커밋 재트리거**

## 파일 구조 (이번 Phase 산출물)

```
lib/migrateLegacy.js                     구버전(projects_v2)→신버전 변환 (순수 함수, TDD)
tests/migrateLegacy.test.mjs
app/api/review/route.js                  Gemini 2.5 Pro SSE 프록시 (legacy/api/review.js 포팅)
lib/scriptLoader.js                      범용 멱등 클래식 스크립트 로더 (report·settings·optimize extras 공용)
lib/reportAssets.js                      CDN 3종+report 스크립트 4종 로드 + normalize대지위치 window 브리지 + collectReportData
components/report/ReviewSection.jsx      검토의견 생성(스트리밍)·프롬프트 선택
components/report/PromptManagerModal.jsx 검토 프롬프트 CRUD 모달
components/report/ReportActions.jsx      표지 이미지 업로드 + 미리보기/다운로드 버튼 + 미리보기 모달(iframe)
components/report/report.css
app/project/[id]/report/page.js          ④ 보고서 화면 전면 재작성
app/project/[id]/report/reportPage.css
components/optimize/ConstraintsModal.jsx 제약조건 설정 모달 (이월)
lib/optimizeExtras.js                    optimizeExplain·optimizeReport 스크립트 로드 래퍼 (이월)
components/settings/SettingsModal.jsx    설정 모달 (SettingsUI.init 재사용)
components/settings/settingsModal.css    legacy style.css의 .set-* 규칙 복사본
app/page.js                              수정: 구버전 프로젝트 가져오기 버튼
app/project/[id]/optimize/page.js        수정: 설정·제약조건·AI 설명·조합 보고서 연동
components/optimize/ComboCard.jsx        수정: AI 설명 버튼·표시 (옵션 prop)
components/optimize/RecommendPanel.jsx   수정: 제약조건 버튼·요약
lib/recommendClient.js                   수정: constraints 전달 파라미터
```

## 데이터 계약 (Phase 6이 의존)

- `data.review = { text: string, at: number }` — 검토의견 plain text (렌더는 escape+`<br>`)
- `data.coverImage = string|null` — base64 data URL
- `data.optExplains = { [rank]: string }` — 카드별 AI 설명
- `data.aiConstraints = { solarNote, nearSubway(boolean), geothermalNote, fuelCellNote, budgetMin(""|number), budgetMax(""|number), excludeSources(string[]), customConstraints }` — AI 추천 제약조건
- 검토 프롬프트 localStorage 키는 legacy와 동일 `"reviewPrompts"` (자연 호환), 설정은 SettingsStore의 `"optset:*"` 그대로
- **구버전 마이그레이션 매핑** (`projects_v2` 항목 → 신규 `data`): `input1`→`input1`(용도별연면적목록의 `비율` 필드는 버림, `세대수` 유지), `scenarios`→`input2.scenarios`(systems의 5필드 유지, `신재생에너지생산량` 등 여분 필드 버림), `reviewText`(우선)/`reviewHTML`(태그 제거 폴백)→`review.text`, `coverImage`→`coverImage`, `optimize.memos`→`optMemos`, `optimize.explains`→`optExplains`. `optimize.inputs`는 구조 상이(비매핑) — 버리고 보고서에 명기. `name`→프로젝트 이름, id 신규 발급
- 마이그레이션은 **복사**(구버전 projects_v2는 삭제하지 않음 — 구앱 병행 사용 보호)

---

### Task 1: lib/migrateLegacy.js — 구버전 변환 (TDD)

**Files:**
- Create: `lib/migrateLegacy.js`, `tests/migrateLegacy.test.mjs`

**Interfaces:**
- Produces:
  - `convertLegacyProject(legacy): { name, data } | null` — 위 매핑 계약. `legacy.input1` 없으면 null
  - `stripHtml(html): string` — reviewHTML 폴백용 (태그 제거, `<br>`→`\n`)
  - `readLegacyProjects(raw): array` — `JSON.parse` 안전 래퍼(`projects_v2` 원문 문자열 입력, 실패/비배열 → `[]`)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/migrateLegacy.test.mjs` (전문)

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { convertLegacyProject, stripHtml, readLegacyProjects } from "../lib/migrateLegacy.js";

const legacy = {
  name: "테스트 사업", savedAt: "2026.07.01 9:00:00", projectName: "테스트 사업",
  input1: {
    사업형태: "민간", 사업연도: "2025", 대지위치: "서울특별시",
    대지면적: 2000, 건축면적: 1000, 연면적: 20000, 건폐율: 50, 용적률: 200,
    용도별연면적목록: [
      { 용도: "공동주택", 비율: 75, 연면적: 15000, 세대수: "1000세대 이상" },
      { 용도: "판매 및 영업시설", 비율: 25, 연면적: 5000 },
    ],
  },
  scenarios: [{ id: "ALT-1", systems: [
    { 에너지원: "태양광", 형식: "태양광-고정식", 단위에너지생산량: 1358, 원별보정계수: 0.95, 적용용량: 50, 신재생에너지생산량: 64505 },
  ] }],
  activeAltIdx: 0,
  reviewHTML: "검토<br>의견", reviewText: "검토\n의견",
  coverImage: "data:image/png;base64,AAA",
  optimize: { inputs: { 무관: 1 }, memos: { 1: "메모1" }, explains: { 1: "설명1" } },
};

test("convertLegacyProject — 매핑 계약 전체", () => {
  const r = convertLegacyProject(legacy);
  assert.equal(r.name, "테스트 사업");
  assert.equal(r.data.input1.사업형태, "민간");
  assert.deepEqual(r.data.input1.용도별연면적목록[0], { 용도: "공동주택", 연면적: 15000, 세대수: "1000세대 이상" }); // 비율 제거
  assert.deepEqual(r.data.input1.용도별연면적목록[1], { 용도: "판매 및 영업시설", 연면적: 5000 });
  assert.equal(r.data.input2.scenarios[0].id, "ALT-1");
  assert.deepEqual(r.data.input2.scenarios[0].systems[0],
    { 에너지원: "태양광", 형식: "태양광-고정식", 적용용량: 50, 단위에너지생산량: 1358, 원별보정계수: 0.95 }); // 생산량 제거
  assert.equal(r.data.review.text, "검토\n의견");           // reviewText 우선
  assert.equal(typeof r.data.review.at, "number");
  assert.equal(r.data.coverImage, "data:image/png;base64,AAA");
  assert.deepEqual(r.data.optMemos, { 1: "메모1" });
  assert.deepEqual(r.data.optExplains, { 1: "설명1" });
  assert.equal(r.data.input3, undefined);                   // optimize.inputs 비매핑
});

test("convertLegacyProject — reviewText 없으면 reviewHTML 태그 제거 폴백", () => {
  const r = convertLegacyProject({ ...legacy, reviewText: "", reviewHTML: "a<br>b<b>굵게</b>" });
  assert.equal(r.data.review.text, "a\nb굵게");
});

test("convertLegacyProject — review 둘 다 없으면 review 미생성, input1 없으면 null", () => {
  const r = convertLegacyProject({ ...legacy, reviewText: "", reviewHTML: "" });
  assert.equal(r.data.review, undefined);
  assert.equal(convertLegacyProject({ name: "x" }), null);
  assert.equal(convertLegacyProject(null), null);
});

test("convertLegacyProject — 선택 필드 부재 허용 (최소 구버전)", () => {
  const r = convertLegacyProject({ name: "최소", input1: { 사업형태: "공공", 용도별연면적목록: [] } });
  assert.equal(r.name, "최소");
  assert.deepEqual(r.data.input2, undefined);               // scenarios 없음
  assert.equal(r.data.coverImage, undefined);
});

test("stripHtml", () => {
  assert.equal(stripHtml("a<br>b<br/>c<div>d</div>"), "a\nb\ncd");
  assert.equal(stripHtml(""), "");
});

test("readLegacyProjects — 안전 파싱", () => {
  assert.deepEqual(readLegacyProjects(JSON.stringify([{ name: "a" }])), [{ name: "a" }]);
  assert.deepEqual(readLegacyProjects("깨진 json"), []);
  assert.deepEqual(readLegacyProjects(null), []);
  assert.deepEqual(readLegacyProjects(JSON.stringify({ not: "array" })), []);
});
```

- [ ] **Step 2: 실행해 실패 확인** — `npm test` FAIL (모듈 없음)

- [ ] **Step 3: 최소 구현** — `lib/migrateLegacy.js` (전문)

```js
// 구버전(projects_v2) → 신버전(rp.projects.v1 data) 변환 — 복사 방식(원본 보존)

export function stripHtml(html) {
  if (!html) return "";
  return String(html)
    .replace(/<br\s*\/?>/gi, "\n")
    .replace(/<[^>]+>/g, "");
}

export function readLegacyProjects(raw) {
  if (!raw) return [];
  try {
    const parsed = JSON.parse(raw);
    return Array.isArray(parsed) ? parsed : [];
  } catch { return []; }
}

export function convertLegacyProject(legacy) {
  if (!legacy || typeof legacy !== "object" || !legacy.input1) return null;
  const data = {};

  const i1 = legacy.input1;
  data.input1 = {
    사업형태: i1.사업형태 ?? "", 사업연도: String(i1.사업연도 ?? ""), 대지위치: i1.대지위치 ?? "",
    대지면적: i1.대지면적 ?? "", 건축면적: i1.건축면적 ?? "", 연면적: i1.연면적 ?? "",
    건폐율: i1.건폐율 ?? "", 용적률: i1.용적률 ?? "",
    용도별연면적목록: (i1.용도별연면적목록 ?? []).map(r => {
      const row = { 용도: r.용도 ?? "", 연면적: r.연면적 ?? "" };
      if (r.세대수) row.세대수 = r.세대수;
      return row;
    }),
  };

  if (Array.isArray(legacy.scenarios) && legacy.scenarios.length) {
    data.input2 = {
      scenarios: legacy.scenarios.map(sc => ({
        id: sc.id,
        systems: (sc.systems ?? []).map(s => ({
          에너지원: s.에너지원 ?? "", 형식: s.형식 ?? "", 적용용량: s.적용용량 ?? "",
          단위에너지생산량: s.단위에너지생산량 ?? 0, 원별보정계수: s.원별보정계수 ?? 0,
        })),
      })),
    };
  }

  const reviewText = legacy.reviewText || stripHtml(legacy.reviewHTML);
  if (reviewText) data.review = { text: reviewText, at: Date.now() };
  if (legacy.coverImage) data.coverImage = legacy.coverImage;
  if (legacy.optimize?.memos && Object.keys(legacy.optimize.memos).length) data.optMemos = { ...legacy.optimize.memos };
  if (legacy.optimize?.explains && Object.keys(legacy.optimize.explains).length) data.optExplains = { ...legacy.optimize.explains };
  // legacy.optimize.inputs는 신규 input3와 구조가 달라 비매핑 (사용자가 ③에서 재입력)

  return { name: legacy.name || legacy.projectName || "가져온 프로젝트", data };
}
```

- [ ] **Step 4: 통과 확인** — `npm test` 기존 38 + 신규 6 = 44 PASS, 스킵 0
- [ ] **Step 5: 커밋**

```bash
git add lib/migrateLegacy.js tests/migrateLegacy.test.mjs
git commit -m "feat: migrateLegacy — 구버전 projects_v2 변환 순수 함수 (TDD 6건)"
```

---

### Task 2: /api/review 포팅 + 범용 스크립트 로더

**Files:**
- Create: `app/api/review/route.js`, `lib/scriptLoader.js`

**Interfaces:**
- `POST /api/review` → Gemini SSE 원본 스트림 (`legacy/api/review.js` 의미 동일 — 기본 모델 `gemini-2.5-pro`, `maxDuration = 60`, 오류 계약은 Phase 4 recommend 라우트와 동일 패턴)
- `loadScriptOnce(src): Promise<void>` — 클래식 스크립트 멱등 로드(성공 캐시·실패 시 재시도 허용). Task 4·6·7이 공용

- [ ] **Step 1: app/api/review/route.js** (전문 — recommend 라우트와 동일 골격, 모델만 상이)

```js
import { NextResponse } from "next/server";

export const maxDuration = 60;

export async function POST(request) {
  const apiKey = process.env.GEMINI_API_KEY;
  if (!apiKey) {
    return NextResponse.json({ error: "GEMINI_API_KEY가 설정되지 않았습니다" }, { status: 500 });
  }
  let body;
  try { body = await request.json(); }
  catch { return NextResponse.json({ error: "잘못된 요청 본문" }, { status: 400 }); }

  const model = body.model || "gemini-2.5-pro";
  try {
    const resp = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/${model}:streamGenerateContent?alt=sse&key=${apiKey}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          system_instruction: body.system_instruction,
          contents: body.contents,
          generationConfig: body.generationConfig ?? { maxOutputTokens: 8192 },
        }),
      },
    );
    if (!resp.ok) {
      return NextResponse.json({ error: "Gemini API 오류", detail: await resp.text() }, { status: resp.status });
    }
    return new Response(resp.body, {
      headers: { "Content-Type": "text/event-stream", "Cache-Control": "no-cache" },
    });
  } catch (err) {
    return NextResponse.json({ error: "프록시 오류", message: err.message }, { status: 500 });
  }
}
```

- [ ] **Step 2: lib/scriptLoader.js** (전문)

```js
"use client";

const cache = new Map(); // src → Promise

export function loadScriptOnce(src) {
  if (typeof window === "undefined") return Promise.reject(new Error("브라우저 전용"));
  if (!cache.has(src)) {
    const p = new Promise((resolve, reject) => {
      const s = document.createElement("script");
      s.src = src;
      s.async = false; // 순서 보존 (report 스크립트 상호 의존)
      s.onload = resolve;
      s.onerror = () => reject(new Error("스크립트 로드 실패: " + src));
      document.head.appendChild(s);
    });
    p.catch(() => cache.delete(src));
    cache.set(src, p);
  }
  return cache.get(src);
}
```

- [ ] **Step 3: 확인** — `npm test` 44 PASS, `node --check app/api/review/route.js`
- [ ] **Step 4: 커밋**

```bash
git add app/api/review/route.js lib/scriptLoader.js
git commit -m "feat: /api/review SSE 프록시 포팅(gemini-2.5-pro) + 범용 멱등 스크립트 로더"
```

---

### Task 3: ④ 보고서 — 검토의견 섹션 + 프롬프트 관리

**Files:**
- Create: `components/report/ReviewSection.jsx`, `components/report/PromptManagerModal.jsx`, `components/report/report.css`

**Interfaces:**
- Consumes: `@/public/review/reviewGenerator.js`의 `generateReview(systemPrompt, calcData, callbacks)`·`buildUserMessage`(ES 모듈 — **구현 전 jsconfig `@/*` 별칭으로 public 하위 import가 빌드되는지 확인, 안 되면 상대경로로 대체 후 보고서 명기**), `@/public/review/reviewPromptManager.js`의 `getPrompts/addPrompt/updatePrompt/deletePrompt/DEFAULT_PROMPT`, Phase 1 `Modal`·`Select`·`Button`·`Field`
- Produces:
  - `<ReviewSection calcData review onSave>` — calcData = `{ input1, output1, output2 }` (페이지가 재계산 제공, null이면 버튼 비활성+안내). 생성 완료 시 `onSave(text)`. 표시: 저장된 review.text 또는 스트리밍 누적 텍스트를 escape+`<br>` 렌더
  - `<PromptManagerModal open onClose onChanged>` — reviewPromptManager CRUD 래핑 (select + textarea + 새로만들기/저장/삭제, 기본 프롬프트 삭제 불가)

- [ ] **Step 1: ReviewSection.jsx** (전문)

```jsx
"use client";
import { useEffect, useState } from "react";
import Button from "@/components/ui/Button";
import Select from "@/components/ui/Select";
import PromptManagerModal from "./PromptManagerModal";
import { generateReview } from "@/public/review/reviewGenerator.js";
import { getPrompts } from "@/public/review/reviewPromptManager.js";
import "./report.css";

const esc = s => s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");

export default function ReviewSection({ calcData, review, onSave }) {
  const [prompts, setPrompts] = useState([]);
  const [sel, setSel] = useState("");
  const [text, setText] = useState(review?.text ?? "");
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState(null);
  const [manageOpen, setManageOpen] = useState(false);

  useEffect(() => { reloadPrompts(); }, []);
  useEffect(() => { setText(review?.text ?? ""); }, [review?.text]);

  function reloadPrompts() {
    const list = getPrompts();
    setPrompts(list);
    setSel(prev => (list.some(p => p.name === prev) ? prev : list[0]?.name ?? ""));
  }

  async function run() {
    if (!calcData) return;
    const prompt = prompts.find(p => p.name === sel);
    if (!prompt) return;
    setBusy(true); setError(null); setText("");
    let acc = "";
    try {
      await generateReview(prompt.body, calcData, {
        onToken: t => { acc += t; setText(acc); },
        onDone: () => { onSave(acc); },
        onError: e => { setError(e.message); },
      });
    } catch (e) { setError(e.message); }
    finally { setBusy(false); }
  }

  return (
    <div className="rv">
      <div className="rv__bar">
        <Select label="검토 프롬프트" options={prompts.map(p => ({ value: p.name, label: p.name }))}
          value={sel} onChange={e => setSel(e.target.value)} />
        <Button variant="ghost" size="sm" onClick={() => setManageOpen(true)}>프롬프트 관리</Button>
        <Button onClick={run} disabled={busy || !calcData}>{busy ? "생성 중…" : "검토의견 생성"}</Button>
      </div>
      {!calcData && <p className="rv__hint">①·② 입력을 완료하면 검토의견을 생성할 수 있습니다.</p>}
      {error && <p className="rv__error" role="status">검토의견 생성 오류: {error}</p>}
      {text
        ? <div className="rv__output" dangerouslySetInnerHTML={{ __html: esc(text).replace(/\n/g, "<br>") }} />
        : <p className="rv__hint">아직 생성된 검토의견이 없습니다.</p>}
      <PromptManagerModal open={manageOpen} onClose={() => setManageOpen(false)} onChanged={reloadPrompts} />
    </div>
  );
}
```

- [ ] **Step 2: PromptManagerModal.jsx** (전문 — Phase 1 `Modal` props는 구현 전 실측: `components/ui/Modal.jsx`의 open/onClose/title 계약 확인 후 맞춰 적용, 차이는 보고서 명기)

```jsx
"use client";
import { useEffect, useState } from "react";
import Modal from "@/components/ui/Modal";
import Button from "@/components/ui/Button";
import Select from "@/components/ui/Select";
import Field from "@/components/ui/Field";
import { getPrompts, addPrompt, updatePrompt, deletePrompt } from "@/public/review/reviewPromptManager.js";

export default function PromptManagerModal({ open, onClose, onChanged }) {
  const [prompts, setPrompts] = useState([]);
  const [sel, setSel] = useState("");
  const [body, setBody] = useState("");
  const [newName, setNewName] = useState("");
  const [msg, setMsg] = useState(null);

  useEffect(() => { if (open) reload(); }, [open]);

  function reload(keep) {
    const list = getPrompts();
    setPrompts(list);
    const name = keep && list.some(p => p.name === keep) ? keep : list[0]?.name ?? "";
    setSel(name);
    setBody(list.find(p => p.name === name)?.body ?? "");
    setMsg(null);
  }

  function pick(name) {
    setSel(name);
    setBody(prompts.find(p => p.name === name)?.body ?? "");
  }

  function act(fn, keep) {
    try { fn(); reload(keep); onChanged(); }
    catch (e) { setMsg(e.message); }
  }

  const isDefault = sel === prompts[0]?.name;

  return (
    <Modal open={open} onClose={onClose} title="검토 프롬프트 관리">
      <div className="pm">
        <Select label="프롬프트 선택" options={prompts.map(p => ({ value: p.name, label: p.name }))}
          value={sel} onChange={e => pick(e.target.value)} />
        <label className="pm__editor">
          <span>내용</span>
          <textarea rows={12} value={body} onChange={e => setBody(e.target.value)} />
        </label>
        <div className="pm__row">
          <Field label="새 프롬프트 이름" value={newName} onChange={e => setNewName(e.target.value)} placeholder="예: 간략 검토" />
          <Button size="sm" variant="ghost" disabled={!newName.trim()}
            onClick={() => act(() => addPrompt(newName.trim(), body), newName.trim())}>새로 만들기</Button>
        </div>
        <div className="pm__row">
          <Button size="sm" onClick={() => act(() => updatePrompt(sel, body), sel)} disabled={!sel}>저장</Button>
          <Button size="sm" variant="danger" disabled={!sel || isDefault}
            onClick={() => act(() => deletePrompt(sel))}>삭제</Button>
          <Button size="sm" variant="ghost" onClick={onClose}>닫기</Button>
        </div>
        {msg && <p className="rv__error" role="status">{msg}</p>}
        {isDefault && <p className="rv__hint">기본 프롬프트는 삭제할 수 없습니다 (수정 후 저장은 가능).</p>}
      </div>
    </Modal>
  );
}
```

- [ ] **Step 3: report.css** (전문)

```css
.rv { display: flex; flex-direction: column; gap: var(--sp-3); }
.rv__bar { display: grid; grid-template-columns: 1fr auto auto; gap: var(--sp-3); align-items: end; }
.rv__hint { font-size: var(--fs-13); color: var(--text-secondary); }
.rv__error { font-size: var(--fs-13); color: var(--color-fail); }
.rv__output { font-size: var(--fs-14); line-height: 1.7; border: 1px solid var(--border-default); border-radius: var(--radius-md); padding: var(--sp-4); max-height: 480px; overflow-y: auto; }
.pm { display: flex; flex-direction: column; gap: var(--sp-3); }
.pm__editor { display: flex; flex-direction: column; gap: var(--sp-1); font-size: var(--fs-13); color: var(--text-secondary); }
.pm__editor textarea { font: inherit; font-size: var(--fs-13); color: inherit; border: 1px solid var(--border-default); border-radius: var(--radius-md); padding: var(--sp-3); background: transparent; resize: vertical; }
.pm__row { display: flex; gap: var(--sp-2); align-items: end; flex-wrap: wrap; }
.ra { display: flex; flex-direction: column; gap: var(--sp-3); }
.ra__row { display: flex; gap: var(--sp-2); flex-wrap: wrap; align-items: center; }
.ra__cover { display: flex; align-items: center; gap: var(--sp-3); font-size: var(--fs-13); color: var(--text-secondary); }
.ra__cover img { max-height: 64px; border-radius: var(--radius-sm, 4px); border: 1px solid var(--border-default); }
.ra__frame { width: 100%; height: 70vh; border: 1px solid var(--border-default); border-radius: var(--radius-md); background: white; }
```

- [ ] **Step 4: 확인** — `npm test` 44 PASS. public import 빌드 가능성은 Task 9 Preview 빌드가 게이트(실패 시 이 Task 파일만 fix).
- [ ] **Step 5: 커밋**

```bash
git add components/report/ReviewSection.jsx components/report/PromptManagerModal.jsx components/report/report.css
git commit -m "feat: 검토의견 — 스트리밍 생성·프롬프트 CRUD 모달 (public/review 모듈 재사용)"
```

---

### Task 4: ④ 보고서 — 자산 로더·미리보기·다운로드·표지 + 페이지 조립

**Files:**
- Create: `lib/reportAssets.js`, `components/report/ReportActions.jsx`
- Modify: `app/project/[id]/report/page.js` (자리 페이지 → 전면 재작성), Create: `app/project/[id]/report/reportPage.css`

**Interfaces:**
- `lib/reportAssets.js`:
  - `loadReportAssets(): Promise<void>` — CDN 3종(`https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js`, `.../html2canvas/1.4.1/html2canvas.min.js`, `https://unpkg.com/docx@8/build/index.umd.js`) + `/report/reportBuilder.js`·`/report/reportPdf.js`·`/report/reportDocx.js` 로드(loadScriptOnce) + `engine/libraryLoader.js`에서 `normalize대지위치` import해 `window.normalize대지위치` 브리지. **reportViewer.js는 로드하지 않음**(legacy DOM 전용 — React 모달로 대체)
  - `buildReportData({ project, output1, output2, 카테고리 }): data` — reportBuilder 계약대로 조립: `input1`(+`카테고리`, `적용연면적: Number(연면적)||0` 주입), `output1`, `output2`, `reviewHTML`(review.text를 escape+`<br>` 변환), `projectName`, `coverImage`, `logoImage: null`
  - **구현 전 실측 의무**: `public/report/reportBuilder.js`·`reportPdf.js`·`reportDocx.js`가 클래식 스크립트로서 노출하는 전역 이름(`buildReport`/`downloadReportPDF`/`downloadReportHTML`/`downloadReportWord` — 함수 선언이면 window 전역)과 reportBuilder가 실제 사용하는 input1 필드를 읽고, 다르면 브리지에서 맞춰 조정 + 보고서 명기
- `<ReportActions project calcReady onCoverChange getReportData>` — 표지 이미지 업로드(FileReader→base64→`onCoverChange`)·미리보기(모달+iframe srcDoc)·PDF/HTML/Word 다운로드. calcReady false면 버튼 비활성
- 페이지: calc 재계산(Phase 3·4와 동일 패턴) → `calcData`·`카테고리` 파생 → ReviewSection + ReportActions 조립. `data.review`·`data.coverImage` 저장

- [ ] **Step 1: lib/reportAssets.js** (전문)

```js
"use client";
import { loadScriptOnce } from "@/lib/scriptLoader";

const CDN = [
  "https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js",
  "https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js",
  "https://unpkg.com/docx@8/build/index.umd.js",
];
const REPORT_SCRIPTS = ["/report/reportBuilder.js", "/report/reportPdf.js", "/report/reportDocx.js"];

export async function loadReportAssets() {
  const { normalize대지위치 } = await import("@/engine/libraryLoader.js");
  if (typeof window !== "undefined" && !window.normalize대지위치) {
    window.normalize대지위치 = normalize대지위치; // reportBuilder가 window 전역을 읽음
  }
  for (const src of [...CDN, ...REPORT_SCRIPTS]) await loadScriptOnce(src);
  const missing = ["buildReport", "downloadReportPDF", "downloadReportHTML", "downloadReportWord"]
    .filter(fn => typeof window[fn] !== "function");
  if (missing.length) throw new Error("보고서 모듈 전역 누락: " + missing.join(", "));
}

const esc = s => String(s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");

export function buildReportData({ project, output1, output2, 카테고리 }) {
  const d = project.data;
  return {
    input1: {
      ...d.input1,
      카테고리,
      적용연면적: Number(d.input1?.연면적) || 0,
    },
    output1,
    output2,
    reviewHTML: d.review?.text ? esc(d.review.text).replace(/\n/g, "<br>") : "",
    projectName: project.name,
    coverImage: d.coverImage ?? null,
    logoImage: null,
  };
}
```

- [ ] **Step 2: ReportActions.jsx** (전문)

```jsx
"use client";
import { useState } from "react";
import Button from "@/components/ui/Button";
import Modal from "@/components/ui/Modal";
import { loadReportAssets } from "@/lib/reportAssets";
import "./report.css";

export default function ReportActions({ coverImage, calcReady, onCoverChange, getReportData }) {
  const [previewHtml, setPreviewHtml] = useState(null);
  const [busy, setBusy] = useState(null); // "preview"|"pdf"|"html"|"word"|null
  const [error, setError] = useState(null);

  function pickCover(e) {
    const file = e.target.files?.[0];
    if (!file) return;
    const reader = new FileReader();
    reader.onload = () => onCoverChange(reader.result);
    reader.readAsDataURL(file);
    e.target.value = "";
  }

  async function withAssets(kind, fn) {
    setBusy(kind); setError(null);
    try { await loadReportAssets(); await fn(); }
    catch (e) { setError(e.message); }
    finally { setBusy(null); }
  }

  const preview = () => withAssets("preview", async () => setPreviewHtml(window.buildReport(getReportData())));
  const pdf = () => withAssets("pdf", async () => window.downloadReportPDF(window.buildReport(getReportData())));
  const html = () => withAssets("html", async () => window.downloadReportHTML(window.buildReport(getReportData())));
  const word = () => withAssets("word", async () => window.downloadReportWord(getReportData()));

  return (
    <div className="ra">
      <div className="ra__cover">
        <span>표지 이미지</span>
        {coverImage && <img src={coverImage} alt="표지 미리보기" />}
        <input type="file" accept="image/*" onChange={pickCover} />
        {coverImage && <Button size="sm" variant="ghost" onClick={() => onCoverChange(null)}>제거</Button>}
      </div>
      <div className="ra__row">
        <Button onClick={preview} disabled={!calcReady || !!busy}>{busy === "preview" ? "생성 중…" : "보고서 미리보기"}</Button>
        <Button variant="ghost" onClick={pdf} disabled={!calcReady || !!busy}>{busy === "pdf" ? "생성 중…" : "PDF 다운로드"}</Button>
        <Button variant="ghost" onClick={word} disabled={!calcReady || !!busy}>{busy === "word" ? "생성 중…" : "Word 다운로드"}</Button>
        <Button variant="ghost" onClick={html} disabled={!calcReady || !!busy}>{busy === "html" ? "생성 중…" : "HTML 다운로드"}</Button>
      </div>
      {!calcReady && <p className="rv__hint">①·② 입력을 완료하면 보고서를 생성할 수 있습니다.</p>}
      {error && <p className="rv__error" role="status">보고서 오류: {error}</p>}
      <Modal open={previewHtml !== null} onClose={() => setPreviewHtml(null)} title="보고서 미리보기">
        {previewHtml !== null && <iframe className="ra__frame" title="보고서 미리보기" srcDoc={previewHtml} />}
      </Modal>
    </div>
  );
}
```

- [ ] **Step 3: report/page.js 전면 재작성** (전문)

```jsx
"use client";
import { useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { getProject, updateProject } from "@/lib/projectStore";
import { useEngineReady } from "@/lib/useEngineReady";
import { calc규모등급, canCalculate, buildEngineInput2 } from "@/lib/calcModel";
import Card from "@/components/ui/Card";
import ReviewSection from "@/components/report/ReviewSection";
import ReportActions from "@/components/report/ReportActions";
import "./reportPage.css";

export default function ReportPage() {
  const { id } = useParams();
  const { ready, error: engineError } = useEngineReady();
  const [project, setProject] = useState(null);
  const [calc, setCalc] = useState(null); // { output1, output2 } | null

  useEffect(() => { setProject(getProject(id)); }, [id]);

  const input1 = project?.data.input1 ?? null;
  const input2 = project?.data.input2 ?? null;
  const check = canCalculate(input1 ?? {});
  const 카테고리 = calc규모등급(input1?.용도별연면적목록 ?? []);

  useEffect(() => {
    if (!ready || !input1 || !input2 || !check.ok) { setCalc(null); return; }
    let alive = true;
    import("@/engine/index.js")
      .then(({ runCalculation }) => runCalculation(input1, buildEngineInput2(input2.scenarios), 카테고리))
      .then(r => alive && setCalc(r))
      .catch(() => alive && setCalc(null));
    return () => { alive = false; };
  }, [ready, input1, input2, 카테고리, check.ok]); // eslint-disable-line react-hooks/exhaustive-deps

  function save(patch) {
    const next = updateProject(id, { data: patch });
    setProject(next);
  }

  if (!project) return null;

  const calcData = calc ? { input1, output1: calc.output1, output2: calc.output2 } : null;

  return (
    <div className="rp-page">
      <Card title="④ 보고서 — AI 검토의견">
        {engineError && <p className="rv__error" role="status">엔진 로드 실패: {engineError}</p>}
        {!ready && !engineError && <p className="rv__hint">계산 엔진 로딩 중…</p>}
        <ReviewSection calcData={calcData} review={project.data.review}
          onSave={text => save({ review: { text, at: Date.now() } })} />
      </Card>
      <Card title="보고서 출력">
        <ReportActions coverImage={project.data.coverImage ?? null} calcReady={!!calc}
          onCoverChange={img => save({ coverImage: img })}
          getReportData={() => buildReportData({ project, output1: calc.output1, output2: calc.output2, 카테고리 })} />
      </Card>
    </div>
  );
}
```

(상단 import에 `import { buildReportData } from "@/lib/reportAssets";` 포함.)

- [ ] **Step 4: reportPage.css** (전문)

```css
.rp-page { display: flex; flex-direction: column; gap: var(--sp-5); }
```

- [ ] **Step 5: 확인** — `npm test` 44 PASS. JSX 균형 재독.
- [ ] **Step 6: 커밋**

```bash
git add lib/reportAssets.js components/report/ReportActions.jsx app/project/[id]/report/page.js app/project/[id]/report/reportPage.css
git commit -m "feat: 보고서 — 자산 로더(CDN·report 전역)·미리보기 모달·PDF/Word/HTML·표지 이미지"
```

---

### Task 5: 제약조건 설정 모달 (Phase 4 이월)

**Files:**
- Create: `components/optimize/ConstraintsModal.jsx`
- Modify: `components/optimize/RecommendPanel.jsx` (제약 버튼·요약), `lib/recommendClient.js` (constraints 파라미터), `app/project/[id]/optimize/page.js` (모달 상태·저장·requestRecommend 전달), `app/project/[id]/optimize/optimize.css` (몇 줄)

**Interfaces:**
- `EMPTY_CONSTRAINTS = { solarNote: "", nearSubway: false, geothermalNote: "", fuelCellNote: "", budgetMin: "", budgetMax: "", excludeSources: [], customConstraints: "" }` — ConstraintsModal이 export
- `<ConstraintsModal open value energySources onClose onApply(next)>` — 8필드 폼 (legacy `#constraint-modal` 필드 구성 동일: 태양광/지열/연료전지 메모 textarea, 지하철 라디오, 예산 min/max number(만원), 제외 에너지원 체크박스(energySources 목록), 기타 textarea) + 초기화 버튼
- `constraintsSummary(c): string` — 활성 조건 수 요약("미설정" | "N개 조건") — RecommendPanel 표시용
- `requestRecommend({ ctx, candidates, constraints })` — `buildEvalMessage(ctx, candidates, constraints ?? {})`. 숫자 필드는 `Number()||0` 변환해 전달
- 저장: `data.aiConstraints` (데이터 계약)

- [ ] **Step 1: ConstraintsModal.jsx** (전문)

```jsx
"use client";
import { useEffect, useState } from "react";
import Modal from "@/components/ui/Modal";
import Button from "@/components/ui/Button";
import Field from "@/components/ui/Field";

export const EMPTY_CONSTRAINTS = {
  solarNote: "", nearSubway: false, geothermalNote: "", fuelCellNote: "",
  budgetMin: "", budgetMax: "", excludeSources: [], customConstraints: "",
};

export function constraintsSummary(c) {
  if (!c) return "미설정";
  let n = 0;
  if (c.solarNote) n++;
  if (c.nearSubway) n++;
  if (c.geothermalNote) n++;
  if (c.fuelCellNote) n++;
  if (c.budgetMin !== "" && c.budgetMin != null) n++;
  if (c.budgetMax !== "" && c.budgetMax != null) n++;
  if (c.excludeSources?.length) n++;
  if (c.customConstraints) n++;
  return n === 0 ? "미설정" : `${n}개 조건`;
}

const Memo = ({ label, value, onChange, placeholder }) => (
  <label className="cm__memo">
    <span>{label}</span>
    <textarea rows={2} value={value} placeholder={placeholder} onChange={e => onChange(e.target.value)} />
  </label>
);

export default function ConstraintsModal({ open, value, energySources, onClose, onApply }) {
  const [c, setC] = useState(EMPTY_CONSTRAINTS);
  useEffect(() => { if (open) setC({ ...EMPTY_CONSTRAINTS, ...(value ?? {}) }); }, [open, value]);
  const p = patch => setC(prev => ({ ...prev, ...patch }));

  return (
    <Modal open={open} onClose={onClose} title="AI 추천 제약조건">
      <div className="cm">
        <Memo label="태양광 조건" value={c.solarNote} onChange={v => p({ solarNote: v })} placeholder="예: 경사지붕, 주변 음영…" />
        <div className="cm__radio">
          <span>인근 지하철 노선</span>
          <label><input type="radio" name="cm-subway" checked={c.nearSubway === true} onChange={() => p({ nearSubway: true })} /> 있음</label>
          <label><input type="radio" name="cm-subway" checked={c.nearSubway === false} onChange={() => p({ nearSubway: false })} /> 없음</label>
        </div>
        <Memo label="지열 조건" value={c.geothermalNote} onChange={v => p({ geothermalNote: v })} placeholder="예: 암반, 지하수위…" />
        <Memo label="연료전지 조건" value={c.fuelCellNote} onChange={v => p({ fuelCellNote: v })} placeholder="예: 도시가스 인입, 공간 제한…" />
        <div className="cm__budget">
          <Field label="예산 최소 (만원)" type="number" mono value={c.budgetMin}
            onChange={e => p({ budgetMin: e.target.value === "" ? "" : Number(e.target.value) })} />
          <Field label="예산 최대 (만원)" type="number" mono value={c.budgetMax}
            onChange={e => p({ budgetMax: e.target.value === "" ? "" : Number(e.target.value) })} />
        </div>
        <div className="cm__exclude">
          <span>제외할 에너지원</span>
          <div className="cm__cbs">
            {energySources.map(src => (
              <label key={src}>
                <input type="checkbox" checked={c.excludeSources.includes(src)}
                  onChange={e => p({ excludeSources: e.target.checked
                    ? [...c.excludeSources, src]
                    : c.excludeSources.filter(s => s !== src) })} />
                {src}
              </label>
            ))}
          </div>
        </div>
        <Memo label="기타 제약" value={c.customConstraints} onChange={v => p({ customConstraints: v })} placeholder="예: 소음 규제, 문화재 보호구역…" />
        <div className="cm__actions">
          <Button size="sm" variant="ghost" onClick={() => setC({ ...EMPTY_CONSTRAINTS })}>초기화</Button>
          <Button size="sm" onClick={() => { onApply(c); onClose(); }}>적용</Button>
        </div>
      </div>
    </Modal>
  );
}
```

- [ ] **Step 2: recommendClient 수정** — `requestRecommend({ ctx, candidates })` → `requestRecommend({ ctx, candidates, constraints })`, 내부 `buildEvalMessage(ctx, candidates, constraints ?? {})`. 호출 전 숫자 변환은 페이지 책임 아님 — 클라이언트에서 `const cc = constraints ? { ...constraints, budgetMin: Number(constraints.budgetMin) || 0, budgetMax: Number(constraints.budgetMax) || 0 } : {};`로 정규화해 전달.

- [ ] **Step 3: RecommendPanel 수정** — props에 `constraintsLabel`·`onOpenConstraints` 추가, 버튼 옆에 `<Button size="sm" variant="ghost" onClick={onOpenConstraints}>제약조건</Button> <span className="rp__hint">{constraintsLabel}</span>` 삽입 (기존 구조 보존).

- [ ] **Step 4: optimize 페이지 연동** — `aiConstraints` 상태(로드 시 `p.data.aiConstraints ?? null`), `<ConstraintsModal open value energySources={lib?.에너지원목록 ?? []} onApply={c => { setAiConstraints(c); updateProject(id, { data: { aiConstraints: c } }); }}>`, `runRecommend`에서 `requestRecommend({ ctx, candidates, constraints: aiConstraints })`, RecommendPanel에 `constraintsLabel={constraintsSummary(aiConstraints)}` 전달. optimize.css에 추가:

```css
.cm { display: flex; flex-direction: column; gap: var(--sp-3); font-size: var(--fs-13); }
.cm__memo, .cm__exclude { display: flex; flex-direction: column; gap: var(--sp-1); color: var(--text-secondary); }
.cm__memo textarea { font: inherit; font-size: var(--fs-13); color: inherit; border: 1px solid var(--border-default); border-radius: var(--radius-sm, 4px); padding: var(--sp-2); background: transparent; resize: vertical; }
.cm__radio, .cm__cbs { display: flex; gap: var(--sp-3); flex-wrap: wrap; align-items: center; }
.cm__budget { display: grid; grid-template-columns: 1fr 1fr; gap: var(--sp-3); }
.cm__actions { display: flex; gap: var(--sp-2); justify-content: flex-end; }
```

- [ ] **Step 5: 확인** — `npm test` 44 PASS. JSX 균형 재독.
- [ ] **Step 6: 커밋**

```bash
git add components/optimize/ConstraintsModal.jsx components/optimize/RecommendPanel.jsx lib/recommendClient.js app/project/[id]/optimize/page.js app/project/[id]/optimize/optimize.css
git commit -m "feat: AI 추천 제약조건 모달 — 8필드·요약 배지·프롬프트 전달·저장 (Phase 4 이월)"
```

---

### Task 6: 카드별 AI 설명 + 조합 보고서 (Phase 4 이월)

**Files:**
- Create: `lib/optimizeExtras.js`
- Modify: `components/optimize/ComboCard.jsx` (AI 설명 버튼·표시), `app/project/[id]/optimize/page.js` (explains 상태·핸들러·조합 보고서 버튼), `components/optimize/comboCard.css` (몇 줄)

**Interfaces:**
- `lib/optimizeExtras.js`:
  - `loadOptimizeExtras(): Promise<void>` — `loadScriptOnce("/optimize/optimizeExplain.js")` + `loadScriptOnce("/optimize/optimizeReport.js")` → `window.OptimizeExplain`·`window.OptimizeReport` 확보 검증
  - `explainCard(card, ctx): Promise<string>` — `window.OptimizeExplain.explain(card, ctx)` 래핑 (endpoint `/api/recommend` — Phase 4에서 이미 포팅됨)
  - `openComboReport(r, ctx, explains, memos): void` — `window.OptimizeReport.openReport(...)` 래핑 (window.open 팝업, 팝업 내 메모 수정은 전달한 memos 객체에 반영됨)
- ComboCard 추가 props: `explain`(string|null 표시), `onExplain`(없으면 버튼 숨김), `explaining`(bool)
- 페이지: `data.optExplains` 저장. 조합 보고서 버튼("조합 보고서")은 결과 있을 때 표시 — 열기 전 explains·memos 최신본 전달, **팝업이 메모를 변경할 수 있으므로 window `focus` 이벤트에서 memos 재저장**(1회 등록·해제)

- [ ] **Step 1: lib/optimizeExtras.js** (전문)

```js
"use client";
import { loadScriptOnce } from "@/lib/scriptLoader";

export async function loadOptimizeExtras() {
  await loadScriptOnce("/optimize/optimizeExplain.js");
  await loadScriptOnce("/optimize/optimizeReport.js");
  if (!window.OptimizeExplain || !window.OptimizeReport) {
    throw new Error("최적화 부가 모듈 로드 실패");
  }
}

export async function explainCard(card, ctx) {
  await loadOptimizeExtras();
  return window.OptimizeExplain.explain(card, ctx);
}

export async function openComboReport(r, ctx, explains, memos) {
  await loadOptimizeExtras();
  window.OptimizeReport.openReport(r, ctx, explains, memos);
}
```

- [ ] **Step 2: ComboCard 수정** — props에 `explain, explaining, onExplain` 추가. 메모 label 위에 삽입:

```jsx
{onExplain && (
  <div className="cc__explain">
    <Button size="sm" variant="ghost" onClick={() => onExplain(combo)} disabled={explaining}>
      {explaining ? "AI 설명 생성 중…" : (explain ? "AI 설명 재생성" : "AI 설명 생성")}
    </Button>
    {explain && <p className="cc__explaintext">{explain}</p>}
  </div>
)}
```

(`Button` import 추가.) comboCard.css에 추가:

```css
.cc__explain { display: flex; flex-direction: column; gap: var(--sp-2); align-items: flex-start; }
.cc__explaintext { font-size: var(--fs-13); line-height: 1.6; background: var(--bg-card-inner, var(--color-na-bg)); border-radius: var(--radius-sm, 4px); padding: var(--sp-2) var(--sp-3); white-space: pre-wrap; }
```

- [ ] **Step 3: optimize 페이지 연동** — 상태 `const [explains, setExplains] = useState({});`·`const [explainingRank, setExplainingRank] = useState(null);`, 로드 시 `setExplains(p.data.optExplains ?? {})`. 핸들러:

```jsx
async function runExplain(combo) {
  if (!result) return;
  setExplainingRank(combo.rank);
  try {
    const text = await explainCard(combo, result.ctx);
    const next = { ...explains, [combo.rank]: text };
    setExplains(next);
    updateProject(id, { data: { optExplains: next } });
  } catch (e) {
    push({ message: `AI 설명 실패: ${e.message}`, tone: "fail" });
  } finally { setExplainingRank(null); }
}

async function openReport() {
  if (!result) return;
  const memosCopy = { ...memos };
  const onFocus = () => {
    setMemos({ ...memosCopy });
    updateProject(id, { data: { optMemos: { ...memosCopy } } });
    window.removeEventListener("focus", onFocus);
  };
  window.addEventListener("focus", onFocus);
  try { await openComboReport(result.r, result.ctx, { ...explains }, memosCopy); }
  catch (e) { window.removeEventListener("focus", onFocus); push({ message: e.message, tone: "fail" }); }
}
```

(`useToast` push — Phase 2·3 페이지와 동일 패턴으로 import·훅 추가. `explainCard`·`openComboReport` import.) "조합 순위" Card actions에 `<Button size="sm" variant="ghost" onClick={openReport}>조합 보고서</Button>` 추가. ComboCard 매핑에 `explain={explains[combo.rank] ?? null} explaining={explainingRank === combo.rank} onExplain={runExplain}` 추가.

- [ ] **Step 4: 확인** — `npm test` 44 PASS. JSX 균형 재독.
- [ ] **Step 5: 커밋**

```bash
git add lib/optimizeExtras.js components/optimize/ComboCard.jsx components/optimize/comboCard.css app/project/[id]/optimize/page.js
git commit -m "feat: 카드별 AI 설명·조합 보고서 팝업 — legacy 모듈 재사용, 메모 양방향 저장 (Phase 4 이월)"
```

---

### Task 7: 설정 모달 — SettingsUI 재사용

**Files:**
- Create: `components/settings/SettingsModal.jsx`, `components/settings/settingsModal.css`
- Modify: `app/project/[id]/optimize/page.js` (헤더 "설정" 버튼)

**Interfaces:**
- `<SettingsModal open onClose>` — Modal 안에 `<div id="settings-content">` 렌더 후 `loadScriptOnce("/settings/settingsUI.js")` → `window.SettingsUI.init()` 호출 (open 시마다 재호출 — 최신 상태 렌더). 엔진 ready 필수(SettingsStore는 engineScripts로 이미 로드됨)
- `settingsModal.css` — **legacy/style.css의 `.set-` 프리픽스 규칙 42건을 그대로 복사**(출처 주석 `/* legacy/style.css .set-* 복사본 — 원본 수정 금지 */` 필수, 색상 리터럴 허용 예외). 다크 배경 전제 스타일이면 모달 배경에 맞게 컨테이너 배경만 `#settings-content { }` 래퍼 규칙으로 보정(래퍼 규칙은 토큰 사용)
- 닫기 시: SettingsStore가 즉시 window 전역·localStorage에 반영하므로 별도 저장 없음. 단, 계산 결과는 파생값이라 **설정 변경 후 재계산 유도 안내** 문구 표시

- [ ] **Step 1: SettingsModal.jsx** (전문)

```jsx
"use client";
import { useEffect, useState } from "react";
import Modal from "@/components/ui/Modal";
import { loadScriptOnce } from "@/lib/scriptLoader";
import "./settingsModal.css";

export default function SettingsModal({ open, onClose }) {
  const [error, setError] = useState(null);

  useEffect(() => {
    if (!open) return;
    let alive = true;
    setError(null);
    loadScriptOnce("/settings/settingsUI.js")
      .then(() => {
        if (!alive) return;
        if (window.SettingsUI?.init) window.SettingsUI.init();
        else throw new Error("SettingsUI 초기화 실패");
      })
      .catch(e => alive && setError(e.message));
    return () => { alive = false; };
  }, [open]);

  return (
    <Modal open={open} onClose={onClose} title="설정 — 입력값 · 가중치 · 라이브러리">
      {error && <p className="sm__error" role="status">{error}</p>}
      <p className="sm__hint">변경 사항은 즉시 저장됩니다. 계산·최적화 결과에 반영하려면 해당 화면에서 다시 실행하세요.</p>
      <div id="settings-content" className="sm__content" />
    </Modal>
  );
}
```

- [ ] **Step 2: settingsModal.css** — 상단 래퍼 규칙(전문) + **legacy/style.css에서 `.set-`로 시작하는 규칙 전부 복사**(구현자가 `grep -n "\.set-" legacy/style.css`로 블록을 찾아 그대로 붙여넣기, 출처 주석 포함):

```css
.sm__hint { font-size: var(--fs-13); color: var(--text-secondary); margin-bottom: var(--sp-3); }
.sm__error { font-size: var(--fs-13); color: var(--color-fail); }
.sm__content { max-height: 65vh; overflow-y: auto; }
/* ↓ legacy/style.css .set-* 복사본 — 원본 수정 금지, 필요 시 배경·글자색만 아래 보정 */
```

복사 후 모달(라이트 배경)에서 대비가 깨지는 규칙이 있으면 `.sm__content .set-…` 오버라이드로 보정하고 보고서에 명기.

- [ ] **Step 3: optimize 페이지 연동** — `const [settingsOpen, setSettingsOpen] = useState(false);`, "③ 최적화 입력" Card의 actions를 `<><Button size="sm" variant="ghost" onClick={() => setSettingsOpen(true)}>설정</Button><Button onClick={runOptimize} …기존…</Button></>`로 확장, JSX 말미에 `<SettingsModal open={settingsOpen} onClose={() => setSettingsOpen(false)} />`.

- [ ] **Step 4: 확인** — `npm test` 44 PASS.
- [ ] **Step 5: 커밋**

```bash
git add components/settings/SettingsModal.jsx components/settings/settingsModal.css app/project/[id]/optimize/page.js
git commit -m "feat: 설정 모달 — legacy SettingsUI 재사용(스크립트 로드·init), .set-* 스타일 복사"
```

---

### Task 8: 대시보드 — 구버전 프로젝트 가져오기

**Files:**
- Modify: `app/page.js` (구버전 감지·가져오기 버튼), 필요 시 해당 페이지 CSS 몇 줄

**Interfaces:**
- Consumes: `readLegacyProjects`·`convertLegacyProject`(Task 1), `createProject`·`updateProject`(projectStore), `useToast`
- 동작: 마운트 시 `localStorage.getItem("projects_v2")` 감지(useEffect) → 있으면 "구버전 프로젝트 가져오기 (N개)" 버튼 표시 → 클릭 시 각 항목 `convertLegacyProject` → null 아닌 것만 `createProject(name)` 후 `updateProject(id, { data })` → 토스트 "N개 가져옴 (실패 M개)" → 목록 갱신. **projects_v2는 삭제하지 않음**. 중복 실행 방지: 가져온 후 세션 상태로 버튼 숨김(영구 플래그 불필요 — 사용자가 원하면 재실행 가능하도록 단순 유지)

- [ ] **Step 1: app/page.js 수정** — 기존 대시보드 구조를 읽고(수술적), 다음을 추가:

```jsx
import { readLegacyProjects, convertLegacyProject } from "@/lib/migrateLegacy";
// 컴포넌트 안:
const [legacyCount, setLegacyCount] = useState(0);
useEffect(() => {
  setLegacyCount(readLegacyProjects(localStorage.getItem("projects_v2")).length);
}, []);

function importLegacy() {
  const items = readLegacyProjects(localStorage.getItem("projects_v2"));
  let ok = 0, fail = 0;
  for (const item of items) {
    const conv = convertLegacyProject(item);
    if (!conv) { fail++; continue; }
    const p = createProject(conv.name);
    updateProject(p.id, { data: conv.data });
    ok++;
  }
  refresh(); // 기존 목록 갱신 함수명은 실제 파일 기준으로 맞출 것 (없으면 listProjects 재호출 setState)
  push({ message: `구버전 프로젝트 ${ok}개 가져옴${fail ? ` (형식 불일치 ${fail}개 제외)` : ""}`, tone: "pass" });
  setLegacyCount(0);
}
// JSX — 프로젝트 생성 버튼 근처:
{legacyCount > 0 && (
  <Button variant="ghost" onClick={importLegacy}>구버전 프로젝트 가져오기 ({legacyCount}개)</Button>
)}
```

(`createProject`·`updateProject`·`useToast`·`Button` 등 기존 import 재사용. 기존 목록 갱신 방식은 파일 실측 후 맞추고 보고서 명기.)

- [ ] **Step 2: 확인** — `npm test` 44 PASS. JSX 균형 재독.
- [ ] **Step 3: 커밋**

```bash
git add app/page.js
git commit -m "feat: 대시보드 — 구버전(projects_v2) 프로젝트 가져오기 (복사 방식, 원본 보존)"
```

---

### Task 9: 통합 검증 — 회귀 + Preview E2E (기능 전수)

**Files:** 원칙적으로 수정 없음 (결함 발견 시 이번 Phase 산출 파일만 fix 커밋)

- [ ] **Step 1: 회귀** — `npm test`(44 PASS) + verify 스냅샷 2종 IDENTICAL(stdout만 비교 — `2>/dev/null`)
- [ ] **Step 2: 푸시+배포** — push → 배포 READY 확인(웹훅 누락 시 빈 커밋 재트리거). **Preview 빌드 성공 자체가 public/review ES 모듈 import의 게이트**
- [ ] **Step 3: API 스모크(컨트롤러 권장)** — share URL 쿠키로 `POST /api/review` 최소 페이로드 → SSE 200 확인
- [ ] **Step 4: Preview E2E** (Playwright msedge + share URL — 시드: Phase 4와 동일 input1/input2(+세대수 케이스 무관)):
  1. **검토의견**: `/project/{id}/report` → 프롬프트 select에 "기본 검토 프롬프트"류 1개 이상 → [검토의견 생성] → 스트리밍 텍스트 증가 관찰(폴링) → 완료 후 텍스트에 "5,492,250" 또는 "ALT" 포함 확인 → 새로고침 → 저장된 검토의견 표시
  2. **프롬프트 CRUD**: 관리 모달 열기 → 새 프롬프트 "테스트P" 생성 → select에 나타남 → 삭제 → 사라짐. 기본 프롬프트 삭제 버튼 disabled
  3. **보고서**: [보고서 미리보기] → 모달 iframe 내 "신재생에너지 검토 보고서"·"5,492,250" 텍스트 확인(frame locator). PDF/Word/HTML 다운로드는 각 버튼 클릭 → `page.waitForEvent("download")`로 파일명 3종(`신재생에너지검토_*.pdf/.docx/.html`) 수신 확인 (다운로드 폴더는 임시 — 파일 내용 검사 불필요, 크기>0만)
  4. **표지 이미지**: 작은 PNG를 setInputFiles로 업로드 → 미리보기 img 표시 → 새로고침 유지
  5. **제약조건**: /optimize → [제약조건] 모달 → 예산최대 50000·태양광 메모 입력 → 적용 → 요약 "2개 조건" → 새로고침 유지 → [AI 추천 받기] 정상 완료(제약 포함 — 총평 표시)
  6. **AI 설명**: 탐색 후 #1 카드 [AI 설명 생성] → 설명 문단 표시(4~6문장) → 새로고침+재탐색 후 유지(optExplains 저장)
  7. **조합 보고서**: [조합 보고서] → 팝업(context.waitForEvent("page")) → 팝업 내 "#1"·메모 textarea 존재 확인 → 팝업 닫기
  8. **설정 모달**: [설정] → "최적화 가중치 · 상수" 텍스트·라이브러리 카드 렌더 확인 → 닫기 (값 변경은 파괴적이라 스킵 — 렌더만)
  9. **마이그레이션**: 대시보드에서 `localStorage.setItem("projects_v2", …구버전 1건…)` 시드 → 새로고침 → "구버전 프로젝트 가져오기 (1개)" 버튼 → 클릭 → 목록에 나타남 → 그 프로젝트 /calc에서 결과 렌더(5,492,250) + /report에서 검토의견 텍스트 표시 → `projects_v2` 원본 잔존 확인
  10. `/engine-check` 5492250, 콘솔 에러 0(허용 예외 외 — CDN 로드는 허용 도메인), 반응형 1024/768/375 (report·optimize 페이지)
- [ ] **Step 5: 정리** — 프로세스 종료, 산출물은 sdd 워크스페이스만, main 무영향

Expected: 마스터플랜 "기존 기능 전수 체크리스트" 중 Phase 5 담당 항목 전부 동작 (남는 것은 Phase 6 검증·배포뿐).

---

## Self-Review 결과

- **스펙 커버리지**: 마스터플랜 Phase 5 행 — 보고서 미리보기+PDF/Word/HTML+메모란(Task 4·6), AI 검토의견+프롬프트 CRUD(Task 2·3), 설정 모달+import/export(Task 7 — export/import은 SettingsUI 내장 버튼 재사용), 저장/불러오기+구버전 마이그레이션(Task 1·8). **Phase 4 이월 2건 포함**: AI 설명(Task 6)·제약조건 모달(Task 5). 표지 이미지(legacy 기능, 보고서 데이터 계약 필수)도 Task 4에 포함
- **플레이스홀더 스캔**: 없음 — 전 코드 전문 포함. Task 7 Step 2(.set-* 복사)·Task 8(기존 파일 수술 수정)은 실측 기반 절차로 구체화
- **타입 일관성**: `data.review/coverImage/optExplains/aiConstraints` ↔ 각 컴포넌트 props ↔ 저장 코드 일치. `requestRecommend` constraints 시그니처 ↔ Task 5 Step 2·4 일치. `buildReportData` 반환 ↔ reportBuilder 입력 계약(탐색 보고서) 일치. `EMPTY_CONSTRAINTS` 필드 ↔ legacy collectConstraints 8필드 일치
- **주의 승계**: public/ 무수정(로드만), 검토의견 plain text, 결과 파생값 미저장 원칙 유지, projects_v2 보존
