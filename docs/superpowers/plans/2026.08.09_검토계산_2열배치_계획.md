# 검토 계산 2열 배치 + 판정 고정 바 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 검토 계산 화면을 ≥1280px에서 좌(입력)·우(결과, sticky) 2열로 재배치하고, 그 미만에서는 판정 요약 고정 바를 띄운다.

**Architecture:** 마크업 3영역(전폭 안내 / 입력 열 / 결과 열) 재배치 + CSS 그리드·sticky. 계산 훅·엔진 호출·데이터 계약은 무변경. 판정 고정 바는 기존 `pickRepresentative` 재사용하는 표시 전용 컴포넌트.

**Tech Stack:** Next.js(App Router, JS), Vanilla CSS(기존 토큰), `node --test`, Playwright(msedge).

**스펙:** `docs/superpowers/specs/2026.08.09_검토계산_2열배치_설계.md` (승인됨)

## Global Constraints

- `legacy/`·`engine/`·`public/` 무수정, 데이터 스키마 무변경(파생값 저장 금지)
- 신규 색 리터럴 금지 — 기존 토큰만 (`--color-pass/-bg`, `--color-fail/-bg`, `--color-na/-bg`, `--sp-*`, `--fs-*`, `--radius-*`)
- 테스트: `npm test` = `node --test "tests/*.test.mjs"`, 현재 61/61 통과 상태에서 시작
- 로컬 `npm run build`/`dev` 금지(Dropbox 느림) — 시각 검증은 Vercel Preview
- 브랜치 `feature/calc-two-column`. 커밋 `<type>: <한국어 설명>`. main 머지는 계획 밖(사용자 승인 후)
- 기존 인터페이스(변경 금지): `pickRepresentative(output2)` → 대표 ALT 또는 null (`lib/projectSummary.js`) · `VERDICT_UI[만족여부]` → `{tone,label,sym}` · `Output1Table({output1})` · `Output2Panel({alt})` · `ScenarioEditor({scenarios, lib, onChange})` · `Card({title, actions, inner, children})` · `Badge({tone, children})`
- **중첩 스크롤 금지**: 좌우 열에 독립 스크롤 컨테이너를 만들지 않는다. 페이지 스크롤 하나 + 우측 열 sticky만 사용

---

### Task 1: 판정 고정 바 컴포넌트

**Files:**
- Create: `components/calc/VerdictSticky.jsx`, `components/calc/verdictSticky.css`

**Interfaces:**
- Consumes: `pickRepresentative`, `VERDICT_UI` (lib/projectSummary.js), `Badge`
- Produces: `<VerdictSticky output2={result.output2} />` — Task 2가 사용. output2가 비었거나 대표 없음이면 `null` 반환

- [ ] **Step 1: 컴포넌트 작성**

```jsx
// components/calc/VerdictSticky.jsx — 좁은 화면(<1280px)에서 판정 요약을 상단 고정
"use client";
import Badge from "@/components/ui/Badge";
import { pickRepresentative, VERDICT_UI } from "@/lib/projectSummary";
import "./verdictSticky.css";

export default function VerdictSticky({ output2 }) {
  const rep = pickRepresentative(output2);
  if (!rep) return null;
  const ui = VERDICT_UI[rep.만족여부] ?? VERDICT_UI.해당없음;
  const others = (output2?.length ?? 0) - 1;
  return (
    <div className={`vsticky vsticky--${ui.tone}`} role="status" aria-live="polite">
      <span className="vsticky__id mono">{rep.id}</span>
      <strong className="vsticky__ratio mono">{rep.비율.toFixed(1)}%</strong>
      <span className="vsticky__req">의무 {rep.의무비율 !== null ? `${rep.의무비율}%` : "-"}</span>
      <Badge tone={ui.tone}>{ui.sym} {ui.label}</Badge>
      {others > 0 && <span className="vsticky__more">외 {others}개</span>}
    </div>
  );
}
```

- [ ] **Step 2: CSS 작성**

```css
/* components/calc/verdictSticky.css — 좁은 화면 전용 판정 고정 바 */
.vsticky {
  display: flex; align-items: baseline; gap: var(--sp-3); flex-wrap: wrap;
  position: sticky; top: 0; z-index: 5;
  padding: var(--sp-3) var(--sp-4); margin-bottom: var(--sp-2);
  border-radius: var(--radius-md); border-inline-start: 4px solid;
  box-shadow: var(--shadow-card);
}
.vsticky--pass { background: var(--color-pass-bg); border-color: var(--color-pass); }
.vsticky--fail { background: var(--color-fail-bg); border-color: var(--color-fail); }
.vsticky--na { background: var(--color-na-bg); border-color: var(--color-na); }
.vsticky__id { font-size: var(--fs-12); color: var(--text-secondary); }
.vsticky__ratio { font-size: var(--fs-18); font-weight: 800; line-height: 1.1; }
.vsticky__req, .vsticky__more { font-size: var(--fs-12); color: var(--text-secondary); }

/* 2열 배치에서는 우측 열 상단의 판정 밴드가 그 역할을 하므로 숨김 */
@media (min-width: 1280px) { .vsticky { display: none; } }
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → 61/61 PASS (순수 로직 신규 없음 — 대표 선택은 기존 테스트가 커버)

```bash
git add components/calc/VerdictSticky.jsx components/calc/verdictSticky.css
git commit -m "feat: 판정 고정 바 컴포넌트 — 좁은 화면에서 설치비율·판정 상시 표시"
```

---

### Task 2: 검토 계산 화면 2열 재배치

**Files:**
- Modify: `app/project/[id]/calc/page.js`, `app/project/[id]/calc/calc.css`

**Interfaces:**
- Consumes: Task 1의 `VerdictSticky`. 기존 상태·훅(`result`, `input2`, `lib`, `check`, `카테고리`, `notices`, `loading`, `applyInput2`)은 그대로 사용

- [ ] **Step 1: page.js의 `return (...)` 블록만 교체** (import 1줄 추가, 그 위 로직 전부 무변경)

임포트 추가:

```js
import VerdictSticky from "@/components/calc/VerdictSticky";
```

`return (` 부터 파일 끝까지를 다음으로 교체:

```jsx
  return (
    <div className="calc">
      {(notices.length > 0 || loading) && (
        <Card title="② 검토 계산" className="calc__full">
          {notices.map(n => <p key={n} className="calc__notice" role="status">{n}</p>)}
          {loading && <p className="calc__hint">계산 엔진 로딩 중…</p>}
        </Card>
      )}

      {result && <VerdictSticky output2={result.output2} />}

      <div className="calc__cols">
        <div className="calc__col calc__col--input">
          <Card title="시나리오 (ALT)">
            {lib && input2
              ? <ScenarioEditor scenarios={input2.scenarios} lib={lib} onChange={applyInput2} />
              : <p className="calc__hint">계산 엔진 로딩 중…</p>}
          </Card>
        </div>

        <div className="calc__col calc__col--result">
          <Card title="신재생에너지 설치비율 (Output 2)">
            {result
              ? result.output2.map(alt => <Output2Panel key={alt.id} alt={alt} />)
              : <p className="calc__hint">시나리오 구성과 계산이 완료되면 의무비율 대비 판정 결과가 여기 표시됩니다.</p>}
          </Card>

          <Card title="② 예상에너지사용량 (Output 1)" actions={
            <span className="calc__cat" title="주거: 세대수 기준 / 비주거: 용도별 연면적 합계 기준으로 자동 결정됩니다">
              규모등급 <Badge tone={카테고리 ? "brand" : "na"}>{카테고리 || "미판정"}</Badge>
            </span>
          }>
            {result
              ? <Output1Table output1={result.output1} />
              : <p className="calc__hint">① 사업정보 입력이 완료되면 용도별 예상에너지사용량이 여기 표시됩니다.</p>}
          </Card>
        </div>
      </div>
    </div>
  );
}
```

주의: `Card`는 `className` prop을 받는다(components/ui/Card.jsx). 결과 열 순서는 **판정(Output2) → Output1** — 사용자가 가장 먼저 볼 값이 판정이기 때문이다.

- [ ] **Step 2: calc.css 교체**

```css
.calc { display: flex; flex-direction: column; gap: var(--sp-5); }
.calc__cat { display: flex; align-items: center; gap: var(--sp-2); font-size: var(--fs-13); color: var(--text-secondary); }
.calc__notice { font-size: var(--fs-13); color: var(--accent-warm-dim); }
.calc__hint { font-size: var(--fs-13); color: var(--text-secondary); }

/* 기본(좁은 화면): 1열 — 입력 → 결과 순서 */
.calc__cols { display: flex; flex-direction: column; gap: var(--sp-5); }
.calc__col { display: flex; flex-direction: column; gap: var(--sp-5); min-width: 0; }

/* ≥1280px: 좌 입력 · 우 결과(sticky) 2열 — 시나리오↔결과 왕복 제거 */
@media (min-width: 1280px) {
  .calc__cols {
    display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1.1fr);
    gap: var(--sp-5); align-items: start;
  }
  .calc__col--result {
    position: sticky; top: var(--sp-4); align-self: start;
    max-height: calc(100vh - var(--sp-8)); overflow-y: auto; overscroll-behavior: contain;
  }
}
```

- [ ] **Step 3: 테스트 + 커밋**

Run: `npm test` → 61/61 PASS

```bash
git add "app/project/[id]/calc/page.js" "app/project/[id]/calc/calc.css"
git commit -m "feat: 검토 계산 2열 배치 — 좌 시나리오·우 결과(sticky), 좁은 화면 판정 고정 바"
```

---

### Task 3: E2E 게이트 (컨트롤러 실행)

**Files:** 변경 없음 — 기존 `verify/e2e_redesign.mjs` 재사용

- [ ] **Step 1: 푸시** — `git push -u origin feature/calc-two-column`, Vercel 프리뷰 빌드 대기(commit status success). 프리뷰 도메인은 `gh api repos/jlaw080-ops/Renewable-Proposal/commits/<sha>/check-runs --jq '.check_runs[].output.summary'`에서 추출
- [ ] **Step 2: E2E 실행** — `export $(grep VERCEL_AUTOMATION_BYPASS_SECRET .env.local) && node verify/e2e_redesign.mjs <프리뷰URL> verify/e2e_out` → Expected `PASS: 오버플로 0 · 모달 트랩 통과`
- [ ] **Step 3: 2열/1열 스크린샷 육안** — 1440px(2열·우측 sticky 동작)과 375px(1열·판정 고정 바) 확인. 1280px 경계에서 레이아웃이 깨지지 않는지도 확인
- [ ] **Step 4: 보고** — 게이트 결과 정리 후 사용자에게 보고. main 머지는 사용자 승인 후

## Self-Review 기록

- 스펙 §1 배치→Task 2, §2 고정 바→Task 1, §3 구현 방식→Task 1·2, §4 게이트→Task 3. 갭 없음
- 타입 일관성: `VerdictSticky({output2})` (T1 정의 = T2 사용), `pickRepresentative`/`VERDICT_UI` 시그니처는 기존 모듈 그대로
- 중첩 스크롤 제약: 우측 열에만 `overflow-y: auto`가 붙지만 이는 sticky 열이 뷰포트보다 길 때의 안전장치이며 좌측 열·페이지 스크롤과 충돌하지 않음. `overscroll-behavior: contain`으로 스크롤 체이닝 차단
