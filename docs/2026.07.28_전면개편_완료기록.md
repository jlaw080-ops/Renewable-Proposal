# 신재생에너지 검토 도구 전면 개편 — 완료 기록 (2026-07-28)

바닐라 JS 단일 페이지(구앱) → **Next.js App Router 웹앱** 전면 개편 프로젝트가 완결되어 프로덕션(`renewable-proposal.vercel.app`)이 새 앱으로 전환됨. 계산 엔진(`engine/`·`public/optimize/` 등)은 무수정 재사용, verify 스냅샷으로 개편 전후 계산 동일성을 전 구간 증명.

## Phase별 요약 (계획 문서: `docs/superpowers/plans/`)

| Phase | 내용 | 완료일 |
|-------|------|--------|
| 0. 기반 | verify 스냅샷 하니스, 레포 재구성(public/·legacy/), Next.js 스캐폴드, 엔진 로더, /engine-check | 07-27 |
| 1. 셸·디자인 | 토큰 42종, 공통 컴포넌트 9종, 대시보드(프로젝트 CRUD), 워크스페이스 4단계 네비 | 07-27 |
| 2. 사업정보+지도 | 폼·3경로 지역 확정(드롭다운/주소검색/지도클릭)·토지정보 자동 채움. NCP 신규 Maps 도메인 전환 | 07-28 |
| 3. 검토 계산 | ALT 편집기(계수 자동), runCalculation 자동 재계산, Output1/2·판정, 규모등급 자동(세대수) | 07-28 |
| 4. 최적화+AI 추천 | 가용면적·요구도 입력, 조합 카드 20(챔피언·태그·메모), Gemini AI 추천(⭐근거) | 07-28 |
| 5. 보고서·검토·설정·저장 | 검토의견 스트리밍·프롬프트 CRUD, PDF/Word/HTML, AI 설명·제약조건·조합 보고서, 설정 모달, 구버전 마이그레이션 | 07-28 |
| 6. 검증·배포 | 전수 체크리스트 11항목, 통합 E2E(Edge·Chrome), **main 머지 6e73ace → 프로덕션 전환**, Lighthouse 85/95/96 | 07-28 |

## 품질 지표

- 단위테스트: node:test **44/44** (regionResolver·landinfo·calcModel·optimizeModel·migrateLegacy·projectStore)
- 엔진 동일성: `verify_all`(최적화 30케이스)·`verify_output12` 스냅샷 **IDENTICAL** 전 Phase 유지
- E2E: Vercel Preview/프로덕션 Playwright(Edge·Chrome) — 신규 사용자 전 여정 완주, 기준값(5,492,250 / 64,505 / 1.2% / 의무비율 14%) 화면-엔진 일치
- 프로덕션: 스모크 PASS, Lighthouse Performance 85 · Accessibility 95 · Best Practices 96

## 개발 중 해결한 주요 이슈

1. NCP 지오코딩 "구독 필요" 오류 → 신규 Maps 게이트웨이(`maps.apigw.ntruss.com`) 전환 + 콘솔 구독 체크로 해결
2. Gemini 2.5 Flash thinking 토큰이 출력 예산 잠식 → JSON 절단 → `thinkingBudget: 0` + 8192로 해결
3. legacy `buildEvalMessage`의 constraints 널 가드 부재 → 빈 객체 전달
4. `npm test`가 Windows/Node 25에서 디렉토리 인자 미지원 → 글롭 패턴 전환
5. Vercel 웹훅 간헐 누락 → 빈 커밋 재트리거로 대응

## 보수작업 1차 결과 (2026-07-28 저녁, 머지 20b9de5)

- [x] **검토계산 레이아웃**: 예상에너지사용량(Output1) 최상단 → 시나리오 → 설치비율(Output2) 순서로 변경
- [x] **보고서 무스타일 출력 수정**: 원인 = reportStyle.css 상대경로가 새 앱 경로(/project/…/report)에서 어긋남 → `lib/reportAssets.buildStyledReport()`가 CSS를 인라인 주입(미리보기·PDF·HTML 다운로드 file:// 모두 보장). 구앱 문서 디자인(A4·표지) 복원됨 — 추가 리디자인 여부는 사용자 확인 후 결정
- [x] **최적화 입력 간소화**: 가용면적·사용자 요구도를 설정 모달 상단 "최적화 입력 — 이 프로젝트" 섹션으로 이동(`OptimizeInputSettings` 분리, 데이터는 input3 프로젝트별 유지, 건물유형 표준값 자동 반영 유지)
- [x] **넓은 모달 옵션**: 보고서 미리보기·설정 모달 900px (Modal `wide` prop)
- [x] **네이버지도 인증 실패 — 해결 (2026-07-29)**: 원인 = Vercel `NEXT_PUBLIC_NCP_CLIENT_ID`에 Client Secret 형태의 40자 값이 들어 있었음(auth 401 실측). 사용자가 올바른 Client ID로 수정 + 대시보드 Redeploy(캐시 미사용) → 번들에 새 키 반영 확인, auth 200·타일 렌더·마커 표시·콘솔 에러 0. **경로1(주소검색→토지정보 자동채움)·경로3(지도클릭→역지오코딩→확정위치 갱신) 프로덕션 E2E 통과.** ⚠️ 후속: 구 번들에 Client Secret이 공개 노출됐었으므로 NCP 콘솔에서 **Client Secret 재발급 + Vercel `NCP_CLIENT_SECRET` 교체 + 재배포** 권장

## 보수작업 목록 (최초 기록)

**사용자 확인 (우선)**
- [ ] 프로덕션 대시보드 "구버전 프로젝트 가져오기"로 **실데이터 마이그레이션 확인** (projects_v2 원본 보존됨 — 실패해도 무손실)
- [x] NCP 콘솔: Web Dynamic Map + 웹 서비스 URL 등록 → 지도 타일 표시 확인 완료 (2026-07-29, 프로덕션 auth 200·타일 200)

**개선 파킹 (Phase 5·6 최종리뷰 권고, 비긴급)**
- [x] 지도 타일 활성화 후 경로3(지도클릭) E2E 재검증 — 완료 (2026-07-29, reverse-geocode 200·확정위치 갱신 확인)
- [x] **NCP Client Secret 재발급(보안)**: 사용자 재발급 + Vercel `NCP_CLIENT_SECRET` 교체 완료 (2026-07-29) — 다음 프로덕션 배포 시 반영
- [ ] 조합 보고서 팝업 focus 리스너 엣지케이스 모니터링 (`app/project/[id]/optimize/page.js`)

## 보수작업 2차 결과 (2026-07-29)

- [x] **CDN SRI 해시**: jsPDF 2.5.1·html2canvas 1.4.1·docx SHA-384 무결성 검증 추가 (`loadScriptOnce`에 integrity+crossorigin 옵션). docx는 `@8` 플로팅 → **8.6.0 고정** (플로팅 버전에 SRI를 붙이면 CDN 패치 시 로드 파손)
- [x] **표지 이미지 2MB 제한**: `ReportActions.pickCover`에 파일 크기 검증 (localStorage 한도 보호, 초과 시 크기 안내 오류)
- [x] **favicon**: `app/icon.svg` (브랜드 그린 #2EA043 + 볼트) — 콘솔 404 제거
- [x] **package.json `"type": "module"`**: MODULE_TYPELESS 경고 원인 = CJS verify가 ESM engine/index.js를 동적 import. verify 스크립트 2종 `.cjs` 개명 + type 필드 추가. verify exit 0·경고 0·테스트 44/44 확인
- [x] **SettingsUI.init 멱등성 — 실버그 발견·수정**: 프로덕션 실측으로 결함 확정 (첫 오픈 15개 요소 → 닫고 재오픈 **0개**, 빈 모달). 원인 = `_built` 모듈 플래그 가드인데 React 모달이 닫힘/페이지 이동 시 `#settings-content` DOM을 파괴 → 재구축 차단. 수정 = 가드를 `_built && host.childElementCount > 0`로 변경 (host 비면 재구축 허용, 구앱 동작에는 영향 없음 — 구앱은 host DOM 상시 유지)

**롤백 절차 (비상)**: `git revert -m 1 6e73ace` → main 푸시 → 구앱 복원 (localStorage 무손실)
